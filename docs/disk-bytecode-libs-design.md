# Archived disk bytecode library design

This compatibility path is retained because the frozen semantic-contract registry for release 1.0 refers to it by name.

The historical design now lives at [the pre-1.0 archive](archive/pre-1.0/designs/disk-bytecode-libs-design.md). It is not a current implementation contract.
