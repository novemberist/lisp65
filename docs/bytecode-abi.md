# Archived bytecode ABI contract

This compatibility path is retained because the frozen Dialect V1 contract
refers to it by name.

The current contract lives at [the bytecode ABI contract](contracts/bytecode-abi.md).
This pointer is historical and must not be used as a separate contract copy.
