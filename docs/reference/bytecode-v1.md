# Archived bytecode v1 reference

This compatibility path is retained because the frozen semantic-contract registry for release 1.0 refers to it by name.

The historical document now lives at [the pre-1.0 archive](../archive/pre-1.0/reference/bytecode-v1.md). It is not part of the current language or product contract.
