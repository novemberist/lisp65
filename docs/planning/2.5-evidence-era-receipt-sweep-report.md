# Block 2.5 evidence-era and receipt sweep

Status: **complete; midnight drift closed**.

The first full closing run crossed midnight.  It exited zero, but the tree was
not idempotent: five repeatable receipt writers changed `recorded_on` from
2026-08-30 to 2026-08-31, and two downstream hashes followed.  The product
world did not change.  All five writers now use the sealing receipt's existing
date through `evidence_era.stable_recorded_on`; a repeatable check no longer
makes history depend on wall-clock time.

Two consecutive complete runs over commit `9df4b8c0` then passed:

| Run | Wall time | Exit | Log SHA-256 | Tree afterwards |
|---|---:|---:|---|---|
| 1 | 1,086.66 s | 0 | `8e1439be…` | clean |
| 2 | 1,090.41 s | 0 | `a02d0854…` | clean |

Both logs contain 2,491 lines and 155,638 bytes.  Their sole differing line is
the live `workspace-capacity-check` free-space observation (163,840 bytes of
environmental movement); the other 2,490 lines are identical.  No receipt or
tracked byte changed between or after the runs.

The permanent audit is
`tools/host-lisp/receipt_idempotence_audit.py`.  It guards the five corrected
writers and inventories remaining dynamic-date sites without pretending that
one-shot device/contact producers are repeatable receipt generators.

The final Block-2.5 closing commit will repeat the two-run certification after
all remaining read-only audits and public-document changes are present.
