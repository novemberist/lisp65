# v1.9.0 Blocks A+B r7 device result

Date: 2026-08-30

Status: **device rows green; forced-collection device subclaim review-pending**

The artifact-only product medium `V19R7P.D81` was uploaded over JTAG, read
back byte-for-byte and mounted before boot. Its readback SHA-256 was
`9bc5d45db0c0280ce8f067856dee98ed1cc14aec256398c5e93eb1b56bb06412`.
No optional library medium was mounted and no optional library was loaded.

The owner observed the repaired composed display exactly as emitted: the
native `lisp65>` prompt and editable cursor shared row 24, with no scattered
positioning blanks and no second cursor. Native prompt editing produced
`(1 2 3)` after Left/insert, and the full C-b/C-f, C-a/C-e, Backspace, C-d
and boundary sequence produced `"abc"`. `*** reader: invalid token` did not
appear. This hardware-pensions the v1.8 native-prompt cursor limitation.

Normal and fast physical typing were visually lossless. The owner shortened
the scripted long-line stimulus because it was impractical to count at the
scrolling screen edge. The script also used the wrong public oracle:
`length` is the Dialect-v2 list operation; strings use `string-length`.
Therefore this contact claims the observed device losslessness, but **does not
claim that a collection was independently witnessed on hardware**. The r7
final-world host wall remains independently green at 94 produced / 94
captured / 94 drained, zero drops, with the delivered `read-line` lifecycle
armed. Whether that combined evidence closes Block A is the next independent
review decision; this result does not silently promote the missing device
subclaim.

The remaining rows were green. Explicit `(read-line)` returned `"abcde"`
after cursor insertion; physical RUN/STOP returned to one live prompt; the A0
type-error path recovered practically immediately and `(list 1 3)` then
returned `(1 3)`. The absent INIT.L65 path was silent. All four performance
smokes took zero frames and returned `2`, `(9 2)`, `98`, and `42`.

The final read-only stopped-state D5 observation used r7 ElfTruth addresses
`nsym=$005A` and `npool=$BE1A`. It read `nsym=645` and `npool=8741`, leaving
**107 symbol slots and 1,467 name bytes**, safely above the release-terminal
floor of 32/384. The device was not resumed after that capture.

Block B is hardware-green. Block A is device-green for user-visible ordinary
and fast input, while its forced-collection hardware qualifier remains
explicitly review-pending. Comfort, Matcher/Blink, Blocks C/D, `$22` closure,
Ship and publication remain outside this result.
