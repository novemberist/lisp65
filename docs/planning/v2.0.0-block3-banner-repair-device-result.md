# v2.0 Block 3 banner-repair device result

Status: **FIRST RED — BLOCK 3 DESCOPED FROM V2.0**

The repaired product medium was uploaded and read back byte-identically as
`V20B3R.D81` (SHA-256 `adb018ea0ca438fc4ccb537d755bcefb71a4b75507b5873afeed6df7730256d1`).
The contact began from fresh BASIC and used no automated target access after
boot.

B3-0 passed: the banner, `lisp65>` prompt and cursor became visible.  B3-1
then stopped before its first matcher form could be entered: the visible REPL
accepted no physical-keyboard input.  This is a deterministic daily-use
blocker in the already repaired feature world.  No deadlock or lower-level
mechanism is claimed from the observation alone.

The bound device decision table therefore applies without another repair or
diagnosis round: Block 3 is descoped from v2.0.  B3-1 is red; B3-2 through
B3-4 were not run and carry no hardware claim.  The generation-coherence and
packed-closure gates remain permanent infrastructure, but their host-green
results do not promote matcher or blink behavior on hardware.

Evidence:
`tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v2.0-block3-banner-repair-device-result-receipt.json`.
