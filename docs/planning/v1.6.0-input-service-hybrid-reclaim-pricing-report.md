# v1.6 adaptive input hybrid — replacement reclaim pricing

Date: 2026-08-19
Authority: `120758b7`

The real emitted call graph contains a replacement for the returned `%rl-put`
slot: `%require-c2d-header-layout-p` has exactly one non-recursive call, from
`%require-c2d-state`.  It is private, owns no state, and its source definition
is in `lib/stdlib-require.lisp`.

The `%take`-standard proof passes the complete 248-case Workbench suite through
the real compiler with the candidate selected for private inlining.  The
candidate entry is absent, `%require-c2d-state` remains present, and its emitted
body is 31 bytes.  The proof is therefore about the compiled product graph, not
a source-text call count.

Bias-adjusted capacity:

| State | Free slots | Free name bytes |
| --- | ---: | ---: |
| Accepted Comfort world | 32 | 572 |
| `%rl-put` entry returns | 31 | 564 |
| Replacement reclaim | **32** | **593** |
| Release minimum | 32 | 384 |
| Final margin | **0** | **209** |

The slot floor is restored exactly; there is no slot margin.  The two owner
alternatives remain untouched because a reclaim exists.  This was host-only:
zero cards, WPLTOs, product links, media builds, or device contacts.  The
unconsumed hybrid implementation card remains closed until review accepts this
price.
