# C2.2 F4 — S1 freight session

Status: **passed on Link 67**. Attempt 1 stopped on an invalid harness delay
before any product row was accepted. Corrected attempt 2 passed all twelve
rows in one device session.

S1 is one device appointment after halt #2 and the one approved product link.
It tests F1 and F2 in row order, then keeps the ordinary REPL and idle-Freezer
regressions in the same session.  Every instrument is prepared before the
appointment; an unexpected result stops the session but does not create a
second ad-hoc hardware request.

The F1 claim is the published fixed-arity call `(%s1n 7 8)`.  Cold execution
must complete in at most 16 frames and the immediate warm repeat in at most
10.  The result pair must be `(7 . 8)`.  The historical nullary path is
measured immediately afterward against the same ceilings, so the new guard
cannot buy its result by regressing the cured path.

F2 executes five successful boundary/negative-value vectors in one form, then
one deliberate type rejection followed by arithmetic.  The second row proves
both fail-closed typing and a surviving REPL.

The final regression is one idle Freezer roundtrip with physical return key
F3, followed by `(+ 4 5)`.  The eventual receipt binds the current product,
ELF, device core, tool identity, raw transcripts, all frame triples and the
current `$E000`/Chip-plane identity.  No older product lends a green row.

F3 is not present in this session candidate.  Its one-carrier probe exceeded
the cold Buffer-read cap and was parked before a product link; omitting its
rows is an explicit disposition, not inherited or simulated acceptance.

The machine-readable row set is
`config/c2.2-s1-freight-session.json`.  The preparation gate validates that
every executable F1/F2 semantic behind these rows is already host-green and
that the product sources contain no parked F3 surface.

The bound successor is Link 67: resident PRG
`1b6fb1a524a71a63489848531b30e1d399b871ed5b863c93be7232f3362e44f3`,
ELF `25e563ba41283fb1ce21624a84f618b2337f889510dee19261509dc29e465f32`.
The deployment uses the canonical 14-role manifest and the established
C2-lite stage addresses; it does not rebuild, repack, or substitute the
candidate.

Attempt 1 deliberately waited 28 seconds after launch and another 5 seconds
after submitting the boot-watch form.  Its reported 1,797-frame counter
therefore measured the harness delay, not boot-to-REPL, and is receipt-bound
as a harness First Red with zero accepted product rows.  The corrected runner
polls once per second for the first visible REPL and submits the boot watch
immediately; it cannot silently spend the 1,500-frame allowance itself.

Attempt 2 passed 12/12 rows on core identity `6b4cb203`.  The boot watch
reported `939f/18780ms<=1500f`; the fixed-arity call measured
`1f/20ms<=16f` cold and `0f/0ms<=10f` warm.  The nullary regression measured
zero frames both cold and warm.  All five bit-operation results were exact,
the type-negative case returned to a usable REPL, and the F3 Freezer return
preserved Bank 2, Bank 3 and Bank 5 byte-for-byte.  Of the 8,192 `$E000`
bytes, 8,190 were identical; the only changes were the two contract-listed
live cells `$FF83/$FF84`.  The post-Freezer expression returned `9`.

The completion receipt records the full accounting as two hardware attempts,
one valid successful session, twelve accepted rows and one product link.  It
does not promote F3, `while`, C2.3 or any release claim.
