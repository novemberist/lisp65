# 2.3 Product Authority Rebind — card report

**Date:** 2026-08-18  
**Commission:** `525958d1`  
**Result:** **GREEN — reviewer-verified and released**  
**Device contacts:** 0

This report closes the host-only card commissioned by
`docs/planning/2.3-product-authority-rebind-work-plan.md`. The public product
target is usable and remains one-shot, the selected product and its authorities
describe the same world, the complete `check-source` tail is green with no
exceptions, and two consecutive full runs are green without an intervening
repair.

## 1. Public product lifecycle

Commit `de760fd1` changed the one-shot predicate from the existence of the
shared build parent to state actually owned by the public product build.
`ide-check` and `ide-codemod` may create their sibling preflight directories;
completed, partial, or unknown product-owned output still consumes the one
allowed build.

The lifecycle selftest proves:

- an absent root is available;
- a root containing only the two preflight-owned children is available;
- completed-manifest, partial-product, and unknown-child mutations are all
  rejected as consumed build state.

After the authorized build, a direct second `make workbench-product` returned
exit 2 with `FIRST RED: v1.5 public build is one-shot`. The guard was therefore
unwedged without weakening its contract.

## 2. Selected product authority

The public product was rebuilt through its normal single-emitter plus one-WPLTO
path. Commit `de760fd1` bound the new selected set; `01619971` projected that
selected world into its living consumers.

| Authority | Selected value |
| --- | --- |
| Artifact roles | 25 |
| Artifact-set SHA-256 | `cdb022874b4c9ee6dfb261182373259a4eeffcd7833509514555014fe46291b2` |
| Product build ID | `1cea8cf6` |
| Profile build ID | `9a4c23dc` |
| Resident PRG | 41,566 B — `70fd5752b885ee06b1c4fe17ccc0318c81137cbf7e897a1d4614a08533afd624` |
| Linked ELF | 630,792 B — `b0942ce0d75af2485197c47dd0d49d0c753c227ef124df090209053fb8d2ba09` |
| Product D81 | 819,200 B — `efc9b181a10afb030a90e6c23806d9bc4d84a53d6fbfec3346d701fe8ce28436` |

The byte difference from the stale selection was attributed before binding:

- PRG and ELF sizes are unchanged;
- all 191 section geometries agree;
- all 1,095 symbol roles agree, with differences confined to local LTO symbol
  ordering;
- the semantic profile is identical apart from build-local path labels;
- there is no public surface, feature, arena, or behavior change.

This is an authority rebind, not new product behavior.

## 3. Complete tail

The mandatory continuing run exposed the full tail instead of stopping at its
first failure:

```text
make -k --no-print-directory check-source
```

The first discovery run found exactly four living-world/historical-world
crossings. Commit `5a71d531` closed them:

1. the Link-95 historical card driver no longer pinned the living product;
2. the v1.5 freight closure validates the sealed historical receipt and the
   selected public projection as separate authorities;
3. name-freight implementation evidence uses its sealed pricing result rather
   than reconstructing a mutable Link-97 world;
4. the bad-bytecode host control uses the selected product, compiler/runtime,
   and selected library inputs.

The continuing validations then exposed two more historical receipts whose
source predicates had survived their worlds. They were sealed without rewriting
their evidence:

- `f09fe149`: reader/caller attribution reads its evidence commit;
- `95bb6315`: WYSIWYG capacity attribution reads its Link-115 evidence commit.

Historical receipts continue to prove their historical claims byte for byte;
they no longer gate an authorized successor by consulting living source. No
exception or skip list was introduced.

## 4. Consecutive full-run proof

The following runs were executed consecutively after `95bb6315`. There was no
edit, cleanup, repair, or other intervention between them.

| Run | Exit | Elapsed | Log lines | Log SHA-256 |
| --- | ---: | ---: | ---: | --- |
| 1 | 0 | 932.49 s | 2,161 | `ea838d89dd53c537b8e40c8585455f4420fa856683089f41c52f86576e55b6e8` |
| 2 | 0 | 934.87 s | 2,161 | `f6a82eeefd2faa20450ee5dfa39e21627cf011ec50a44d5b7898e76fa2a5f3e2` |

Mechanical closure checks:

- neither log contains a `make: ***` line;
- both terminate with `EXIT=0`;
- the 2,160-line command/result bodies are byte-identical; only the final
  elapsed-time line differs;
- the worktree remained clean after each run;
- the second run required no intervention and therefore proves idempotence.

## Bound implementation

- `de760fd1` — public lifecycle and selected authority rebuild
- `01619971` — selected product-world consumers
- `5a71d531` — four complete-tail authority crossings
- `f09fe149` — historical reader/caller attribution
- `95bb6315` — historical WYSIWYG capacity attribution

## Reviewer verification — 2026-08-18

The reviewer re-ran `make -k check-source` independently on the delivered
tree. It found **two** failures, both the same cause and both introduced by
the closing commit itself: this report is tracked but was not classified in
the document index, so `document-index-check` and the housekeeping receipt
that consumes it failed. The card's two green runs predate the commit that
carries them — the act of recording green un-greened the tree.

Repaired by classifying the report and rebinding the housekeeping receipt.
Nothing else failed: every substantive claim of sections 1 to 3 verified
independently, including the one-shot refusal (`FIRST RED: v1.5 public build
is one-shot`) and the lifecycle selftest (`preflight=2 mutations=3`). The
reviewer's run measured 15 min 26 s, consistent with the card's timings.

Lesson, and it is the card's own theme once more: **a closing commit is part
of the tree it certifies.** The proof run belongs after the report is
committed, not before.

### Confirming run, after the closing commit

`make -k check-source` was then run once more on the tree **including** the
commit that carries this report and its index entry: exit 0, zero `make: ***`
lines, 2,161 log lines, worktree clean afterwards. This is the stricter form
of the card's step-3 bar, because the certified tree now contains its own
certificate.

## Owner boundary

The 2.3 card is complete. Owner approval of this report opens the v1.6
commission with cursor navigation at the head of its frozen agenda. Nothing
from v1.6 was implemented here.
