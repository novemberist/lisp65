# v1.8 Block 1 `$22` host reproduction

Status: **target-faithful non-reproduction.** The calibrated product-symbol
path produces `$22` on command, but the sealed mixed world does not reach that
edge.

The positive control runs first. It presents a NUL-terminated 34-byte name to
the modeled `src/symbol.c:new_symbol()` path. The target limit is 33 bytes;
the result is mechanically `LISP65_ERR_TOO_MANY_SYMBOLS` (decimal 34, `$22`),
with `positive_control` recorded as caller and the complete passed name in the
receipt. This proves that a later green result is not caused by a host interner
which cannot express the target failure.

The diagnostic composes the sealed Block-3 r10 static plane (788 entries,
3,122 resolutions and 52,230 code bytes) with the real nine-entry `v16core`
manifest.  The append reaches 797 entries, 3,213 resolutions and 53,949 code
bytes.  It then drives the exact first-Left sequence by entering `(list 1 3)`,
pressing Left and finally Return through the externally appended `read-line`.

The result is the unchanged string `"(list 1 3)"` after 12,703 VM steps. The
trace contains 198 CALLPRIM invocations in their real order, across nine target
identities: `screen-size`, `peek`, `screen-put-char`, `key-event`, `rplaca`,
`rplacd`, `numberp`, `write-char`, and `%string-from-codes`. The last target is
also proved present in the generated product exclusion table; no trace target
falls outside the product dispatch.

The product-symbol half models all three target rejection conditions (33-byte
name cap, `MAX_SYM=752`, `NAMEPOOL=10208`) and reads the native comparison
order from the generated dispatch used by `vm_apply_primitive()`. It starts at
the sealed device counters 659/8,889. Target identities carried by the observed
bytecode literals are treated as necessarily present; every other comparison
name is pessimistically absent. The complete 198-invocation sequence, including
the `peek` boundary after `key-event` and the exclusion scan through
`%string-from-codes`, ends at 701 symbols and 9,224 name bytes: 51 slots and
984 name bytes remain. A complete replay of the same 198 invocations allocates
nothing.

Therefore no real `new_symbol()` caller/name can honestly be named: the
calibrated failure edge is live, but neither the mixed-world execution nor its
capacity-faithful native scan reaches it. The sealed hardware `$22` is a
genuine non-reproduction under the recovered world, not evidence for a
particular writer. No product repair follows from this result.

No product source, WPLTO, link, medium, device, or network was touched.

Machine evidence:
[`c2.3-v1.8-block1-symbol22-host-attribution.json`](../../tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.8-block1-symbol22-host-attribution.json).
