# C2.2 KERNAL-unmap and input-ownership contract

Status: **owner-commissioned contract; bounded probe not yet run**

Date recorded: 2026-07-19
Commissioning source: `docs/planning/v1.2-scope-memo.md`, section
“KERNAL-unmap block — commissioned (owner go, 2026-07-20)”
Machine-readable contract: `config/c2-kernal-unmap-contract.json`

## Claim boundary

This contract authorizes a bounded proof of the complete ownership transfer
described below. It does not authorize product bytes, a KERNAL-free product
claim, a capacity repin, or a fifth substitution link. Those require, in
order, a green probe receipt and separate review.

The block is indivisible. It transfers all of these responsibilities before
the KERNAL mapping is removed:

- typed keyboard events and their event-time modifiers;
- the RUN/STOP evaluation abort;
- IRQ and NMI ownership, including the frame source used by latency gates;
- Freezer return and map re-establishment;
- post-startup character output; and
- ownership of the CPU window `$e000..$ffff`.

Removing only the ROM mapping while retaining any one of those firmware
dependencies is a failed probe, not a partial success.

## Pinned hardware facts

The authority is the repository snapshot
`docs/reference/mega65-chipset-reference.pdf`, SHA-256
`107610ae3ea9f7e3f1e78915dcbe2cae1a6f404ca2e538762524a7e58cced220`.
The PDF's printed page numbers differ from its physical PDF page numbers:

| Fact | Printed pages | PDF pages |
| --- | ---: | ---: |
| MAP selects eight 8-KiB slabs and overrides legacy banking | 9–11 | 23–25 |
| VIC raster compare/IRQ registers `$D012/$D019/$D01A` | 50–51 | 64–65 |
| typed queue and modifier registers `$D60A/$D619` | 102–104 | 116–118 |

The following facts are bound, not inferred:

- `$e000..$ffff` is one 8-KiB CPU-map slab; MAP overrides the legacy KERNAL
  banking there.
- `$D60A` reports queue presence and the modifiers captured for the event at
  the head of the queue. `$D619` reports that event as PETSCII; writing it
  advances/clears the ready event.
- `$D012`, `$D019` and `$D01A` expose the VIC raster compare, interrupt flag
  and mask required for an owned raster IRQ.

The contract does not infer instruction encodings or a Freezer return
sequence from those tables. The bounded probe must derive and disassemble the
actual MAP/vector sequence produced by the pinned llvm-mos toolchain.

## Gross and net capacity law

The gross opportunity is exactly 8,192 bytes of **16-bit CPU address space**.
It is not a Bank-0, EXT, Attic, symbol, name-pool or directory credit.

The preceding first-red receipt fixes the resident deficit at 3,639 bytes.
The real-link model is:

```text
replacement_residents =
    typed_queue_driver
  + irq_handler
  + nmi_and_freezer_return
  + frame_source
  + map_switch_and_guards
  + post_startup_output_seam
  + alignment_and_vectors

future_margin = 8192 - 3639 - replacement_residents
```

Every category is mandatory in the probe and link receipts, including
zero-valued categories. `future_margin` must be strictly positive. The
approximately 4.5-KiB figure in the scope memo is therefore a gross upper
bound before replacement residents, not a permitted result claim. Only the
real link may name the final margin.

## Ownership-transfer state machine

The only legal forward sequence is:

1. `firmware-owned`: cold boot, CRT startup and C2 staging retain the KERNAL.
2. `replacement-armed`: native output, typed input, abort, frame IRQ and the
   owned vectors have each passed their readiness checks while firmware is
   still mapped.
3. `handoff-closed`: interrupts are masked for the bounded MAP/vector commit;
   both old and new sources are represented by the transition record.
4. `product-owned`: the owned mapping is active, the new sources are live and
   advancing, and firmware services are unreachable from the post-unmap
   closure.
5. `freezer-suspended` / `freezer-return`: hardware may enter the Freezer;
   product execution may resume only after the mapping and vector generation
   have been re-established and checked.

Before `product-owned`, any readiness failure leaves the KERNAL mapped. After
the handoff, an unknown map or vector generation must fail closed to the
documented disk-reboot path; product code must never resume speculatively.
A platform reset is not a product restart: it restores the firmware boot
world and may reach BASIC until the disk boot path is invoked.

### Continuity invariants

At every state transition, including the bounded interrupt-masked handoff:

- at least one authoritative RUN/STOP source exists;
- at least one authoritative frame source exists;
- the new abort source is proven before STKEY `$91` is retired;
- the new frame source is installed and shown advancing before the old
  firmware-maintained source is retired; and
- a vector is never published before its target bytes and mapping are valid.

The handoff may mask delivery for a bounded critical section, but it may not
erase the source or lose a pending abort. A RUN/STOP event observed during the
critical section is latched and delivered before the next evaluator step.
Frame continuity is measured as a monotonic product counter: no duplicate,
backward or missing transition may be hidden by switching clocks.

## Typed queue and L-full inheritance

The product event is one atomic tuple `(key code modifiers)`. Code and
modifier bits are sampled from the same queue head, and the event is dequeued
exactly once after both have been captured. Blocking and non-blocking reads
share that operation. Queue-empty is not a key with code zero.

RUN/STOP has exactly one product meaning: abort a running evaluation to a
usable REPL. It is not an editor binding and is a no-op while idle. Its
hardware recognition may use the typed event or matrix path, but the chosen
source must be single and generated, not a second handwritten key table.

L-full evolves `config/v11-l-lite-keymap.json#modifier_bindings` through the
existing generator; the Lisp table, host cases, P0 cases, hardware cases and
`docs/generated/ide-keymap.md` remain one closure. The first required modifier
bindings are physical `Control-Space` for set-mark and `Meta-x` for the command
launcher. The permanent end-to-end gate follows each physical queue tuple
through the pinned core producer, the product-owned atomic queue capture, the
product's key normalization (including raw `88` to normalized `120`), and the
generated action compiled by the Werkbank manifest. Contract text and isolated
probe output are not substitutes for that consumer proof. Legacy `C-x Space`
and `C-x x` may remain compatibility bindings only if that choice is
represented in the same source. No physical Meta/Alt claim enters user
documentation until both injected queue cases and a real-key hardware sample
pass.

## Interrupt, output and reset contract

- The bounded probe selects and pins one owned frame interrupt source. The
  primary candidate is VIC raster IRQ via `$D012/$D019/$D01A`; no CIA or
  firmware-jiffy assumption may survive implicitly.
- The owned IRQ acknowledges only the source it services, preserves the
  documented machine context and cannot call Lisp or allocate.
- The 45GS02 `STZ` instruction stores the live Z register; it is not a
  constant-zero instruction. IRQ/NMI entry therefore begins with arbitrary
  Z. The owned IRQ saves it with `PHZ`, establishes one handler-local
  `LDZ #0` authority before every `STZ`, and restores the interrupted value
  with `PLZ`. The permanent source-derived dominance gate covers every
  handwritten assembler `STZ` in `.s`/`.S` and C inline assembler, including
  hardware entries outside the C-called leaf universe, and rejects per-site
  `INZ`/`LDZ #1` mutations.
- The same source-derived gate enforces the reverse boundary: every regular
  handwritten entry returns or tail-calls with `Z=0` on every path, and every
  ASM-to-external call edge enters with `Z=0`. Interrupt entries are the
  explicit exception class: their `RTI`/firmware-chain paths must preserve the
  arbitrary interrupted Z value. An `INZ` immediately before every discovered
  return/tail edge is a permanent rejecting mutation.
- A platform or toolchain lesson present only in prose is not closed. Every
  new lesson gets a permanent gate, or a written explanation why no gate can
  exist.
- The owned NMI path contains no normal evaluator work. It exists to preserve
  the platform/Freezer contract and to make the return mapping check
  unavoidable.
- The lone audited `CHROUT` edge belongs to CRT startup before `main`. Once the
  post-startup closure is entered, all output is native; no KERNAL output edge
  is permitted.
- Cold boot and hardware reset retain the firmware mapping. KERNAL removal is
  a post-boot product transition, never a permanent platform reconfiguration.

## Structural KERNAL-freedom gate

The link gate distinguishes the address window from the firmware identity:
C2-owned code may live in `$e000..$ffff`, but it must be in a named owned
section. The post-unmap closure must prove all of the following:

1. the linker map assigns the entire live portion of `$e000..$ffff` to named
   C2-owned sections and vectors;
2. no relocation, call edge or absolute operand targets KERNAL service entry
   points (including `$FFD2` and `$FFE4`) or the retired STKEY byte `$0091`;
3. every post-unmap edge into `$e000..$ffff` resolves to an owned symbol in
   the owned section, never merely to an address in the range;
4. no KERNAL, CBM-I/O or legacy GETIN object/archive member remains in the
   post-unmap link closure; and
5. the generated source/disassembly census contains exactly one input path,
   one abort source and one frame owner.

An execution test cannot substitute for this gate.

### Fixed cross-domain facade

The owned `$e000..$ffff` image may not bind directly to whole-program symbols
whose Bank-0 addresses can move under LTO. Its only calls into moving Bank-0
implementations pass through thirteen three-byte tail-jump vectors beginning at
`$b5a2`; every vector address is contract-pinned. The implementation target
may move, while the window-visible address may not.

The original twelve addresses remain unchanged. The thirteenth vector at
`$b5c6` is `c2_facade_gc_mark`, appended for the C2-owned block root walker.
Its addition moves the separately pinned I/O-reveal, MAP-switch and handoff
state sections by three bytes; none of those addresses is implicit.

The 408 bytes of boot-visible Bank-0 state, the five-byte `kb_cursor_off`
helper at `$c218`, the 40-byte `c2e_cons` facade target at `$c21d`, and seven
bytes of zero-page state
referenced by the window likewise live in explicitly placed sections (`$c080`
and `$c218` for Bank 0, `$0089` for zero page). The fixed Bank-0 code ends at
`$c245`, leaving 273 bytes before the runtime overlay. The session emitter's
346-byte mutable state lives
in a named owned-window state section because it does not exist in the
pre-ownership lifetime. These regions are governed by the same orphan-error
policy as code.
The link fails if a vector or state address moves, if a fixed section is absent
or changes size, if the enumerated pre-ownership closure reaches the
session-only code or state, or if disassembly finds a direct owned-window call
to low memory outside the vector set. This replaces 132 implicit
LTO-sensitive bindings with thirteen public vectors and declared storage and
lifetime geometry.

Pre-ownership graph nodes are identified by `(section, VMA)`, never by VMA
alone. Runtime-overlay sections intentionally share `$c356`; the gate derives
the boot-section set from the same pack specifications as the emitter and
keeps same-address roots distinct. Its permanent mutation set covers a boot
edge into session code, an operand into session state, and an edge through the
session-cons facade vector, as well as same-VMA section identity itself.

## Link-18 publish-last verifier binding

The product closure is linked exactly once against the SHA/CRC identity of the
owned window extracted from Link 17. That complete 8-KiB identity is a pre-link
input and must be reproduced byte for byte; it is never rebound after LTO.
The resident-island seed materializer remains a separately counted prerequisite
and is not a second product-closure link.

The four runtime-family verifier tuples live in one assembler-owned, non-LTO
section of exactly 32 bytes. Its frozen order is boot catalog, boot record,
session catalog, session record. The link contains recognizable sentinel words.
After both runtime families have been packed from that one ELF, the packer
replaces only this section in the PRG as a publish-last operation. Section size,
symbol offsets, the changed-byte range and all four manifest-derived tuples are
gated. No code is relinked to publish verifier data.

Packing is repeated from the same ELF after publication. The identity gate
validates the full image SHA/CRC and every slice payload SHA/CRC, then compares
every record field and every payload byte across the two packs. A permanently
pinned mutation flips one payload byte and must be rejected. Stable sizes alone
are never sufficient evidence.

Link 18 confirmed this binding model: one product-closure link reproduced the
pinned owned window, the 32-byte publication changed 30 bytes and no byte
outside the section, all 53 family-record occurrences and payloads were
identical across the repack, and the deliberate payload mutation was rejected.
The product PRG remained 41,542 bytes, equal to Link 17.

The first red moved to the KERNAL-freedom gate. Its original ownership model
required every direct `JMP` target in the owned window to equal an `nm` symbol
start. LTO emits local basic-block targets without symbols: Link 18 contains
113 such `JMP` occurrences to 47 unique decoded instruction boundaries, all
within the same named function and executable owned section. Conversely, the
model's unfiltered symbol table included absolute and weak KERNAL aliases,
which are not owned executable entries. Thus the gate is neither sound nor
complete at this boundary; KERNAL freedom remains unproved, not disproved.

The proposed correction is a section-qualified control-flow model. A direct
`JSR` or inter-function tail `JMP` must land exactly on a `T/t` function symbol
inside a named executable owned section. An intra-function `JMP` may land only
on a decoded instruction boundary inside that same function and section.
Absolute/weak aliases and all state/data sections are never ownership evidence.
Permanent mutations must reject a mid-instruction target, a state/data target,
an inter-function `+offset` target and a `JSR` to an internal basic block, while
accepting a real same-function basic-block jump. Any authorized replay operates
on the SHA-bound Link-18 artifacts without invoking the linker again.

The authorized artifact-only replay passed with zero new product links. The
qualified model inspected 275 direct edges into the CPU window: 161 owned entry
edges, 113 same-function symbol-less `JMP` edges, and the one separately audited
pre-main `CHROUT`. It ignored 95 absolute/weak aliases as ownership evidence and
reported zero violations. Its positive case and five negative mutations all
behaved as required, including rejection of an alias-disguised `$ffd2` exit.

The resulting structural balance reports 4,775 live bytes in the 8-KiB window
and 3,417 bytes of actual future margin. Relocated C2 residents contribute
4,595 bytes against the historical 3,639-byte deficit; replacement machinery
costs 482 bytes. The boot/session runtime families occupy 12,273/58,270 bytes,
leaving 53,263/7,266 bytes in their mutually exclusive stores. Bank 5 retains
31,696 bytes. The publish-last table is exactly neutral (32 bytes retired,
32 bytes introduced). These are structural link claims only; hardware and
promotion remain not-run.

## Complete-profile immutable data and window closure

The canonical Dialect-v2 product profile adds three immutable service-dispatch
tables whose failed Link-27 placement consumed 342 Bank-0 bytes:
`.rodata.eval_v2_workbench_service` (32 bytes), `.rodata.vm_callprim`
(164 bytes), and `.rodata.vm_native_call` (146 bytes). Link 28 may place exactly
these three input sections in one named owned-window data section. Generic
`.rodata`, VM-core tables, strings, and later constants are not implied tenants.

This data move has a proof obligation distinct from control-flow ownership.
The product ELF retains relocations, including split low/high immediate address
formation. A dedicated gate resolves every relocation into the moved interval
and rejects the link if its source section belongs to the direct pre-handoff
reachable closure. Each of the three source components must have at least one
retained reference. Thus no code-path or facade-vector claim is reused as a
data-reachability claim.

The failed first placement map also establishes the expected Bank-0 result:
ordinary `.bss` would end at `$c06d`, leaving 19 bytes before the fixed C2
state at `$c080`. The next successful probe must measure this as its own
currency. Like the remaining E000 bytes, those 19 bytes are contingency for
the existing resident composition, not authorization for new resident growth.

Retaining relocations also exposes the LTO-internal `.llvm_sympart` section.
The second placement probe proved that pinned lld still classifies it as an
orphan even when the script gives it an exact INFO output command, `KEEP`, and
nonempty/address-zero assertions. It is therefore not a sixth ordinary output
allowlist member. The five prior output names remain unchanged and unknown
orphans remain hard errors. `--emit-relocs` also remains mandatory.

The owner-authorized `/DISCARD/` probe preserved the generated LTO object under
an explicit `--lto-obj-path`, found the required 15-byte non-ALLOC
`.llvm_sympart`, SHA-bound and write-protected that object, and named the
section explicitly in `/DISCARD/`. Pinned lld nevertheless issued the same
hard orphan diagnostic before emitting a final ELF. Script-level discard is
therefore disproved for this toolchain stage: lld classifies the saved LTO
partition section before the script disposition can satisfy the orphan gate.
Final relocation retention and the data-reference gate remain not reached and
not passed.

The owner-authorized second fallback changes that one diagnostic to a warning
and then admits exactly one line whose section *and originating LTO object*
match the current target. Zero matching lines, two matching lines, any other
orphan, or any wording/origin drift are hard errors. A complete ordered
140-name final-ELF inventory in
`config/c2-final-section-inventory-pin.txt` is the compensating gate:
`.llvm_sympart` must be the sole 15-byte address-zero, non-ALLOC INFO occupant
with zero runtime load bytes. Any missing or additional section is red. The
first wrapper probe accepted exactly the one authorized diagnostic and produced
an ELF with all 140 pinned names, no additions and no omissions. It nevertheless
stopped because the initial gate also compared section-table order: the final
ELF placed `.llvm_sympart` immediately after the five explicit INFO sections,
whereas the failed-map-derived pre-pin listed it last. Ordering has no stated
runtime or proof meaning here, so this is recorded as an overstrict gate-model
First Red, not a passed inventory. A membership-only replay against the same
SHA-bound ELF corrected the model without invoking the compiler or linker. It
passed exact 140-name membership, LTO metadata classification, and retention of
57 relocation sections. Section order is now recorded only as provenance.

The first replay then reached the data-reference gate and stopped. Five
retained references existed, but they named the merged output section plus
addends rather than the three input-section names. Resolving that output base
covered all three components, while their source records identified only
merged `.text`; the first gate therefore could not prove containing-function
provenance. That result remains recorded as First Red rather than being
rewritten.

The owner-authorized corrected replay ran against the same SHA-bound ELF with
zero compiler and zero linker invocations. It resolves the owned output-section
base plus addend into the moved component intervals, then binds every source
address to exactly one positive-sized, section-qualified function interval.
Unknown, overlapping, or unsized provenance is a hard error. All five retained
references are now proven post-ownership: one originates in
`eval_v2_workbench_service`, three in `vm_callprim`, and one in
`vm_native_call`; all three moved components are covered. The only explicit
pre-handoff interval is the 31-byte `main` prefix ending at the ownership call,
and it contains no moved-data reference. Nine positive and negative gate cases
pin output-section-plus-addend resolution and reject missing component,
pre-handoff, unknown, overlapping, and unsized provenance. The replay also
freshly passed exact 140-name section membership, 57 retained relocation
sections, LTO metadata, handoff, pre-ownership, fixed-facade, and KERNAL-freedom
gates. Its measured result is 19 bytes ordinary Bank-0 BSS contingency and 386
bytes E000 contingency. The receipt is
`c2.2-link28-v2-profile-data-placement-gate-replay-receipt.json`.

This is a proof replay, not Link 28 or a hardware claim. Removing
`--emit-relocs` remains forbidden because that would destroy the separately
required data-reference proof.

The one subsequently authorized Link 28 reached the pre-link whole-window
identity gate and stopped. Its seed and materialized product closure both
passed the exact profile, orphan-wrapper, 140-name inventory, and LTO metadata
gates. The final window did not reproduce the replay pin: expected
`70b5562c…` / `fc2b`, actual `2406ebb8…` / `acdc`. Read-only localization
found all 167 changed bytes inside the 342-byte profile-data interval and none
outside it. Of its 171 16-bit values, 163 changed between the current seed and
final product.

The failed assumption is now explicit: compiler-generated service-dispatch
tables are immutable during one execution, but their contents are direct
link-resolved Bank-0 function addresses. They are therefore not stable across
the prepared replay seed, a fresh resident-island seed, and the materialized
final product link under LTO. The data-reference proof remains valid for its
immutable replay ELF; the whole-window pre-link identity model does not remain
valid for this link-dependent content. Link 28 stopped without retry, gate
resume, hardware execution, or promotion. The First-Red receipt is
`c2.2-product-link28-window-identity-first-red-receipt.json`.

A followup requires a fresh architecture decision. The leading bounded option
is a designated non-LTO publish-last verifier binding for the final
product-linked window, with exact mutable-byte bounds and mutation tests, by
analogy with the runtime-family verifier record. Stable ID/vector projection or
returning the tables to Bank 0 are separately costed alternatives, not implicit
retries.

The owner authorized that publish-last correction as an artifact-only
continuation of the already emitted Link 28, not another compiler or linker
run. The final linked window (`2406ebb8…`, CRC `acdc`) is the sole identity
source. The complete post-link mutable product domain is exactly 34 bytes:
the existing 32-byte runtime-family verifier table at `$b914..$b933`, plus the
high and low CRC immediate operands at `$b4aa` and `$b4ae`. `$b86d` was the
table address in historical Link 19; the current Link 28 ELF places it at
`$b914`, and the contract follows the actual candidate rather than preserving
the stale address.

Both directions are gated: any change outside those 34 bytes is rejected, and
an intentionally wrong CRC operand must fail the published-window comparison.
The generated deployment header, Freezer comparison, and hardware presmoke
must consume the post-link SHA/CRC. The old replay seed remains First-Red
history and is not a downstream comparison value.

The artifact-only continuation passed without invoking the compiler or linker.
It changed the two CRC operands from `fc2b` to the final-window value `acdc`,
then published the four runtime-family tuples. The declared post-link domain is
34 bytes; 32 bytes actually changed because two table bytes already equalled
their published values. No byte outside the three named domains changed. The
outside-domain and wrong-CRC mutations were both rejected.

All downstream structural gates then passed on Link 28: five profile-data
relocations cover all three moved components with zero pre-handoff readers;
319 owned-window control-flow edges have zero violations; all 45 runtime-family
records and payloads are identical after repacking; legacy sources, symbols,
and L65M magic are absent. Capacity closes at 19 bytes ordinary Bank-0 BSS
contingency, 273 bytes fixed-block headroom, 386 bytes closed E000 contingency,
31,696 bytes Bank-5 headroom, and 53,007/15,202 bytes boot/session runtime-bank
headroom. The bound product SHA is `d6308939…`; the structural receipt is
`c2.2-product-link28-post-link-publish-last-structural-receipt.json`.

This remains structural closure, not promotion. The next gate is the
receipt-less fail-fast hardware presmoke, and it must compare the Freezer return
and deployment window against the post-link `2406ebb8…` identity.

With this cut the `$e000..$ffff` window is closed to new tenants. Whatever
positive margin the authorized placement probe measures is contingency reserve
for the already named occupants, not a budget for another migration. Future
growth must use Bank 5, the Attic/native-store growth path, or return as an
explicit architecture decision.

## Formal one-time reopening — owner decision 2026-07-21

The coordinated Link-33 Whole-Program seed exhausted the prescribed cold/hot,
Boot-installer and Bank-5-state placements but left Bank-0 text at -162 B,
ordinary BSS at -167 B and the Resident Island at -228 B; the window itself
measured +531 B. The owner therefore reopens the window exactly once under
these binding rules:

- total debit is capped at **450 B**, including alignment, facade and closure
  costs; a larger measured debit is First Red and the cap does not move;
- freight is purpose-bound to at least 167 B of ordinary text relief (which
  must also close positional BSS) and at least 228 B of Island relief;
- the hot handle normalizer and entry materializer remain in the Island;
- unrelated freight and opportunistic tenants are forbidden; and
- the remaining measured bytes become the final contractual window floor.
  There is no third opening. Any later resident demand triggers automatic
  MUST/SHOULD freight triage.

The one placement candidate groups whole functions rather than shaving bytes:
`c2_abort_driver` and `c2_facade_target_c2_dma` form the text package;
`vm_runtime_overlay_transaction_begin`,
`vm_runtime_overlay_transaction_end` and
`vm_runtime_overlay_exec_batch_island` form the Island package. The latter's
one return edge to the moving runtime transport requires one appended fixed
facade vector. The placement probe must bind all actual function, alignment,
facade and closure bytes from one product-shaped Whole-Program-LTO map. It is
not a product link.

### Whole-Program result — First Red

The sole authorized Whole-Program attempt consumed **419/450 B** of the debit
cap and proved both freight packages large enough (181 B text; 235 B Island),
but it could not satisfy the BSS condition. The measured walls were +41 B
Bank-0 text, **-379 B ordinary BSS**, +7 B Resident Island and +115 B `$e000`.
No PRG or ELF was emitted; Link 32 remains byteidentical.

The attempt corrects the pre-probe positional model. Ordinary BSS does not
follow the movable end of `.text` across this cut: the handoff, facade and
fixed low sections have absolute VMAs between `.text` and
`.rodata`/`.data`/`.bss`. Text relief is absorbed before those anchors and
cannot close the independent BSS overlap. In addition, the new facade output
made lld advance `.rodata` by 212 B and assigned an overlapping file range to
the `$e000` profile data. Correcting that load-domain plumbing would not cure
the already present BSS deficit.

The green-only final-floor clause therefore did not activate. The result is
bound by
`c2.2-link33-e000-reopening-placement-probe-receipt.json` (SHA-256
`d94b2997…`). There is no retry, product link or hardware presmoke under this
authorization; the residency decision returns to owner review.

### Independent BSS-triage authorization

The owner authorizes one further placement probe for the independently measured
379-byte ordinary-BSS overlap. It must first bind every colliding object to a
temperature and lifetime, then apply this exact ladder: proved lifetime unions;
DMA-capable cold state in Bank 5; CPU-hot state in the fixed Bank-0 block within
its 273-byte margin; and only the unavoidable session-hot tail in E000 within
the provisional 115 bytes. The 212-byte `.rodata` load-domain collision is part
of the same probe and must be closed by explicit VMA/LMA placement.

The E000 debit cap remains 450 bytes, the Island remains closed, the slice cap
does not move, and one Whole-Program-LTO map is the only capacity authority.
First Red stops. A green probe binds the final floor; a product link and hardware
presmoke still require separate authorization.

The first invocation stopped before LTO because the generated linker fragment
contained an empty `SECTIONS {` opener after the old standalone facade output
was removed. No map, LTO object, PRG or ELF exists, so no capacity claim or
final-floor binding was made and the authorized Whole-Program attempt is not
consumed. Continuation is review-gated.

The owner authorizes that exact continuation: one-line parser repair followed
by the still-unconsumed single WPLTO placement attempt. Historical Pre-LTO
evidence is immutable and the continuation writes a distinct receipt.

The repair passed and the one remaining WPLTO attempt ran exactly once. Its
complete map placed the 240-B heap at `$c245`, leaving 33 B before the runtime
overlay; ordinary BSS ended at `$bf9b`, leaving 229 B before `$c080`; text,
Island and `$e000` showed 41/7/115 B respectively. The explicit ordinary
`.rodata` anchor alone failed: required `$b694`, actual VMA=LMA `$b5f8`.
The inserted location-counter assignment does not place the inherited
llvm-mos output section; lld starts it immediately after the explicit
low-resident state. No PRG/ELF or post-link gate exists, so these remain map
observations and the 115-B final floor is not bound. No retry is authorized.

The recommended next contract cut binds `.rodata` to that predecessor
relation at `$b5f8` instead of reproducing the toolchain's complete inherited
output definition merely to force `$b694`. This retains 156 B of measured BSS
headroom. A successor WPLTO measurement still needs separate authorization.

The owner authorizes that successor measurement exactly once. The binding is
the symbolic predecessor relation, not a replacement numeric dot anchor. It
must run in distinct evidence, inherit no green result and stop at its first
red. Even on green, the measured 115-B window remainder is only the candidate
for the subsequent explicit final-floor contract update; no Link 33 or
hardware execution is authorized by this probe.

The successor WPLTO consumed its single attempt and passed that predecessor
relation at `$b5f8`. Its map kept all five walls positive (41/229/33/7/115 B
for text/BSS/fixed block/Island/window), but the fixed-facade gate then rejected
`c2_entry_records` at `$f3ec`: it calls Island-resident
`c2_product_handle_normalize` directly with `JSR $1800`. Thus the 115-B floor
is still unbound and later KERNAL-freedom gates remain not-run.

The narrow measured follow-up candidate is a fifteenth fixed facade vector.
It can occupy `$b5cc…$b5ce` if I/O reveal and MAP switch move three bytes;
their present trailing alignment pocket ends exactly at the unchanged `$b5e4`
state base. This projects zero change to state, ordinary `.rodata`/BSS,
Island and `$e000`, but changes the facade and two low-resident address
contracts and therefore requires separate review and a fresh WPLTO proof.

That follow-up is owner-authorized as one WPLTO placement probe. The fixed
facade has fifteen three-byte vectors; `c2_facade_handle_normalize` is pinned
at `$b5cc` and is the sole permitted `$e000` exit to the Island normalizer.
I/O reveal and MAP switch move to `$b5cf` and `$b5da`; the state base remains
`$b5e4` and ordinary `.rodata` remains predecessor-bound at `$b5f8`. This
change consumes the existing three-byte alignment pocket and therefore must
measure zero delta in every resident wall. It does not itself bind the final
window floor or authorize Link 33 or hardware.

The single WPLTO attempt confirmed the zero-delta map and passed the 15-vector
facade, pre-ownership, profile-data and KERNAL-freedom gates. It stopped later
because the preinstallation-Island relocation gate could not assign the
`$b5cd` relocation to a positive-sized source function: the owning assembler
vector at `$b5cc` is intentionally zero-sized `NOTYPE`. The floor remains
unbound. Any continuation must model that one vector as a contract-derived
three-byte provenance interval without treating arbitrary facade bytes or
other unsized symbols as functions; a replay against the immutable ELF needs
separate authorization.

That replay is owner-authorized. Its only additional provenance object is
`(.lisp65_c2_host_facade, $b5cc, 3 B,
c2_facade_handle_normalize)`. Wrong-address, wrong-size and unlisted-address
mutations are mandatory. A green replay is the prerequisite for binding the
115-B final window floor and starting Link 33; no relink is part of the replay
itself.

The authorized replay stopped before relocation binding because the existing
`symbol_sections` helper does not classify zero-sized `NOTYPE` symbols and
returned no section for the otherwise exact `$b5cc` vector. The immutable ELF
still places that symbol inside the 45-B facade output. This is a second,
narrow proof-model boundary: any successor must derive ownership from the
contractual output range or an ELF view that preserves section indices; it
must not relax the interval or admit unsized symbols generally. The final
115-B floor remains unbound.

The owner authorizes one successor replay with ELF `st_shndx` as the primary
section source and the fixed facade address range as a mandatory independent
crosscheck. A section index naming any other output is a format/gate error;
there is no class-wide `NOTYPE` exception. The four mutations are wrong
address, wrong size, wrong section index and an unlisted facade relocation.
No compiler or linker run belongs to this replay.

The replay passed: `st_shndx=14` resolves the vector to the 45-B facade output,
the complete `$b5cc…$b5ce` range is contained there, and all four negative
mutations are rejected. Receipt SHA-256 is `67422c4b…`. The measured 115-B
window remainder is therefore bound as the final floor. Both the linker
assertion and KERNAL-freedom gate require actual remaining window capacity to
be at least 115 B. There is no third opening; any future resident demand is a
freight-triage event rather than another placement negotiation.

### Terminal CRC-convergence floor break (2026-07-21)

After the corrected single-driver WPLTO measured 104 bytes of resident growth
against only 19 bytes of ordinary text and 33 bytes of fixed-block room, the
owner delegated and Review selected one final, exact floor break. The active
minimum is **63 bytes**, a 52-byte debit from the historical 115-byte floor.
It is purpose-bound to the one shared CRC-convergence retry driver and its
contractual call seam; Island 00/01, cold Boot helpers and slice-local Boot
barriers are part of the same probe package but are not new window tenants.

The 104-byte equation is explicit: 52 bytes of `$e000`, 19 bytes of ordinary
text and 33 bytes of the fixed pocket. All three are terminal allocations,
not future headroom. The driver is a normal ELF and control-flow citizen:
its size and address are pinned, its two hot callers are enumerated, and
pre-ownership, data-reference, facade and KERNAL-freedom gates are rerun.
The retry tail occupies exactly `$ff44..$ff77`; its cold start sample and the
serial Island-finalizer call scaffold use only the named `$c335..$c355`
pocket. A sixteenth three-byte facade vector,
`c2_facade_rtov_crc_mem` at `$b5f1`, is the sole permitted exit from the
window tail to the existing CRC leaf.

The 63-byte floor defends itself. Any successor requiring even one additional
resident or window byte beyond these allocations automatically selects
**C2-lite**. There is no third floor proposal, no exception and no owner
escalation. Link 36 and hardware remain separately blocked until the complete
WPLTO package is green.

The product-shaped WPLTO probe is green. The measured window tail is exactly
52 B at `$ff44`, leaving the terminal 63-B floor. The fixed retry scaffold is
31 B with 2 B remaining in its named pocket; ordinary text has 2 B, ordinary
BSS 129 B, the Resident Island 267 B, and the tightest runtime slice 21 B of
headroom. Island phases 00/01 measure 1,771/282 B. The facade has 16 vectors,
including the pinned CRC vector at `$b5f1`. CRC/codegen, CRC-leaf parity,
handoff, pre-ownership, profile-data-reference, fixed-facade and
KERNAL-freedom gates all pass; the latter covers 349 owned control-flow edges.
This is one non-promotable WPLTO seed only: Link 36 and hardware remain not
run and require their separate Class-C authorization.

The separately authorized Link 36 then stopped at its first real link gate,
before the Resident-Island seed could be published. Its `.text` is 38,024 B,
10 B larger than the green terminal probe and ends at `$b4aa`; the unchanged
handoff begins at `$b4a3`, producing an exact 8-B overlap. The accompanying
ordinary-rodata predecessor failure is downstream of that overrun. No seed,
product closure or hardware run exists, and Link 35 remains byteidentical.

This is precisely the trigger named by the terminal-floor self-destruction
clause. **C2-lite is selected automatically.** Link 36 is not retried; the
63-B floor, handoff anchor and fixed pocket are not reopened or renegotiated,
and no exception or owner escalation exists for this disposition.

The first fresh Link-33 attempt after binding stopped before a product link.
Its Resident-Island seed used a reconstructed, stale profile: 20 append slices
instead of 21, no `LISP65_C2_RESIDENCY_TRIAGE`, and 346 B rather than 10 B of
session-emitter CPU state. The consequent `.text`/state overlaps also tripped
the 115-B floor assertion, but do not invalidate the floor contract. A
successor must consume the exact WPLTO profile configuration as one source;
no profile field may be independently restated by the product-link driver.

The owner-authorized successor makes that source machine-readable as
`config/c2-link33-product-profile.json`. It contains the exact nine feature
defines, 21 append phases, 10-B session-emitter state and 15-vector facade
geometry used by the green WPLTO map. Both WPLTO and product drivers import one
strict configurator; rebuilding any of those fields in a driver is a gate
failure. Before another seed link, a pure replay must bind the immutable WPLTO
resolved profile to the object. Its receipt and the eventual product-link
receipt must expose the identical object SHA-256. This closes stale profile
reconstruction as a class; it does not inherit any capacity or structural
green into the new Link 33.

The pure replay passed without compiler, linker, product or hardware work.
Canonical object SHA-256 is `4ceb35d4…`; binding-receipt SHA-256 is
`2ac45ba1…`. The immutable WPLTO profile is exactly equivalent to that object,
and both drivers pass the no-reconstruction source gate. A fresh, separately
gated Link 33 is therefore authorized.

That one-shot profile-bound attempt stopped after one Resident-Island seed
link and before any product-closure or hardware run. The canonical profile SHA
is present in the resolved profile and passed before compilation. The next
gate, however, still pins the 140-name Link-28 final-section shape. The correct
profile-bound seed has 167 names: four obsolete `capacity`/`rollback` section
and relocation names disappear, while 31 v5-append, reopening-gap, relocation
and fixed-hot-state names appear. The protected First-Red receipt is SHA-256
`7e128fcb…`. Its map observes positive headroom at every resident wall (51 B
text, 229 B ordinary BSS, 33 B fixed block and 7 B Island). The earlier 531-B
window display is the pre-package value: `531 - 416 in-window bytes = 115 B`;
the two facade vectors cost 6 B outside the window, so the formal debit is
`416 + 6 = 422 B`. No capacity acceptance is claimed because the inventory
gate stopped first.
No pin change or retry is implicit.

The owner authorizes replacing that frozen profile-shaped suffix with a
derivation from the canonical profile configuration. The Link-28 list remains
the stable envelope; its old append sections and relocations are replaced by
the configured 21-slice ABI, and the E000-reopening/BSS-triage flags add their
exact named sections. The target ELF is never an expectation source. Exact
count and name-set equality remain hard, with missing, additional and
same-count old-profile-resurrection mutations required. The derivation must
show all four removed and 31 added names. It first runs as a pure replay on the
immutable seed; only green authorizes one fresh Link 33.

The pure replay passed at 167/167 names. All four removals and 31 additions are
listed in the receipt; missing, additional and same-count resurrection
mutations are rejected. The complete protected seed-tree hash is identical
before and after, with zero compiler/linker/product/hardware runs. Receipt
SHA-256 is `d8e6d6f3…`. One fresh profile-and-inventory-bound Link 33 is now
authorized with no inherited capacity or structural green.

That fresh attempt passed the 167-name inventory on its Resident-Island seed,
bound the LTO metadata and materialized the 1,781-B final Island image. The
final product link then stopped at the unchanged Slot-37 cap: its installer
carrier is 1,883 B against 1,792 B, an exact 91-B overflow. The failed map
attributes 102 B to installer code and 1,781 B to the embedded immutable
Island image. The seed had been 1,633 B because its placeholder image was only
1,536 B; the final materialization is the first truthful carrier measurement.
The Island itself is 2,041/2,048 B including its annex, leaving 7 B. Receipt
SHA-256 is `35e1ac30…`; no product identity or hardware run exists.

This is not authorization to lift the 1,792-B execution-window cap. The
recommended contract-first direction is to separate the immutable Island
image into an identity-bound boot data carrier and leave the executable
installer in its own slice; both pieces individually fit. The installer would
DMA the carrier directly from boot storage to `$1800`, verify the destination
CRC and publish READY last. The alternative is to evict at least 91 B of hot
Island payload, which conflicts with the measured 7-B Island margin and needs
its own residency triage. No probe or retry is implicit.

The owner-authorized two-record contract probe is green. Because L65R-v1
requires an in-payload entry point and ABI 1 for every record, the honest
carrier contract is strict L65R-v2: unchanged 32-B header and record widths,
one `DATA_ONLY` flag, `entry_offset=$ffff`, ABI 0, destination `$1800`, and no
dual decoder or feature sniffing. A stale v1 Attic generation is invalidated
before a v2 restage.

The probe used the real 1,781-B Island image from the bound seed ELF. Its two
records measure 102 B executable / 1,690 B headroom and 1,781 B data / 11 B
headroom. The ten-record boot model occupies 14,325 B and leaves 51,211 B;
the split costs 154 B of alignment/storage. Sixteen malformed or unsafe cases
were rejected, and publish-last was proved by authenticating generation,
record, source payload and destination before READY. Receipt SHA-256 is
`5a52cb00...`; no compiler, linker, product or hardware run occurred.

The result authorizes no product link. The target packer/decoder, resident
cost, generation transition and real placement remain unmeasured and require
separate review as an L65R-v2 product-shaped implementation probe before a new
Link 33 can be considered.

## Freezer fidelity class

Freezer entry/return is hardware-only. Xemu may exercise the queue and mapping
mechanics as a non-authoritative dry run, but it cannot emit a Freezer safety
PASS. Before any full acceptance chain, a receipt-less fail-fast hardware
smoke must cover: typed modifier event, RUN/STOP during evaluation, advancing
frame counter, Freezer entry/return, preserved map generation, and one further
key event after return.

## Bounded-probe exit gate

The next authorized step is one isolated, non-product proof target. It must:

- materialize the full state machine and owned vector table;
- build the typed queue path and generated L-full closure;
- use the pinned toolchain to emit and disassemble the MAP/vector sequence;
- link the replacement residents into the 8-KiB window and report every byte
  category in the net equation;
- prove both continuity invariants at every cutpoint;
- run the contract's negative matrix and mutation tests; and
- emit no product artifact or fifth substitution-link claim.

If that receipt is green, review may authorize exactly one real substitution
link. Any red capacity or structural gate returns here under first-red
discipline; no floor, slice cap or claim may be softened.

## One-time low-resident handoff reanchor (owner-authorized 2026-07-21)

The complete L65R-v2 WPLTO text ends at `$b483`, two bytes beyond the
historical `$b481` handoff anchor. The owner authorizes one contractual
reanchor by 34 bytes, establishing a standing 32-byte LTO-noise reserve. The
active successor geometry is handoff `$b4a3`, fifteen-vector facade `$b5c4`,
I/O reveal `$b5f1`, MAP switch `$b5fc`, and low-resident state `$b606`.
Ordinary rodata remains predecessor-bound and is projected at `$b61a`;
verifier bindings, data and BSS follow at `$b954`, `$b974`, and `$b98a`.

All fifteen facade vectors move together, from `$b5c4` through `$b5ee` in
three-byte steps. The fixed blocks at `$c080` and `$c218`, the runtime-overlay
VMA at `$c356`, and the 33-byte `$c335..$c355` pocket do not move. The lawful
pre-`$c080` pocket falls from 229 to 195 bytes; the latter is now the minimum
for this successor. The failed seed's two-byte state/rodata displacement is a
rejected predecessor drift and is never counted as capacity.

The complete post-link mutation domain remains 34 named bytes. Its successor
projection is the 32-byte verifier table at `$b954` and Handoff CRC operands
at `$b4cc/$b4d0`. These addresses become accepted pins only when a real linked
ELF proves them. `$b86d` and `$b914` remain historical Link-19/Link-28 facts;
their receipts are not rewritten.

Exactly one WPLTO capacity/placement probe is authorized after migration of
the current contract and gate sources. It must leave at least 32 bytes of text
room and 195 bytes before `$c080`, preserve the 1,792-byte slice cap and the
115-byte E000 floor, and pass handoff, facade, pre-ownership, profile-data,
one-truth, KERNAL-freedom, family-identity and publication gates. Link 33 and
hardware remain separately blocked.

## C2-lite append-final Hybrid geometry (owner-authorized 2026-07-22)

The historical paragraph above remains the first-reanchor contract. Its
115-byte floor is not current authority for the append-final successor. The
active floor is now **54 bytes**: `reopen_gap0` stays at `$fca2` for 128 bytes
and ends at `$fd22`; the ten-byte Session-emitter state is explicitly placed
at `$fd22`; the 342-byte profile RODATA follows at `$fd2c`. This resolves the
observed 26-byte gap/state overlap without changing live-byte total or adding
a tenant.

The 54-byte floor has a self-defense clause: any future E000 demand below it
automatically triggers scope triage. There is no exception, negotiation, or
fourth floor event. Bank-0 text independently retains a mandatory 32-byte
LTO-noise reserve. The current eight-byte measurement is therefore not an
acceptable final map; Alex first selects one scope cut covering at least 24
attributed text bytes. Only then may one WPLTO test all walls together.

Alex selected `numeric-early-errors` (81 attributed text bytes). The cut
removes only the resident E2e/E2f/E3d/E3e sentences; the stable codes,
overlay-first renderer, numeric fallback, status identity and fail-closed
paths remain. Cursor and media-change classification stay in the product.
This selection authorizes exactly one combined WPLTO and no product link.

## Append-plan facade vector 16 (owner-authorized 2026-07-22)

The ABI-correct append-final WPLTO exposed two owned-window exits to the fixed
resident-Island plan walker. They now cross one new fixed facade vector,
`c2_facade_append_plan_walk`, at `$b5f1`. The facade remains one contiguous
contract surface at `$b5c4`, grows from fifteen/45 bytes to sixteen/48 bytes,
and has exactly one relocation to `c2_append_plan_walk`. Both C consumers,
including the terminal rollback tail jump, target the vector rather than the
Island symbol directly.

The vector consumes `$b5f1..$b5f3`; the complete predecessor-bound chain after
it advances once by three bytes: I/O reveal `$b5f4`, MAP switch `$b5ff`, state
`$b609`, ordinary RODATA projected at `$b61d`, the 40-byte publish-last table
at `$b949`, data at `$b971`, and BSS at `$b973..$bfa5`. The handoff stays at
`$b4a3`; `$c080`, `$c218`, and the overlay VMA `$c356` do not move. The two
Handoff CRC operands remain `$b4cc/$b4d0`, so the named publish-last domain is
still 42 bytes.

This is one full successor pin set. Historical fifteen-vector and `$b9cd`
receipts are not rewritten. A fresh WPLTO must prove the projected addresses,
all walls and gates, and the Session aggregate before the already authorized
successor product link may run.

## Link-58 fixed-block code tenant (owner-authorized 2026-07-23)

The historical 45-byte fixed-code/33-byte pocket geometry remains historical.
The first two Link-58 candidates exposed the missing terms in that shorthand:
six inherited `.noinit` bytes follow the hot heap, and the workbench linker
starts its overlay floor at `ALIGN(__noinit_end + 1, 2)`. The 30-byte
`lisp65_mod_adjust_tagged` tenant therefore missed the floor by four bytes;
the 28-byte `rtov_read` tenant still missed it by two. Both were rejected by
the real linker before a product closure completed.

For the successor, the fixed code gains the intact 21-byte `rtov_fail` leaf
at `$c245`. Its sole control target resolves through structured ELF identity
to the named resident `rtov_wipe` function. The fixed code ends at `$c25a`;
the unchanged 240-byte heap occupies `$c25a..$c349`; `.noinit` is pinned at
`$c34a..$c34f`; the aligned overlay floor is `$c352`, and the overlay remains
`$c356`. The real fixed reserve is four bytes. The move returns 21 bytes to
ordinary text, projecting 12 bytes to 33 and restoring the 32-byte noise
reserve. The final-ELF gate pins the function, resolved fixed target, heap,
`.noinit`, and inherited alignment geometry.

## Current C2.2 E000-S1 authority (owner-selected 2026-07-24)

The older reopening paragraphs retain their historical receipts and symbol
inventories. They are not the current authority for batch residency.
`rtov_run_batch` and `vm_runtime_overlay_exec_batch_island` are retired from
the Resident Island. Every L65M-, commit- or LCC-policy repetition now enters
the complete authenticated single-record loader again. That deliberately
trades cold-path speed for 60 measured E000 bytes without changing the
visible result, failure policy, repeat limit, target CRC or wipe semantics.

The current Island inventory and pre-install closure gate therefore require
both retired symbols to be absent. The sole installer-closure control edge
into the uninstalled Island remains the READY-guarded transaction-context
edge. Historical maps that contain either batch symbol remain valid evidence
for their own product identities and are not rewritten.

## Link-60 fixed-heap and E000 re-pin (owner-authorized 2026-07-24)

The Link-58 fixed-block paragraph remains historical. In the current
whole-program allocation, `rtov_fault` no longer occupies zero page. Its three
accesses in the fixed `rtov_fail` leaf are therefore one byte longer each.
The leaf remains at `$c245`, grows from 21 to 24 bytes, and the complete fixed
code now ends at `$c25d`. No instruction or semantic behavior is changed by
the placement decision.

The intact 240-byte heap is re-pinned to `$c25d..$c34c`; inherited `.noinit`
follows at `$c34d..$c352`. Applying the inherited
`ALIGN(__noinit_end + 1, 2)` rule yields the new fixed floor `$c354`, leaving
two measured bytes before the immutable overlay VMA `$c356`. The final-ELF
gate distinguishes executable and data relocations structurally: exactly one
control edge must resolve to `rtov_wipe`, and exactly three absolute data
edges must resolve to `rtov_fault`.

The E000 pins are updated from the proved lifetime union, not reconstructed
from obsolete adjacency. The `session_emitter_state` output is a zero-byte
anchor at `$fd22`; profile RODATA remains explicitly pinned at `$fd2c` for
342 bytes. The ten-byte address interval between those two pins is not a
resident tenant and does not move the profile. The active E000 floor remains
54 bytes with its existing self-defense clause.

Exactly one fresh WPLTO must bind this geometry and show text at least 32
bytes, E000 at least 54 bytes, nonnegative fixed-block headroom, both Session
regions within contract, and all structural gates green. Historical Link-58
receipts and pins are not rewritten. Only a completely green map permits the
separately authorized Link 60.

### Link-60 publish-last Neu-Pin `$B94E` → `$B972` (owner-authorized 2026-07-24)

The sole Link-60 WPLTO measured the 40-byte runtime-overlay verifier and
family-stage table at `$B972` in two independent observations. The movement
by 36 bytes follows deterministically from current linked text; it is not LTO
noise and changes no semantic byte. Current Link-60 authority, the
publish-last gate and the identity gate therefore consume `$B972`.

The historical `$B94E` Link-49/50 receipts remain immutable evidence for
their own product identities. The Handoff CRC operands remain at
`$B4CC/$B4D0`, so the complete post-link mutation domain is still exactly 42
named bytes: the 40-byte table plus the two one-byte CRC operands.

The frozen WPLTO artifact may now be completed artifact-side without a new
compiler or linker run. The completion must prove that no byte outside that
42-byte domain changed, rerun all remaining structural gates and bind the
resulting identity before exactly one fresh Link 60.

### Link-76 boot-only CRC relocation and publish-last repin (owner-authorized 2026-07-28)

The strict interrupt-ownership cut grew the formerly exact-fit handoff by
42 bytes. The unchanged 108-byte `c2k_crc16` body is boot-only and has one
caller, so it moves from the pinned handoff to ordinary resident section
`.text.c2_kernal_boot_only`. The frozen WPLTO ELF places it at `$A1DD`;
the handoff remains based at `$B4A3`, now occupies 223 bytes and leaves
66 bytes before the unchanged 48-byte facade at `$B5C4`.

The compiler consequently places the two publish-last KERNAL-window CRC
immediates at `$B4F4/$B4FA`. Historical `$B4CC/$B4D0` receipts remain
unchanged evidence for their product identities; Link 76 and successors use
the new pins. The verifier table remains at `$B972`, so the mutable
post-link domain is still exactly 42 named bytes.

The location gate requires one whole-ELF direct call from
`c2_kernal_take_ownership`, no second or post-ownership caller, placement
before the fixed handoff and outside E000, plus the normal pre-ownership,
profile-data and KERNAL-freedom proofs. The already linked WPLTO artifact is
completed artifact-side; no additional compiler or linker run is permitted.
