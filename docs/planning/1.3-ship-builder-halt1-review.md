# 1.3 Ship Builder — Halt #1 review

Status: **Class-C Halt #1** after the first quiet device session on

**Reviewer disposition 2026-07-31: accepted.** One corrected closing
session is authorized for D1+D3+D4 only, with exactly one change — the
redundant reset after the already-resetting FTP mount is removed; nothing
else in the harness moves. defstruct is neither retried nor investigated
in this block; its conviction and new reproducibility are booked in the
parked register, and its restart returns as a Class-C commissioning
question after this block ships. The D3 editor quiet-typing row rides the
closing session as planned.
2026-07-31. No release or promotion claim is open yet.

## What is fully green

- The Ship-v1 contract, closure and media verifier.
- Three real `(ship ...)` samples with three host Runtime executions and 27
  verified media members.
- Two independent fresh checkouts reproduce `hello.d81` byte-for-byte
  (`6b2435b4…478a7`).
- `check-source` is fully green, including the permanent Ship gates.
- All three D81 uploads to the device read back byte-identically.

## What the device session actually decided

### D1 — no physical Ship execution claim yet

The FTP helper reset the machine after mounting each D81. The runner then
issued a second reset roughly two seconds later. All three rows landed in
fresh BASIC and the four-byte Runtime witness at `$0086` remained
`00 02 00 00`, not `RUNTIME_COMPLETE`. This is one repeated harness
choreography error, not three Ship failures. The media transfer itself is
proved; stager and Runtime execution are untested.

The minimal correction is known: remove only the redundant reset after the
FTP helper's mount-and-reset exit, then wait quietly before the one result
readback. No product or image byte changes.

### D2 — quiet defstruct is a real product red

`(require 'defstruct)` returned `t` after a 45-second quiet window. The next
form, `(defstruct point x y)`, received a full 180 seconds with **zero monitor
traffic**. It produced neither result nor prompt; the first observation after
the window was the red fail-closed frame.

This rejects the monitor-polling-only explanation for the old Link-75 event.
`defstruct` remains parked as a genuine product issue. The Ship block neither
repeats nor investigates it; this quiet row has fulfilled its one-question
contract.

### D3/D4 — not run

The red frame made the session terminal. Editor quiet typing and the trailing
`require`/`fx`/`time` rows therefore make no claim.

## Decision

Recommended: authorize **one dependency-sharp completion session** containing
only:

1. D1 with the redundant reset removed (three Ship images);
2. D3 quiet editor typing;
3. D4 trailing rows.

D2 is carried forward as the concluded red observation and is not touched.
If D1 is green, the Ship Builder has its hardware proof; D3 may independently
resolve the editor park. If the corrected D1 still returns to BASIC or fails,
the Ship Builder returns as a product/media Class-C question rather than
receiving another harness adjustment.

Alternatives are to hold the Ship block for a different manual boot method,
or to park Ship-v1 entirely. Neither is recommended because the only D1
failure mechanism is already attributed to a redundant reset in the runner.
