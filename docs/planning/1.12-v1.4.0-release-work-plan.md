# 1.12 work plan — the v1.4.0 release block

Status: **Phases A-C host-closed; the owner-authorized trace/untrace descope
has been reclosed over the unchanged Link-92 r5 product core, and both
SHA-bound media variants read back green; Phase D resumes at the D3 remainder**
(owner commission `b9980cb0`, Phase-A authorization `5bd3c2ec`, trace descope
`f426f7c7`). D1 is green on the unchanged product and string freight;
`who-calls`, the physical editor row and D2 remain. No selector, Halt, Phase E
or release claim exists before those device rows.

## The one sentence

Deliver the four-value bundle as v1.4.0 — the inspect/string tools, the
1.10 defstruct freight, the 1.11 compiler locality, and the corrected
editor story — under the A1 rule, two owner halts, and one bundled
device session run entirely under the 1.6-era instrument rules.

## Freight (final, scope-frozen at commissioning)

1. **Inspect/string tools**: `who-calls`, `capitalize`, `string-split` — public
   surface grows accordingly; surface-parity binds the three names and the
   guide sections land with them. `trace`/`untrace` are explicitly descoped by
   `f426f7c7`: the delivered ABI cannot read and therefore cannot faithfully
   restore an arbitrary old function-cell value.
2. **1.10 + 1.11 performance freight**: promotion of the locality
   candidate from `lcc-locality-candidate.lisp` into the product
   carrier as a release-block act; defstruct sequence 788 → 677 s
   priced, 179 s post-require.
3. **defstruct enters the delivered surface** if and only if its
   device row is green (see D2 below) — the notes then replace the
   v1.3.0 "not delivered" entry with the honest performance
   characteristics; a red row keeps it undelivered with the sharper
   story.
4. **Editor known-issues correction**: the stall entry softens per
   the physical 64/64 disconfirmation; RUN/STOP guidance stays.

## Device session (one, bundled, under the 1.6-era rules)

Cold reset, complete reset-domain staging with readback, C2J CLEAR
proof, quiet windows behind priced bounds only, one stop per row,
CPU-view/physical-translation read protocol throughout.

- D1: boot + banner `WORKBENCH 1.4.0` + smoke (q, time, strings).
- D2: **the defstruct row** — owner types require + defstruct, waits
  the priced ~3-minute post-require window with zero observation,
  then looks once: `(defstruct point x y)` followed by
  `(make-point 3 4)`. Green means the oldest known issue converts to
  a delivered feature with honest timing documentation.
- D3: `who-calls` smoke; editor quiet-typing confirmation row.

→ Halt #1: everything on one table. Then Phase E (notes, banner,
assets) and **Halt #2: publish go**. Banner rule: regular release,
`WORKBENCH 1.4.0`, no divergence mechanism.

## Bounds

A1 rule (check-source green or named exceptions — the known unrelated
calendar-drift rows are candidates for the named list, nothing else);
one-truth rule for every new public name; sealed v1.3.0 untouched;
claims stay in rows; the 1.6 instrument lessons are binding session
law.

## Phase A — release closure and conditional-surface contract

Build one machine-readable release closure before changing the product. It
binds the commissioned freight to its existing authorities:

- the three unconditional inspect/string names to their host receipt, shelf
  metadata, exact call edges, library sources and deferred surface rows;
- the 1.10 definition-freight candidate and the 1.11 locality carrier to the
  receipts and source identities that priced 677 s total / 179 s
  post-require;
- the current editor implementation and the physical 64/64 disconfirmation
  to the exact known-issues wording that may change;
- every guide, language-reference, surface, product-medium, sample, release
  note and known-issues consumer that must agree with the selected freight.

The closure has two surface rows. **Unconditional** contains the three
inspect/string names. **D2-conditional** contains `defstruct` and is false
until the device row is green. A removed consumer, an unbound source/receipt, a name
missing from either delivery or documentation, or `defstruct` appearing in an
authoritative surface while the selector is false is a loud failure. The
closure also freezes the out-of-scope set: no parity primitive, tick hook,
ownership retry, `$91` repair, new string conversion, or other feature may
ride this release.

A1 is run here. The preferred named-exception list is empty. A calendar row
may enter it only as an exact, dated exception whose historical payload is
byteidentical and whose current semantic recomputation agrees after date
normalization; there is no category-wide calendar exemption. Any semantic,
artifact, capacity or product red closes Phase A.

### Phase-A result — release closure and A1

The machine-readable closure binds six names, six consumer surfaces, both
performance authorities, the comfort artifact/graph, and the physical 64/64
editor authority. Its permanent gate rejects eleven scope, selector,
one-truth, timing-overclaim and premature-surface mutations. Initial selector
state is `pending`; all six commissioned names remain absent from the public
surface before integration.

The full `check-source` lane completed green with an empty exception list.
Six historical generators rewrote only their `recorded_on` fields and the
resulting exact dependency SHAs; the A1 receipt binds every changed JSON
pointer and proves each SHA against both versions before restoring the tracked
historical receipts. These rows are dated write side effects, not semantic
gate exceptions. The one real pre-A1 drift — the 1.11 receipt binding an
ever-growing plan and the whole `gates.mk` — is closed by a loud dated rebind:
the accepted plan commit and the exact 1.11 wiring projection replace those
mutable whole-file authorities while every workload, freight, price and
mutation value remains unchanged.

Phase A changes no product bytes, media, public names or device state.

## Phase B — host integration and permanent gates

Promote the 1.11 carrier into the live product composition as an explicit
release act, then integrate the 1.10 definition freight and the three
inspect/string surface names. The sealed v1.3.0 sources and artifacts remain
untouched. The integration reruns, rather than cites, the permanent 1.10,
1.11 and library
gates and adds one release-freight gate that proves:

1. the 11 defstruct forms, nine publish-last appends, rollback result,
   constructor result and final C2J CLEAR remain semantically identical;
2. the locality price does not regress above 677 s total or 179 s
   post-require on the bound ruler, external Bank-2 freight and every resident
   delta are explicitly priced, and resident growth is zero;
3. `who-calls`, `capitalize` and `string-split` have one source, one delivered
   implementation and one reference name each;
4. `trace` and `untrace` are absent from every delivered surface while the two
   string functions retain their contracted edge cases;
5. the conditional selector cannot be bypassed by a source, manifest,
   documentation or media-only edit.

Every claimed execution has a witness and every contract family has a loud
mutation. This phase is host-only. A red correctness gate rejects the freight;
it is never converted into a release-note limitation.

### Phase-B result — promoted freight and one-truth surface

The locality candidate is promoted as an additional source of the complete
v1.4 compiler composition; it does not replace the full live profile.  The
release compiler blob, directory and external image are byteidentical to the
accepted v1.11 candidate, with all ten private-inline functions and all six
carrier cases executed.  The 1.10 reconstruction still yields eleven forms,
nine publish-last appends, `(point 3 4)` and C2J CLEAR.  Its bound ruler remains
677 s total / 179 s post-require; the latter remains a structural price, never
a completion upper bound.

The comfort artifact contains ten objects and exposes exactly the five
commissioned names through the established surface-extension mechanism.  The
canonical Dialect-V2 surface remains the one base truth; the release closure
is the one extension truth, and the delivery-parity gate binds every extension
to the COMFORT library manifest and language reference.  Its bound-name count
is 110.  Executable lifecycle witnesses cover normal, error and repeated-wrap
`trace`/`untrace` restoration.

The combined Bank-2 accounting leaves 12,767 bytes in the base candidate and
9,462 bytes in the defstruct acceptance sibling, both above the 8,192-byte
preserved floor; resident growth is zero.  `defstruct` remains absent from the
public surface while the D2 selector is pending.  The permanent release-freight
gate rejects eighteen semantic, pricing, capacity, restoration and selector
mutations; the release-closure gate remains green with eleven mutations.

Phase B changes no product core, media, device state, product link or release.

## Phase C — one product core, two closed media identities

Run exactly one fresh product build/WPLTO and seal its Link candidate. From
that single byteidentical core, construct two deterministic media closures:

- **base candidate** — all unconditional v1.4.0 freight, with `defstruct`
  absent from the delivered surface;
- **D2 acceptance sibling** — the same core and shared media roles, differing
  only by an enumerated defstruct library/manifest/surface-candidate delta.

The sibling is an acceptance instrument, not a second product link and not a
release claim. A gate proves byteidentity of every shared role, exactness of
the allowed delta, and absence of any third difference. The selector chooses
the sibling only after a green D2; otherwise the already-built base remains
the sole release candidate. Thus a red conditional row cannot require a
post-device rebuild or silently strand tested product bytes.

Two varied clean checkouts must reproduce the product core and both media
closures byte-for-byte, including manifests and all release-bound roles. The
ordinary geometry, capacity, reset-domain, banner, surface-delivery and
artifact-integrity gates run against the packed candidates. No device session
starts until Phase C and the full A1 result are green.

### Phase-C startup First Red (2026-08-07)

The first Link-92 invocation stopped before it created a product-build
directory and consumed zero WPLTO runs.  The wrapper configured the inherited
one-shot product entrypoint directly and that entrypoint configured itself a
second time; the require-resolver selector correctly rejected the already
selected CALLPRIM profile.  The correction removes the direct preconfigure and
binds the wrapper through `PRODUCT.configure` only.  A permanent startup gate
with a rejected double-configure mutation is wired into `check-source`; the
First Red remains bound as
`c2.3-v1.12-link92-startup-first-red.json`.  This is a pre-WPLTO harness
closure, not a product, linker, geometry, media, Link-92, or release result;
the single product/WPLTO card remains unconsumed.

The corrected wrapper then exposed a second pre-WPLTO bootstrap edge: the
inherited v1.2.1 entrypoint checked its canonical environment before its
configure callback, saw the inherited Link-84 driver, and re-executed that
driver.  Link 84 correctly rejected the v1.4 profile as foreign freight.  No
build directory existed and WPLTO consumption again remained zero.  The same
startup gate now requires the canonical environment plus release, link,
build, manifest, driver and configure bindings before the inherited main
entrypoint; a missing-environment/re-exec mutation is its second rejected
case.  The bound authority is
`c2.3-v1.12-link92-entry-reexec-first-red.json`.

The first fully source-emitted static plane then exposed the remaining
linker-free profile omission: compiler locality removes 36 kind-4 direct call
edges, so the measured direct-entry census is 674 rather than the inherited
v1.3 value 710.  The ordinary direct-entry contract stopped before WPLTO; no
ELF exists and WPLTO consumption remains zero.  The preflight now enumerates
the direct descriptors from the emitted Shelf/C2D pair, compares that result
to the independently recorded profile value, records the measurement, and
rejects an omitted-census mutation.  The partial `link92-r1` directory is
First-Red evidence only; the still-unconsumed product card uses fresh
`link92-r2`.  Authority:
`c2.3-v1.12-link92-direct-entry-profile-first-red.json`.

### Phase-C product-card First Red (2026-08-07)

The first actual `link92-r2` product-card attempt passed the linker-free
profile and reached the fresh replacement-gate closure, where the historical
zero-literal checker stopped it.  The active six-image product still contains
the unique `%lcc-consp` witness with zero literals and 38 code bytes, but the
accepted v1.11 locality carrier moved that function from lcc local ordinal 6
to 5.  The promoted v1.12 carrier preserves local ordinal 5.  The checker
already derives the witness row and global ordinal from the active product,
then incorrectly demands the historical local ordinal 6 as an additional
constant.  This is a stale checker pin, not a semantic witness failure.

The attempt is consumed and no automatic retry ran.  Historical driver
accounting records one whole-program closure-link attempt, while the closer
authorities bind `product_completed=false`, internal product-closure links 0,
no completed WPLTO map, and no ELF or map.  Consequently there is no immutable
linked artifact on which an artifact-only replay can finish Phase C.  No
media, device row, Link-92 candidate or release claim exists.

The recommended disposition is deliberately narrow: rebind the gate to the
unique semantic witness and derive its ordinal from the active emitted
manifest, add loud mutations against both a historical ordinal pin and every
semantic identity field, then run exactly one explicitly authorized
replacement product card with all freight, geometry and scope unchanged.
Until the owner authorizes that exception, Phase C remains stopped.  Bound
authority:
`c2.3-v1.12-link92-zero-literal-ordinal-first-red.json`.

### Semantic-witness correction and replacement authorization

Owner disposition `d1436725` accepts the attributed checker mechanism and
authorizes one replacement card.  The zero-literal gate now binds the unique
semantic tuple `(lcc, %lcc-consp, function, 0 literals)` plus the exact 38
normalized code bytes (SHA-256
`8caea511a5e3e550646b36d942e5715d6d97d840edadd026d5f06506afd6b69d`).
Local/global ordinals and the C2D row remain recorded measurements derived
from the active six-image carrier; neither is an acceptance constant.

The permanent `check-source` lane rejects seven new mutations: restoration of
historical local ordinal 6 and one dimmed mutation for each of image, name,
kind, literal count, code length and code bytes.  The accepted v1.10, v1.11
and promoted v1.12 carriers independently confirm that the exact witness
bytes survive the legitimate local 6-to-5 reorder.  Product bytes changed by
the checker correction: zero.

The replacement card uses fresh `link92-r3`; it inherits no partial output
from consumed `link92-r2`.  This is the sole replacement attempt authorized
by `d1436725`; another red returns to the owner without retry.

### Replacement-card First Red — source ownership closure

The sole authorized replacement reached the compiler and the resident-island
seed link, then stopped before orphan-wrapper acceptance with undefined
`vm_code_load_converged` and `c2_physical_read_converged`.  The semantic
zero-literal correction passed first with local ordinal 5, global ordinal 651
and the independently bound code hash; it is not implicated.

The source closure is exact.  The canonical base `definitions()` acquired
`LISP65_CODE_WINDOW_CONVERGENCE`, `LISP65_DMA_CONTENT_CONVERGENCE` and
`LISP65_C2_ASM_CONVERGENCE` during the parked Link-91/ownership work.  Their
assembly owners, `c2_mapped_far_service.s` and
`c2_mapped_far_convergence.s`, remain correctly routed only by an explicit
`LISP65_CODE_WINDOW_CONVERGENCE` feature in the opt-in `extra_definitions`
tuple.  Link 92 correctly omits that feature because the frozen v1.12 scope
does not reopen ownership or Link 91.  The unconditional base macros
nevertheless expose four calls and suppress their C fallbacks, producing
ownerless symbols.

Adding the assembly owners to Link 92 would silently promote final-parked
freight and is therefore rejected.  The narrow correction direction is to
return all three macros to the same explicit opt-in seam as their owners and
add a permanent source-closure mutation against a base define without its
source owner.  That restores frozen v1.4 scope; it does not deliver the parked
convergence feature.  The authorized replacement attempt is consumed, no
automatic retry ran, and no seed ELF, product link, media or device result
exists.  A further card requires a new explicit owner decision.  Authority:
`c2.3-v1.12-link92-replacement-card-source-closure-first-red.json`.

### Source-owner scope repair and third-card authorization

Owner disposition `b6c0c735` ratifies the refusal to add the parked assembly
owners to Link 92 and authorizes the scope repair instead.  The canonical base
definitions now contain none of the three convergence macros.  The explicit
`LISP65_CODE_WINDOW_CONVERGENCE` opt-in closes the complete three-define bundle
and the two assembly owners at one seam; an ordinary Link-92 build selects
neither.  Thus the repair restores the frozen release scope and does not
promote Link-91/ownership freight.

The permanent startup lane rejects three source-owner mutations: restoring
the parked defines in the base, removing an owner from a selected scope, and
supplying a companion define without its trigger.  It now reports 13 rejected
mutations in total.  The independent code-window convergence gate remains
green at 8 executions, 15 rejected mutations, four refill paths and two
products.  Authority:
`c2.3-v1.12-link92-source-owner-scope-repair-receipt.json`.

The newly authorized product card uses fresh `link92-r4`.  Both earlier cards
ended without a product artifact, so it inherits no ELF, map, medium or device
result.  This is attempt three for the block.  A red of any class returns to
the owner as a block-disposition question; no fourth card follows locally.

### Third-card First Red — parked ownership linker-layout scope

`link92-r4` passed the repaired define/source-owner closure, the semantic
zero-literal witness and the 674-reference direct-entry contract, then reached
the resident-island seed link.  The linker rejected one internally
contradictory geometry claim: its unconditional parked-ownership fragment
captured the six compiler static-stack bytes in the named arena at `$C074`,
while the surviving legacy BSS-triage assertion still required the same six
bytes to inhabit `.noinit` at `$C34D`.  The map therefore shows the owned stack
at `$C074`, empty `.noinit` at `$C353`, and the exact 240-byte hot BSS ending at
`$C34D`; the diagnostic is `C2 inherited noinit/fixed-block geometry drift`.

This is another parked-ownership scope contamination, now in linker-layout
closure rather than base defines.  It is not a capacity result, does not
implicate compiler locality or the source-owner repair, and did not promote
the parked convergence service.  No seed ELF, product, medium or device row
exists.  The third attempt is consumed, no retry ran, and disposition
`b6c0c735` now returns the whole block to the owner.  Authority:
`c2.3-v1.12-link92-third-card-owned-layout-scope-first-red.json`.

## Phase D — bundled device acceptance and Halt #1

The session uses cold reset, complete reset-domain staging and readback, C2J
CLEAR proof, code-identity before symbol interpretation, CPU-view reads for
code, mapping-captured physical translation for data, and no monitor crossing
during an active persistent form. Setup or harness red produces no product
claim and no conditional promotion.

The execution order is deliberately **D1, D3, cold reset, D2**. The
unconditional rows finish before the potentially terminal conditional row.

- **D1 — product and boot:** boot the base candidate; bind the visible
  `WORKBENCH 1.4.0` banner and smoke `q`, `(time)`, strings, reset-domain
  recovery and the promoted compiler carrier.
- **D3 — unconditional freight:** exercise the exact `who-calls` output after
  the already-green string rows. Then run
  the quiet physical editor row: 64+ ordinary physical keys, no observation
  while typing, and an exact final buffer postcondition. This supports the
  softened editor story; it cannot erase the standing RUN/STOP guidance.
- **D2 — conditional defstruct row:** after a cold reset, boot the D2 sibling.
  The owner types `(require 'defstruct)` and observes `t`, then types
  `(defstruct point x y)`. From its RETURN onward there is zero observation or
  monitor traffic for the Phase-A-bound quiet floor (at least the commissioned
  approximately three-minute window). The first look is a single
  postcondition observation. Only visible completion followed by a successful
  `(make-point 3 4)` makes D2 green.

The 179-second figure is a structural price, not a wall-time completion upper
bound. Therefore a non-completion at the first post-window look is **red for
v1.4.0 delivery only**: it keeps `defstruct` out, but does not claim an
infinite hang or correctness defect. Any early observation invalidates the
row. No retry or improvised longer wait occurs in this block.

The session receipt binds each row separately and records the selector result.
After it, Codex prepares one Halt-#1 table containing the candidate identities,
A1 result and any exact exceptions, sizes and walls, all device rows, the
selected public surface, and both prewritten documentation branches. The owner
either accepts that table or returns a named red; Phase E cannot begin on an
implicit acceptance.

## Phase E — mechanical v1.4.0 release and Halt #2

Materialize exactly the Halt-#1-selected branch. A green D2 promotes the
acceptance sibling, exposes `defstruct`, and documents its measured/priced
performance without calling 179 s a target upper bound. Any other valid D2
outcome selects the base candidate, keeps `defstruct` absent, and sharpens the
notes without a product-hang overclaim. In both branches the three
inspect/string names are public, `trace`/`untrace` are explicitly absent, and
the editor wording reflects the physical 64/64 evidence.

Finish the language reference, guide, known issues and release notes; set and
gate the ordinary-release banner `WORKBENCH 1.4.0`; build the complete asset
set from the selected sealed identity; verify hashes, clean-checkout
reproducibility and release-readback inputs; and prepare the closing receipt.
The divergence mechanism used by the v1.2.5 correction release is forbidden.

Halt #2 presents the exact candidate commit, tag, asset names and hashes,
readback plan, documentation branch and closing receipt. Only the owner's
literal publish word authorizes the public-main push, tag and release. A
post-publish readback must bind commit, tag, notes and every asset before the
block may close.

## Halt and failure semantics

- Codex works autonomously through A–C. The owner is needed only for the
  bundled Phase-D session and the two named halts.
- Halt #1 is the freight/device/selector decision; Halt #2 is publish go. They
  are not interchangeable.
- D2 red is an anticipated selector result and does not by itself reject the
  unconditional release. D1 or D3 red, an invalid D2 setup, a shared-role
  identity mismatch, or any correctness/capacity/A1 red blocks Phase E.
- No red may be relabelled as a narrower claim to preserve a date. No new
  product link, hardware improvisation or scope addition is authorized by
  this plan.


## Reviewer acceptance of the phasing — 2026-08-07

Accepted without amendment. Named verifications:

- **The two-variant construction is the smart piece and respects the
  one-truth rule**: one product core, two byteidentically closed
  media variants prepared *before* the session, the D2 row selecting
  which one publishes. No rebuild after the halt, no ship-only
  surface divergence — exactly one variant ever reaches users; the
  other is destroyed by the decision, not shipped alongside.
- Session order protects the bundle: D1/D3 complete before the
  potentially terminal D2 — the comfort freight cannot be hostage to
  the defstruct row.
- Claim discipline is pre-bound on both sides: 179 s stays a
  structural price (a longer wait is not a red), and a red D2 keeps
  defstruct out without founding a hang claim.
- A1 with a preferred-empty exception list, calendar exceptions only
  individually dated, scope freeze and the regular banner binding.

**Phase A is authorized.**


## Link-92 first red disposition — 2026-08-07 (reviewer, Class A)

The mechanism is accepted: the historical checker pinned a *position*
(`local_ordinal == 6`) where it meant an *identity*; the accepted
1.11 locality carrier legitimately moved `%lcc-consp` to ordinal 5,
and v1.12 inherits exactly that placement. The semantic witness (LCC,
0 literals, 38 code bytes) is unchanged. This is the positional-
expectation class the 1.9 park's inversion wisdom names — caught
here pre-completion with a clean mechanism.

**Approved (Class A):**
1. Rebind the witness to **semantic identity** (kind, literal count,
   code-byte identity), never a position.
2. Loud mutations both ways: any reintroduced fixed historical
   ordinal must fail, and any dimmed semantic witness field must
   fail.
3. **Exactly one replacement card**, explicitly authorized here — the
   consumed attempt produced no product artifact of any kind
   (`product_completed=false`: no link, ELF, map, media or device
   run), so nothing is inherited and nothing could be replayed; the
   one-card discipline transfers to the replacement intact.

Product freight, geometry and scope stay frozen; Phase C reopens for
the replacement card only.


## Replacement-card first red disposition — 2026-08-07 (reviewer)

Accepted: the three parked convergence macros sat wrongly in the
canonical base defines while their assembler owners ride only the
parked Link-91/ownership feature path; Link 92 omitted that path
scope-correctly, and the seed link failed on four ownerless
references — cleanly, with no artifact of any kind. The refusal to
"just link the owners in" is ratified explicitly: that would have
silently reopened the finally-parked ownership programme through a
release block's back door. The semantic witness rebind stands green
and permanently gated alongside.

**Approved (Class A):** relocate the three macros to the same
explicit opt-in seam as their owners, plus a permanent gate against
any base define whose source owner is not linked in the same scope
("base define without source owner" fails loudly, with a mutation
reintroducing today's shape).

**Authorized (Class C, review half; owner veto open): one new
product card** once that repair is host-green. Both consumed cards
produced zero product artifacts — nothing is inherited. The card
count of this block then stands at three attempts, and a further red
of any kind escalates to the owner as a block-disposition question,
not another card.


## Block disposition — 2026-08-07 (owner): closure inventory first

The escalation edge fired as bound; the owner adopts the
recommendation. **No isolated fourth card.** Commissioned instead
(desk, autonomous):

**The complete opt-in closure of the parked ownership programme.**
Inventory every trace the parked programme left in the canonical
build — defines, linker fragments, assertions, section placements,
checker expectations — and bind each to the explicit Link-91/
ownership opt-in seam. The two convicted members (the convergence
macros in the base defines; the `$C074` static-stack move against the
`$C34D` `.noinit` assertion) are the seed of the inventory, not its
extent. Completeness is proven by mutation and by a canonical-build
closure check: a seed link of the canonical scope must resolve every
reference and satisfy every assertion **with the opt-in path absent**
— that check becomes a permanent gate so contamination of the
canonical build by any parked feature path can never again be found
one card at a time.

The card question returns to the owner explicitly once the closure
gate is green. The four-value freight, geometry and scope stay
frozen; the sealed parks stay sealed.


### Complete opt-in closure result — 2026-08-07

The commissioned desk closure is green.  Its independent contract
inventories the whole parked selection, not just the two convicted
members: three defines, two assembler owners, eight guarded source/header
consumers, five output sections with six input patterns, six linker
symbols, seven assertions, two linker flags, the fixed overlay-floor
mode, the three full-map platform replacements, and all seven selected
final-inventory additions.  Both selectors close the same bundle: the
Link-91 convergence trigger and `FULL_MAP_OWNERSHIP` cannot select only a
linker fragment, source, define, flag or checker row.

The canonical projection is the exact inverse: zero parked defines,
sources, flags, ownership assertions/symbols or platform replacements;
the ordinary linker retains its single derived
`ALIGN(__lisp65_workbench_noinit_end + 1, 2)` floor.  Sixty-two loud
mutations delete every selected vocabulary member, leak each base-scope
member in turn, switch both floor modes, and remove every guarded-consumer
token.  All are rejected.

The second closure is artifact-side.  The exact Link-92 candidate stack
runs in its owned opt-out directory through one real WPLTO seed link.  The
link resolves and all canonical assertions pass with the parked path absent;
the terminal seed-only edge then stops before island materialization and the
final product link.  The receipt binds one seed link, zero product links,
`product_completed=false`, no final product, and no card.  A second clean
run produced byteidentical receipts and no tracked write outside the two
owned evidence paths.  This check is now permanent in `check-source`.

The selected historical ownership gates remain green.  Their 2026-08-07
rebind is loud: the v1.8 selected linker bytes must remain identical before
its immutable historical generator authority is accepted, and the v1.9
vocabulary checker requires unchanged semantic observations before accepting
refreshed live-gate source authorities.  History is not rewritten; the new
closure receipt binds the current producers.

Authorities:

- `config/c2-v112-ownership-opt-in-closure.json`
- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-ownership-opt-in-closure-receipt.json`
- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-ownership-opt-out-seed-link-receipt.json`

Per the owner disposition, **no fourth card has run or is authorized by this
result**.  Freight, geometry and scope remain frozen.  The next action is the
explicit owner decision on whether Link 92 receives a new card after this
clean foundation.


## Fourth card authorized — 2026-08-07 (owner)

The opt-in closure is accepted (complete inventory, 62/62 mutations,
the canonical WPLTO seed link repeated byteidentically, historical
ownership gates green, the permanent canonical-closure gate live in
`check-source`). **The owner authorizes exactly one new Link-92
product card** on the proven-clean foundation. Green proceeds to
Phase D (the bundled device session with the defstruct D2 row behind
its priced quiet window) and the two halts; another red of any kind
returns to the owner as a disposition question. Freight, geometry
and scope stay frozen; claims stay in rows.


## Fourth-card First Red — 2026-08-07

The one authorized card ran once as fresh `link92-r5`; no retry ran.
Unlike the previous cards it completed both the resident-island seed link and
the sole WPLTO product link.  The SHA-bound PRG, ELF and map exist, the current
post-link identity check is green, and the closed walls report 243 bytes of
Bank-0 text headroom, 54 bytes at E000, 50 bytes in the resident island,
65,423 bytes in the main session region and 1,956 bytes in its overflow.

The card is nevertheless terminal red and none of those artifacts is
promotable.  The inherited v1.2.1 completion protocol launches the *current*
rebound product driver in a fresh process with the private `_complete` action.
The v1.12 top-level parser exposes `profile`, `build`, `check`, `selftest` and
`ownership-closure-seed`, but not `_complete`; argparse rejected the action
before artifact completion.  Consequently there is no artifact-completion
receipt or log, no canonical product manifest, no media and no Phase-D run.
The red-labelled historical raw/base/qualification receipts are separately
the expected typed legacy boundary accepted by the current canonical WPLTO
driver; their current replacement gates are green.  They are not this First
Red.

Classification: **post-link acceptance/completion-dispatch closure**, not
product, locality, geometry, capacity, parked-ownership scope or hardware.
Execution accounting is one resident-island seed link, one product-closure
link, zero retries, zero promotable links and zero hardware runs.  The fourth
card is consumed; a fifth is neither run nor authorized.

Recommended owner disposition: authorize only a completion-dispatch closure
and an artifact-only completion replay against the immutable SHA-bound r5
artifacts, with a permanent mutation requiring every inherited private
completion action to remain dispatchable by the current wrapper.  Such a
disposition must forbid compilation, relinking and a new card; Phase D remains
closed until the replay itself closes.  No repair or replay has been performed
without that decision.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-fourth-card-completion-dispatch-first-red.json`


## Fourth-card disposition — 2026-08-07 (reviewer, Class A)

The red is accepted at its true weight: **the product link succeeded
for the first time in this block** — PRG, ELF and map exist with
green internal geometry (243 B text, 54 B E000, 50 B island
reserves); only the downstream completion failed, on a
harness-lineage seam (the inherited closer invokes the current
driver's private `_complete` action, unknown to its parser). No
artifact was damaged; nothing product-side is in question.

**Approved: the narrow dispatch repair plus an artifact-completion
replay against the unchanged r5 artifacts** — no compiler run, no
relink, no new card; the replay completes *this* card. Conditions:
the repair touches only the closer/driver dispatch seam and carries a
mutation (an unknown driver action must fail loudly at the parser,
and the closer must speak only the driver's public contract); the
replay proves the artifacts byteidentical before completing them;
completion output (manifest, media) binds to the existing r5
identity. Green proceeds to Phase D as authorized; any further red
returns as a disposition question.


## Completion-replay First Red — 2026-08-07

The approved public-dispatch repair is green.  The current driver exposes
`complete`; the closer calls exactly that public action.  The permanent
startup selftest rejects both required mutations: an unknown action exits
loudly through argparse, and a closer changed back to private `_complete`
fails its source contract.  The selftest total rose from thirteen to fifteen
mutations.

The replay then crossed the boundary that stopped the fourth card.  Its
four-SHA preflight passed, the fresh process entered through `complete`, and
artifact completion closed with zero compiler and zero linker runs.  The
completed final PRG, ELF, map and resolved profile are byteidentical to their
WPLTO inputs; the completion receipt and log exist and are SHA-bound.

The same replay stopped before the canonical manifest on a new, fully
attributed checker error.  Its reconstruction required the internal
replacement-gate status to equal the long live `run_wplto` return label
`passed-current-v4-pre-publish-WPLTO-closure`; the persisted internal receipt
actually stores the projected status `passed`.  The code confused two status
vocabularies of the same green gate.  No product or completion data differ.

Per the owner's further-red edge, no correction and no second replay ran.
There is no canonical product manifest, media or Phase D.  Because the final
tree now exists and is deliberately one-shot, the recommended disposition is
a resume-safe manifest/media continuation from the existing completion
receipt: correct only this persisted-status projection, mutate the live versus
persisted vocabulary confusion, and forbid any second artifact-completion
call.  This requires no compiler, relink, new card or hardware.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-completion-authority-reconstruction-first-red.json`


## Authority-projection repair approved — 2026-08-07 (reviewer, Class A)

The dispatch repair and the artifact completion are accepted: final
PRG/ELF/map/profile byteidentical to the r5 WPLTO, `complete` now the
public driver contract with unknown/private actions loudly rejected,
zero compiler/linker/card runs. The remaining red is a pure status
projection: the authority reconstruction expected the live status
name while the persisted receipt correctly carries `passed` — the
passed-not-promoted discipline surprising its own reader.

**Approved: the resume-safe continuation** that corrects only the
status projection (the reconstruction reads the persisted vocabulary,
never the live one), guards the confusion with a mutation (a
reconstruction expecting a live status name over a persisted receipt
must fail loudly), and provably does **not** re-run the completed
artifact completion — the byteidentity of the completed artifacts is
asserted before and after. Green proceeds to the manifest/media
stage and then Phase D as already authorized.


## Base-media mutation-cardinality First Red — 2026-08-07

The approved resume path is green.  It consumed the persisted replacement
status `passed`, rejected the live-status vocabulary mutation, made zero
artifact-completion calls, proved the completed PRG/ELF/map/profile unchanged
before and after, and wrote the canonical Link-92 r5 product manifest.

Media closure then completed and independently rechecked the shared 19-role
system medium.  It also materialized the base library D81 with exactly the
unconditional `comfort` artifact and one L65I row.  The inherited L65I
mutation gate stopped after those bytes were written: although the format and
decoder permit one through 32 rows, four historical mutations unconditionally
address row ordinal 1 and the gate's total is pinned to its two-row fixture.
The legal 80-byte one-row base index therefore raised `IndexError` before the
defstruct sibling or either aggregate variant manifest existed.

No correction or retry ran.  The recommended disposition is a cardinality-safe
mutation gate that retains all 32 cases for two-row inputs and runs the 28
applicable cases plus an explicit one-row execution witness for the base.  A
resume-safe continuation can consume the SHA-bound shared-system and base
artifacts, forbid their reconstruction, then build only the defstruct sibling
and aggregate manifests.  This needs no product compiler, relink, new card or
hardware.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-base-index-mutation-cardinality-first-red.json`


## Cardinality-safe index checker approved — 2026-08-07 (reviewer, Class A)

Accepted green: the authority repair (17 mutations, zero completion
re-runs), the Link-92-r5 manifest with PRG/ELF/map/profile unchanged,
the shared system medium (19 roles) and the base library correctly
materialized with its single `comfort` L65I row. The red is a checker
cardinality bug: the historical mutation checker unconditionally
addresses a second index row and throws IndexError on a legal
one-row index.

**Approved:** the cardinality-safe L65I checker — the 32 existing
cases apply at two-plus rows, the 28 applicable cases plus a
dedicated one-row witness cover the base; an unconditional
second-row access must fail loudly as its own mutation. Then the
resume-safe continuation builds **only** the defstruct sister and
the aggregate manifests; the system medium and base artifacts stay
SHA-fixed and are not regenerated. Green lands the two-variant media
set and opens Phase D as authorized.


## Sibling resolver-order First Red — 2026-08-07

The approved cardinality repair is green: the legal one-row base rejects its
28 applicable mutations plus the dedicated unconditional-second-row witness,
while the existing two-row lane remains at all 32 mutations.  The resume
source gate also proved that neither the shared system nor the base library
could be rebuilt, and their preflight SHAs remained unchanged.

The resume materialized the defstruct sibling D81, then stopped at a new
post-build verifier assumption.  Both L65I rows explicitly declare zero
dependencies.  The resolver therefore returns `[0]` for `comfort` and `[1]`
for `defstruct`; the checker instead expected `list(range(2)) == [0, 1]`,
silently treating every prior media row as an implicit dependency.  Row order
is placement, not dependency identity.  The index decodes, both artifacts are
present, and no product, shared-system or base bytes moved.

No correction or second resume ran.  No aggregate manifest exists.  The
recommended disposition is to bind the verifier to the declared dependency
graph: a zero-dependency row resolves only itself, and a mutation that
reintroduces implicit-prefix semantics must fail loudly.  A later resume can
consume the now SHA-bound shared, base and sibling artifacts without rebuilding
any of them, then write only the two variant and aggregate manifests.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-sibling-resolver-order-first-red.json`


## Aggregate-readback double-configuration First Red — 2026-08-07

The declared dependency-closure repair is green.  `comfort` resolves to `[0]`
and `defstruct` resolves to `[1]`; the checker rejects both an expectation that
adds an undeclared prefix row and a resolver result that omits a declared
dependency.  The resume source gate also rejects reconstruction of each of the
three SHA-fixed inputs: shared system, base library and defstruct sibling.

The resume then wrote both variant manifests, their aggregate manifest and the
byteidentical persisted receipt.  Its immediate readback did not start.  The
wrapper called `configure_shared()` once during `resume()` and again when
`main()` entered `check()` in the same process.  The inherited product
configuration is deliberately one-shot: its first require-resolver selector
advances the CALLPRIM profile from 164 to 166 bytes; the second invocation sees
166 where only the 164-byte predecessor is legal and fails closed with
`require-resolver profile selector order drift`.

This is the exact lifecycle class already caught at Link-92 startup, now on the
aggregate readback seam.  No correction or second check ran.  The four outputs
are SHA-bound but remain non-authoritative until a readback replay succeeds.
The recommended disposition is a wrapper-only repair that permits one product
configuration per process, rejects same-process double configuration by
mutation, and replays only the readback over the frozen aggregates without
rewriting any artifact or manifest.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-aggregate-readback-double-config-first-red.json`


## Resolver-order checker repair approved — 2026-08-07 (reviewer, Class A)

Accepted: the cardinality-safe checker is green (32/32 two-row,
29/29 one-row including the second-row access witness), shared and
base artifacts unchanged, the defstruct sibling materialized. The
red is a checker assumption: it expected prefix `[0,1]` where the
resolver's contract — dependency closure only — correctly yields
`[1]` for a library declaring no dependencies.

**Approved:** the checker learns the resolver's actual contract
(expected rows = declared dependency closure of the requested
package, nothing more), with mutations both ways: an expectation
demanding undeclared rows must fail, and a resolver returning less
than the declared closure must fail. Then the resume-safe
continuation builds the aggregates over the SHA-fixed artifacts;
green lands the two-variant media set and opens Phase D as
authorized.


## Readback repair approved — 2026-08-07 (reviewer, Class A)

Accepted: resolver contract green both ways, all aggregates written
without any media rebuild and SHA-fixed, shared/base/sibling
untouched; the one-shot profile configuration correctly refused a
second `configure_shared()` in the same process — the guard worked,
the harness called it twice.

**Approved:** the readback re-runs in a fresh process with exactly
one `configure_shared()`; the double-configuration shape becomes a
loud permanent mutation (a second in-process call must fail the
harness, not merely the profile); the persisted receipt gains
acceptance authority only from the clean readback. All artifacts
stay SHA-fixed — the readback verifies, it never rewrites. Green
completes the media closure and opens Phase D as authorized.


## Phase C closed; Phase D open — 2026-08-07

The lifecycle repair is green.  The source gate proves that the harness guard
precedes the one-shot product selector and that completion invokes readback in
a fresh process.  Its two permanent counterexamples are both rejected: a
dimmed guard fails the structural gate, and a real second in-process
`configure_shared()` is stopped by the harness with `candidate media harness
double configuration` before the product profile can be touched.

The fresh readback then passed with two variants, nineteen shared roles and six
variant mutations.  The base index retained its 29 cardinality-aware
mutations, the sibling retained all 32, and the resolver contract retained its
two over-/under-closure mutations.  All four aggregate SHAs were identical
before and after; the readback wrote nothing.  The persisted two-variant
receipt therefore gains acceptance authority through the separate readback
receipt.

Phase C is closed: one immutable Link-92 r5 core, one shared system medium, the
base library, the defstruct acceptance sibling and both aggregate manifests
are SHA-bound.  Phase D is open under its already commissioned device-session
law.  No device, D2 selector, Halt, Phase E or release claim is made here.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-two-variant-media-readback-receipt.json`


## Phase-D D1 AUTOBOOT First Red — 2026-08-07

The first Phase-D contact stopped before the D1 product row.  The exact closed
Link-92-r5 shared product D81 was uploaded and read back byteidentically at
819,200 bytes, then its mount was requested through the ordinary FTP command
stream.  After the FTP tool's terminal reset and thirty seconds without
monitor or input traffic, the one allowed screenshot still showed only the
MEGA65/C65 BASIC 65 welcome screen.  Neither `WORKBENCH 1.4.0` nor a `lisp65>`
prompt was visible.

This is a setup/harness First Red, not a product-boot result.  The transfer
entlasts the candidate media bytes, but the log does not independently prove
that the requested mount became active, that AUTOBOOT entered the product, or
that any Link-92 byte executed.  No D1 smoke, D3 freight row, D2 conditional
row, selector decision, Halt or Phase-E action ran; there was no owner input
and no automatic retry.

Phase D is halted at its launch-choreography boundary.  A further contact is
not authorized.  The recommended desk response is a Link-92-specific Phase-D
runner and dry-run that bind the exact medium, mount/reset semantics, the
absence of a redundant post-mount reset, the first-observation floor and a
fail-closed `WORKBENCH 1.4.0` banner assert before any recontact decision.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-phase-d-d1-autoboot-first-red.json`


## D1 setup red — boot-choreography desk commission 2026-08-07 (reviewer)

Accepted: the Link-92-r5 D81 transferred and read back byteidentical;
the first permitted look showed plain C65 BASIC — mount/AUTOBOOT
entry unproven, hence no product claim; D2/D3 untouched.

**Commissioned (desk, no contact):** bind this session's
mount/AUTOBOOT choreography against the proven-green precedent — the
1.3 ship-block D1 rows booted sample D81s from power-on on this
device. Diff the two choreographies step by step (mount mechanism,
drive/image association, reset-vs-power-on entry, autoboot
expectations of the shipped stager) and name what this run did
differently or left unproven. If the precedent choreography is
recoverable as-is, the repeat contact uses it verbatim; any gap gets
a mutation. The contact question returns with the named difference.


## D1 boot-choreography desk diff complete — 2026-08-07

The mount path itself did not differ from the hardware-green v1.3 Ship
precedent: cold reset, fresh BASIC, `mega65_ftp -0 5 -F`, exact put/get,
`mount`, and the helper-owned reset-and-exit with no second explicit reset.
The Link-92 descriptor binds the same default drive-0/unit-8 association, and
the exact D81 has `autoboot.c65` as its first PRG entry with byteidentical
payload plus both required stager inputs (`boot.id`, `lisp65.prg`).  Media,
drive and AUTOBOOT delivery shape are therefore host-closed.

The actual difference was the observation contract.  The First-Red contact
did not machine-assert fresh BASIC before the mount and made one screen-only
observation after 30 seconds.  The historical Link-84 Workbench AUTOBOOT had
already proved why that is insufficient: it still showed the same BASIC 65
splash at 20 seconds and reached the REPL roughly fourteen seconds later.
The standalone Ship rows used an explicit Runtime-state witness instead of a
fixed screen time.  Thus the C65 screen at 30 seconds is an underspecified
early observation, not evidence that mount or AUTOBOOT failed.

The recovered Link-92 launch runner uses the exact precedent mount/reset
sequence, asserts fresh BASIC first, permits no post-mount access for exactly
45 seconds, and then fails closed unless the first capture has no red frame
and contains both `WORKBENCH 1.4.0` and `lisp65>`.  Sixteen mutations reject
wrong media/drive/image/type, missing preconditions, changed reset ownership,
the historical 30-second look, missing postconditions, and their source-level
reintroductions.  The dry-run and permanent `check-source` selftest are green.

No contact or product row ran.  One D1 launch-only repetition through
`scripts/c2-v112-link92-phase-d-boot-hw.sh start-d1` is recommended but remains
owner-gated.  D1 smokes, D3 and D2 stay closed until that launch row is green.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-phase-d-boot-choreography-diff-receipt.json`


## D1 repeat authorized — 2026-08-07 (reviewer, Class B)

The desk diff is accepted: mount, drive-8 and reset match the green
v1.3 precedent; the 30-second screen-only look was simply too early
(historical boot shows the BASIC splash at 20 s and the Workbench
~14 s later). The corrected runner proves fresh BASIC, uses the
identical mount-and-reset path, waits 45 s with zero access, then
requires `WORKBENCH 1.4.0` plus `lisp65>`; 16 mutations and the
permanent check are green.

**Authorized: one D1 launch repeat via the single bound entry
point.** Green proceeds to the D1 smokes and then D3/D2 as phased;
another setup red returns as a disposition question. Owner veto
open before execution.


## D1 AUTOBOOT repetition green — 2026-08-07

The one Class-B-authorized repetition used the sole bound Link-92 entry point.
Fresh BASIC passed, the exact 819,200-byte product D81 uploaded and read back
byteidentically, and the FTP helper owned the mount-and-reset exit with no
second reset.  The runner then made no target access for the complete
45-second window.  Its first and only post-mount capture contained both
`WORKBENCH 1.4.0` and `lisp65>` and no fail-closed frame.

This closes the prior D1 setup red as a premature screen-only observation;
the product AUTOBOOT launch row is green.  It does not yet claim the D1 q,
time, string, reset-domain or promoted-carrier smokes.  D3 and D2 remain
untouched, the selector is unset, and Phase E stays closed.  The next ordered
work is to bind and run those D1 smokes before entering D3 and D2.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-phase-d-d1-boot-repeat-device-receipt.json`

## D1 smoke First Red — `(time)` phase oracle — 2026-08-07

The smoke runner reused the already-green live Link-92 REPL without reset or
remount.  Its q row passed exactly: `(q->string(q*(q 1 64)(q -2 0)))`
returned `"-3.0"`.  The next row submitted `(time(+ 1 2))` only after its
complete active-input echo had been verified.  The device returned `1 3`,
while the runner required the historical Link-89 observation `0 3` exactly.
It therefore stopped before the string row; D3 and D2 remain untouched.

This is a harness First Red, not a product finding.  The second field is the
correct user result `3`; the first is elapsed raster frames and may be zero or
one for this short form depending on its starting phase.  The classifier had
mistaken a prior measurement for the semantic contract.  The recommended
correction is a structural result oracle: a nonnegative frame count followed
by the exact result `3`, while retaining exact form echo, fail-closed-frame
and trailing-prompt checks.  No repetition is authorized by this entry.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-phase-d-d1-time-oracle-first-red.json`


## D1 smoke checker repair — 2026-08-07 (reviewer, Class A)

Accepted: q green ("-3.0"); `(time (+ 1 2))` correctly returned
`1 3` and the checker wrongly demanded the historical measurement
`0 3` — a measured value treated as a semantic contract (the
informative-not-limit rule, violated by a checker). **Approved:** the
structural expectation — non-negative frame count plus exact result
3 — with a mutation pinning any exact frame value loudly red.
**Authorized: one smoke repeat at the standing live REPL** (string
smoke included), then D3 and D2 in order. The device stays where it
is; no reboot needed for a checker repair.

## D1 smokes green — 2026-08-07

The authorized continuation reused the standing Link-92 REPL and preserved
the original First-Red artifacts.  The repaired structural `(time)` oracle
accepted `0 3` as a nonnegative frame count plus the exact preserved result
`3`; the new gate rejects any reintroduced exact frame pin.  The final string
row returned `"hi"` exactly.  Together with the already-bound q result
`"-3.0"`, all three D1 smokes are green.

No reset, remount, product/media rebuild or later-row access occurred.  D3 is
next; D2 and the selector remain untouched, and Phase E stays closed.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-phase-d-d1-smoke-device-receipt.json`

## D3 setup First Red — virtual uppercase transport — 2026-08-07

The D3 split identity itself is green through the library boundary: the base
library D81 read back byteidentically, all seven product preloads matched,
C2J was CLEAR before RUN, the 45-second boot reached `WORKBENCH 1.4.0` plus
`lisp65>`, and the monitor-quiet 120-second `(require 'comfort)` returned `t`.

The first comfort smoke never executed.  On all three verified-input attempts
the requested `(capitalize "hELLO")` appeared as `(capitalize "h")`; the
virtual transport omitted the uppercase characters.  The runner therefore
sent no RETURN, cleared each incomplete line, and stopped at a live empty
prompt.  No editor form, physical key, D2 form or selector action occurred.
This is a harness First Red and makes no comfort-product claim.

The narrow recommendation is a lowercase-only transport fixture:
`(capitalize "hello")` must return `"Hello"` in the ordinary, traced and
post-untrace rows.  The visible trace markers and restoration oracle remain
unchanged.  No continuation is authorized by this entry.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-phase-d-d3-uppercase-transport-first-red.json`


## D3 fixture change approved — 2026-08-07 (reviewer, Class A)

Accepted: D1 fully green; the D3 stop is transport-side (the virtual
line truncated `(capitalize "hELLO")` to `(capitalize "h")`; no
RETURN was sent, nothing ran). **Approved:** the lowercase-only
fixture `(capitalize "hello") → "Hello"` — the semantic claim
(capitalize uppercases the first character) is fully carried by a
lowercase input, and repairing the shift transport mid-session would
be scope creep. The uppercase transport limitation is booked as a
named instrument item on the register side (virtual lines may not
carry uppercase until that transport is separately proven).
**Authorized: one D3 repeat** at the standing session, then D2 as
phased.

## D3 repeat First Red — virtual apostrophe transport — 2026-08-07

The approved lowercase fixture repair is host-green with 21 rejected
mutations and the stronger mixed-case host oracle unchanged.  On the standing
device session, `(capitalize "hello")` returned `"Hello"` and
`(string-split "a,b,c" ",")` returned `("a" "b" "c")`.

The next verified-input row never executed.  On all three attempts the
requested `(who-calls '%ide-region-parts-tail)` appeared without its
apostrophe as `(who-calls %ide-region-parts-tail)`.  The runner therefore sent
no RETURN, cleared every incomplete line and stopped at a live empty prompt.
No `who-calls`, trace, editor, physical-input, D2 or selector row ran.  This is
a harness First Red and makes no comfort-product claim.

The narrow recommendation is the quote-form fixture
`(who-calls (quote %ide-region-parts-tail))`, preserving the exact expected
caller list while avoiding the already historical virtual-apostrophe transport
boundary.  No continuation is authorized by this entry.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-phase-d-d3-apostrophe-transport-first-red.json`


## Owner decision 2026-08-07: library split — `string-extra` + `inspect`

The D3 quote-form fixture is approved as recommended (semantically
identical, unchanged oracle). Before it runs, a surface decision
taken at the last cheap moment: **the `comfort` library splits into
two speaking names** — `(require 'string-extra)` carrying
`capitalize` and `string-split`, `(require 'inspect)` carrying
`who-calls`, `trace`, `untrace`. After v1.4.0 advertises, names are
forever; a heterogeneous grab-bag name was rejected by the owner.

**Commissioned (host-side, Class A/B):** re-materialize the library
rows under the two names (semantics and bytes of the five functions
unchanged — rename only), rebuild sibling and aggregates over the
unchanged product core, rerun the media closure and readback (the
two-plus-row checker cases now apply to the base index), rebind
surface-parity, guide sections and smoke fixtures to the new names,
mutations against any surviving `comfort` reference. **No new card,
no product-link change, no card consumption.**

Session consequence, accepted: the standing session ends; Phase D
restarts on the renamed media with the proven D1 choreography
(autoboot, 45-second window, banner + prompt, smokes), then D3 with
the corrected fixtures, then D2. The device may be powered off until
the new media set is green.

## Library split host closure — 2026-08-07

The commissioned split is host-green without a product compiler or linker
run.  The immutable Link-92-r5 product manifest and all 19 shared-system roles
remain the authority.  The rebuilt base library medium contains exactly two
independent L65I rows, `string-extra` and `inspect`; the acceptance sibling
contains those same two byteidentical artifacts plus `defstruct`.  All three
rows declare no dependencies, so resolver order is exactly the requested
row's declared closure.

The implementation sources and forms did not change.  The three
`string-extra` objects are byteidentical to the accepted combined development
artifact.  The seven `inspect` objects have identical symbolic instruction
payloads, literal lists, flags and lengths; their encoded bytes carry only the
necessary literal-base relocation caused by moving from combined ordinals
3..9 to a standalone 0..6 artifact.  This relocation is named and gated rather
than mislabeled as byteidentity.  Total objects remain ten, resident delta is
zero, and the preserved Bank-2 floors remain green.

Surface parity, the release closure, the guide and the Phase-D runner now bind
the speaking library names.  The permanent split gate rejects the five legacy
public `comfort` spellings, including the old require form, delivery identity,
manifest path and media row.  D3 also rejects both previously observed virtual
transport fixtures: mixed-case input and apostrophe shorthand.  Its admitted
forms are lowercase `capitalize` input and explicit `(quote ...)` for
`who-calls`.

The renamed two-variant media and its read-only aggregate readback are green.
Phase D restarts from D1 on a fresh device session; no prior live-session result
is inherited across the media rename.

Authorities:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-split-media-receipt.json`
- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-split-media-readback-receipt.json`
- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-release-freight-receipt.json`

## Host-closure First Red — compiler-carrier identity — 2026-08-07

The first full `check-source` after the library split stopped in the unrelated
bound-artifact parity verifier before making any product claim.  The current
Link-92 product manifest already bound the accepted v1.12 compiler carrier,
its generated suite and all source authorities, but the verifier recognized
only the historical literal name `c2-product-compiler-tier`, the historical
`tier-generation.json` filename and identical textual path spellings.  The
release carrier correctly uses `c2-v112-product-compiler-tier`,
`generation.json`, and a mixture of absolute and repository-relative
references to the same files.

The verifier now binds semantic identity instead: exactly one product-bound
disk library exporting `%c2-compile-form` and containing the three required
compiler entries, with a recognized generator schema and resolved file
identity for its suite and generated sources.  It still verifies every source
hash, generated output, carrier execution and product-manifest binding.  New
teeth reject historical-name pinning and textual-path pinning, while the
selftest separately proves both in-tree generator filenames.  The repaired
gate is green with 60 bound carrier cases, six product manifests and eight
mutations.  Product bytes, Link 92 and every split-media artifact are
unchanged.

The next full-gate row stopped in the permanent Option-A prior-append lane,
whose intentionally exact current-profile tuple still named Link 90.  This is
the established profile-authority class already recorded for Links 82, 88 and
89: Link 92's accepted locality carrier reduced the profile to 748 entries,
2913 resolutions and 45794 Bank-2 code bytes with build ID `3b48650d`, while
the lane still required 787/3031/47282 and `293611ce`.  A loud dated First-Red
receipt binds both tuples; only those expected values move.  The unchanged
Option-A execution lane must still pass its baseline, two-prior-append case,
five mutations and seven witnesses.  This is a corrected authority, not an A1
exception and not a product rebuild.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-require-profile-authority-first-red.json`

A later Wave-2 metadata check found one more downstream authority drift.  The
full gate had rematerialized the current resident manifest; its SHA changed
from `be818600...` to `95992874...`, while its 14805-byte code blob remained
byteidentical (`cc5319...`) and all 139 derived metadata records remained
identical.  A dated First Red binds the manifest-only delta.  Only the derived
metadata receipt is rebound; the index, public surface and all code bytes stay
unchanged.  This is likewise a corrected authority, not an A1 exception.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-function-metadata-manifest-authority-first-red.json`

## Split-media host close; Phase D reopened — 2026-08-07

The final `make check-source` completed with exit zero through the last Ship
selftest.  A post-run idempotence check also leaves release freight (23
mutations), release closure (11), split-media readback, boot choreography and
D3 preparation (24) green.  The shared Link-92-r5 product D81 remains
`ed4e5c72...`; the renamed base library D81 is `9f4fd70b...` and the
defstruct-acceptance sibling is `e6d780dd...`.  No product link or hardware
contact occurred.

The split decision's pre-authorized session consequence now applies.  Phase D
restarts from a fresh D1 launch using the proved 45-second AUTOBOOT window,
then D1 smokes, D3 with lowercase/explicit-quote fixtures, and finally D2.
The boot preparation binds both the original choreography authorization
`a33a438d` and the split-restart authorization `a1cf5b9b`; D1 recontact is
true, while no later row may bypass the ordered gates.

## Split-restart preparation First Red — historical calendar projection — 2026-08-08

The authorized D1 restart stopped before every device operation because the
boot-preparation checker recomputed its persisted receipt's `recorded_on`
field from the wall clock.  The complete structural diff contains exactly one
changed JSON pointer: `/recorded_on`, from the bound preparation date
`2026-08-07` to the current date `2026-08-08`.  Medium identity,
authorization, choreography, bindings and every semantic field agree.

This is the already named calendar-projection class, not a reason to rewrite
historical evidence each day.  The receipt date is now a historical constant,
and the permanent source mutation rejects a reintroduced `date.today()`
projection.  The repair changes no product or medium and consumes no device
contact.  Once the preparation gate is green again, the existing split-restart
authorization continues to govern the same ordered D1 contact; D3 and D2 stay
closed until D1 passes.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-phase-d-split-restart-calendar-first-red.json`

## Split D1 boot green; smoke inheritance First Red — 2026-08-08

The repaired preparation gate passed, fresh BASIC was proven, and the exact
split product D81 uploaded and read back byteidentically.  After the full
45-second no-access window, the first capture showed both `WORKBENCH 1.4.0`
and `lisp65>` with no fail-closed frame.  The fresh split-media D1 boot row is
green.

Before a smoke form was sent, preflight found that the historical smoke runner
still targeted the pre-split output directory and intended to reuse its prior
q result, running only time and strings.  That would violate the split
decision's explicit rule that no live-session result survives the media
rename.  The runner now binds the fresh split boot, writes only beneath the
split-session directory, and executes q, time and strings anew.  Mutations
reject both prior-q reuse and omission of the fresh q row.  D3 and D2 remain
closed until all three rows pass.

Authorities:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-phase-d-split-d1-boot-device-receipt.json`
- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-phase-d-split-d1-smoke-inheritance-first-red.json`

## Split D1 smokes green — 2026-08-08

The fresh split-media REPL passed all three D1 rows in order.  The q row
returned `"-3.0"`; `(time (+ 1 2))` returned frame count `0` and exact result
`3` under the structural nonnegative-frame oracle; `string-trim` returned
`"hi"`.  Each complete input line was captured before its separate RETURN,
each result was checked independently, and no D3 or D2 row ran.  D1 is green;
the next ordered row is D3 on the renamed library media.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-phase-d-split-d1-smoke-device-receipt.json`

## D3 First Red — trace installation under result polling — 2026-08-08

The split libraries and the D3 prefix are target-green: both quiet requires
returned `t`; `capitalize`, `string-split` and `who-calls` returned their exact
bound results.  The next complete input, `(trace capitalize)`, was verified
before its separate RETURN.  During the ordinary repeated screenshot polling
for the expected result, the target entered the red fail-closed frame after
three seconds with the form still visible and no result.

This is an honest two-case First Red.  It proves neither that `trace` fails
without observation nor that the polling alone caused the transition.  The
known source-less guard/monitor-crossing class is a strong candidate because
the helper observed repeatedly while trace installation was still active, but
the first target execution of this public trace row also leaves an independent
product failure possible.  No `untrace`, post-untrace, editor or D2 row ran.

The recommended next work is host-first: bind the exact macro expansion and
target operation path, determine whether installation owns a guard-sensitive
or persistent interval, and—if so—price and mutation-prove a no-poll first
observation.  No device retry is authorized by this entry.

Authority:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-phase-d-split-d3-trace-first-red.json`


## D3 trace red — host-first attribution commissioned 2026-08-07 (reviewer)

Accepted: D1 fully green on the renamed media; `string-extra` and
`inspect` load; `capitalize`, `string-split`, `who-calls` green; the
`(trace capitalize)` red frame arrived **during screenshot polling**,
leaving the honest binary — monitor crossing during an active trace
installation, or a real target fault.

**Commissioned (host-first, no contact):**

1. Bind from the delivered implementation whether `trace`'s
   function-cell wrap performs persistent writes (append machinery,
   C2J traffic) — if yes, the session choreography misclassified
   `trace` as a non-persistent form, and polling during it is the
   proven Link-72 class; the fix is choreography (quiet execution,
   postcondition read) plus a classification rule: **any form is
   treated as persistent unless proven otherwise** — polling defaults
   to forbidden.
2. Host-replay the exact trace installation against the bound media
   state; a host-green installation with a crossing-consistent
   target red completes the harness attribution; a host-red names a
   product mechanism instead and returns as a disposition.
3. Either way the repeat runs `(trace capitalize)` behind a quiet
   window with a single postcondition read (traced-call output and
   untrace), never under polling.

## D3 trace red — host attribution result 2026-08-08

The host-first binary does not close as harness-only.  It found **both** a
choreography error and an independent product mechanism in the exact delivered
Link-92 artifacts.

First, the target workbench is treewalk-stripped.  Its `lcc-run` sends every
non-direct top-level form through `%c2-compile-form` and
`lcc-install compiled 't`; the native bridge then executes
`c2_product_install` as a transient C2D append, runs the emitted main object and
rolls the append back.  `(trace capitalize)` therefore enters the C2J-governed
persistent path.  Repeated screenshots while waiting for its result were an
invalid Link-72 crossing.  The standing policy is inverted accordingly: every
form is persistent until a bound proof says otherwise; polling is forbidden by
default, and observation is one postcondition read after a bound quiet window
or product completion.

Second, exact macro execution and compilation against the shipped `inspect`,
`string-extra` and compiler manifests is host-red.  `trace` emits a 64-byte
helper CodeObject and an 85-byte transient main.  In that main the delivered
compiler lowers `(function capitalize)` to `PUSHLIT capitalize`; it does not
read the existing function cell as the trace source assumes.  The main stores
that symbol as the supposed original and installs a closure whose code handle
names the transient helper.  The mandatory rollback removes the helper from
the directory but does not undo the external `set-symbol-function` side
effect.  The first later traced call therefore reaches `VM_DIRMISS`; `untrace`
would restore the symbol, not the prior published BCODE function cell.

This does **not** identify which of the two mechanisms caused the observed red
frame at three seconds: the target run had no stage witness, so the red remains
crossing-consistent while the unchanged product is independently red-capable.
It does rule out the commissioned harness-only closure and rules out a quiet
retry on unchanged bytes.  No product or media byte changed, no device contact
ran, and recontact remains false pending an owner disposition over the product
fix.  A future fixed repeat must cover trace installation, traced-call output
and untrace restoration with zero observations during the sequence and exactly
one postcondition read.

Permanent evidence and policy:

- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-phase-d-d3-trace-host-attribution.json`
- `config/c2-repl-observation-policy.json`
- `tools/host-lisp/c2_v112_trace_red_attribution.py`

The contact question returns with the attribution.


## trace fix commissioned — 2026-08-07 (owner)

The host attribution stands: the polling was an illegal Link-72
crossing *and* the delivered `trace` is product-red independently
(symbol literal instead of the function-cell value; the wrapper
closure targets a transient helper the rollback removes while the
mutated function cell survives → `VM_DIRMISS`; `untrace` would
restore the symbol). The persistent-by-default observation policy is
already bound.

**Commissioned: the library-level fix in `inspect`** —

1. Installation captures the **current function-cell value** at
   trace time (never a symbol literal; the resolution happens through
   the delivered runtime primitive, not compile-time sugar).
2. The wrapper lives as a **persistent, rollback-surviving owner**:
   installation and cell mutation commit in the same transaction, so
   no rollback ordering can leave a cell pointing at a removed
   helper; `untrace` restores the captured original BCODE value.
3. Host equivalence: traced call, untraced restoration, rollback-
   mid-installation and double-trace/untrace cases, each with
   mutations; then the split-media closure rebuilds and D3 repeats
   behind a quiet window with a single postcondition read.

**Pre-bound edge:** if the fix provably requires product-core or
compiler changes rather than library-level work, it returns as a
descope question, not as silent scope growth. No device contact
before the rebuilt media are host-green.


## trace fix library-scope First Red — 2026-08-08

The pre-bound edge fired before any source change.  Link 92 delivers no
primitive that returns the current function-cell value.  `function-kind`
collapses every BCODE cell to `bytecode`; `set-symbol-function` returns the
new value it was given; Prim 21 is `%disk-poke`; and `%c2-control` exposes
only the born-code session emitter.  The treewalk implementation can read
`sym_function`, but the product is treewalk-stripped and the delivered
compiler lowers `(function SYMBOL)` to the symbol literal.

This absence is not merely an inconvenient spelling.  Two symbols whose
function cells contain different BCODE ordinals have identical observations
through every delivered Lisp operation available to `inspect`, while correct
`untrace` must restore different values.  No library program can distinguish
those states.  The persistent C2 publisher does capture the old cell in its
private export journal, but clears that journal and returns the definition
name; it exposes neither the old value nor an API that joins an arbitrary
library-side cell mutation to the same transaction.

Therefore the commissioned contract is **not representable as an
`inspect`-only change**.  The descope edge is binding: no change was made to
`inspect`, compiler, core, product, or media; no device contact or D3 retry is
authorized.  The owner choice is between removing `trace`/`untrace` from the
v1.4 surface (recommended; keep `who-calls` and both string utilities), or
commissioning an explicit core capability that captures the old cell and
atomically publishes/restores a persistent wrapper, with the normal new-link
and hardware burden.  Holding all v1.4 freight for that larger block remains
the third, costlier option.

Permanent executable evidence:

- `tools/host-lisp/c2_v112_trace_fix_scope.py`
- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.12-link92-r5-trace-fix-library-scope.json`


## Owner disposition 2026-08-07: trace/untrace descoped from v1.4.0

The impossibility proof is accepted (no function-cell getter in the
delivered ABI; the journal knows but does not expose the old value;
two BCODE cells indistinguishable to a library). **v1.4.0 ships
without `trace`/`untrace`**: `inspect` carries `who-calls` alone,
beside `string-extra`, the 1.10/1.11 performance freight and the
editor correction. The release notes state plainly that `trace` is
designed but awaits a core capability.

Commissioned (host-side): rebuild the `inspect` row and media closure
without the trace pair (mutations against any surviving trace
surface), then the D3 remainder and D2 as phased, then the halts.

**The parked row** (register): trace/untrace with the complete
attribution and the ABI requirements list as its restart package —
a function-cell getter (or journal old-value exposure) plus an atomic
library transaction; reopened only by an explicitly commissioned
core-ABI block with its own link and hardware acceptance. The
delivered surface never carries a tool that cannot restore what it
changed.

## Trace-descope host closure — 2026-08-08

The owner-authorized rebuild is green without relinking the product core.
`inspect` now materializes exactly two objects, `%comfort-callers-index` and
`who-calls`, and exposes only `who-calls`; `string-extra` remains byte- and
semantics-identical with `capitalize` and `string-split`. Surface-delivery
parity binds 93 names. The split library freight is 4,304 bytes, leaving
14,270 bytes in the base candidate and 10,965 bytes in the defstruct sibling,
with zero resident delta.

Both library media were rebuilt and independently read back over the frozen
Link-92-r5 core: base SHA-256
`1a77a2f5d71c58ef8e9650316d7d0103675fd419b5aa96d37e8f44e7b24186b7`,
defstruct sibling SHA-256
`937dc157da3dfbc9dbebe049731633dd6eada1431d2f102e2d5c63c449f80e4a`.
The media closure rejects eight explicit trace-surface resurrections spanning
suite source/object, artifact object, release contract, parity, D3, guide and
language reference; the release-freight, release-closure and D3 gates add
their own independent mutations. The accepted red attribution remains sealed
historical evidence and is checked in archive mode; rebuilt media are never
misrepresented as the bytes that produced that red.

D3 is prepared with exactly three functional rows. The two string rows are
already device-green on unchanged freight; the ordered remainder is the
`who-calls` postcondition and the quiet physical editor row. D2 and both owner
halts remain closed until that remainder is green.

## D3 descope-media result and D2 preparation — 2026-08-08

D3 is green on the rebuilt media. Both quiet library requires returned `t`;
`capitalize`, `string-split` and `who-calls` returned their exact contracted
results. The physical editor row began with an empty `measure3` buffer and,
after a monitor-free owner-input window, persisted all 64 of 64 physical
characters with cursor position 64. The immutable Link-92-r5 product remains
unchanged; no D2 or selector action ran.

The conditional D2 row is now host-prepared over the SHA-bound defstruct
sibling. Its runner cold-stages the full 50,816-byte reset domain, proves C2J
CLEAR and sibling-media readback, boots behind the established 45-second
floor, and then yields all three forms to physical owner input. The 180-second
post-defstruct interval is a device-access-free action guarded by fifteen
mutations; the first acceptance read occurs only after that floor. A green
receipt additionally requires the exact `(make-point 3 4)` result
`(point 3 4)`. The 179-second structural price remains explicitly not a
completion upper bound.

## D2 result and Halt #1 table — 2026-08-08

D2 is red for v1.4 delivery and nothing wider. The sibling passed cold
staging, full reset-domain/C2J-CLEAR proof, byteidentical media readback,
45-second boot floor, banner and prompt. Physical `(require 'defstruct)`
returned visible `t`. After physical `(defstruct point x y)`, the target ran
for a conservative 180 seconds with zero automated observation; the owner's
first physical look then found no prompt and a red frame. `make-point` did not
run. This selects the already-built base candidate, keeps `defstruct`
undelivered, and makes no infinite-hang, new correctness or mechanism claim.

Halt #1 now has the complete table:

| Row | Bound result |
|---|---|
| A1 | `check-source` green with empty exception list; later host closure and descope gates green |
| Product core | Link-92-r5, 41,566 bytes, SHA-256 `fcc785365d2a6d7a3269367a4234cb372783d46b9debdee6ad37e758f6e20a52` |
| Base library medium | 819,200 bytes, SHA-256 `1a77a2f5d71c58ef8e9650316d7d0103675fd419b5aa96d37e8f44e7b24186b7` |
| D1 | Green: banner, `q`, `(time)`, strings and reset-domain recovery |
| D3 | Green: three library names and physical editor 64/64 |
| D2 | Delivery red after the valid quiet floor; base selected, `defstruct` absent |
| Public v1.4 additions | `who-calls`, `capitalize`, `string-split` |
| Explicitly absent | `trace`, `untrace`, `defstruct` |
| Capacity | zero resident delta; base Bank-2 headroom 14,270 bytes |
| Documentation branch | honest trace ABI park; defstruct remains undelivered with performance/acceptance history; editor issue softened by physical 64/64 |

Recommendation at Halt #1: accept the base candidate and open Phase E on that
exact identity. No rebuild, relink or narrower D2 claim is required or
permitted by this result.


## Halt #1 decision — 2026-08-07 (owner): base candidate accepted, Phase E open

D1 green (banner, q, time, strings), D3 green (three library names,
physical editor 64/64), D2 honestly red for delivery (require → `t`;
red frame after 180 seconds with zero observation; no hang or
mechanism claim). The unchanged base D81 `1a77a2f…186b7` becomes the
v1.4.0 release candidate: public additions `who-calls`, `capitalize`,
`string-split`; not delivered: `trace`, `untrace`, `defstruct`; zero
resident additions, 14,270 bytes Bank-2 reserve.

**Phase E is authorized**: notes leading with the performance story
and the three new names, plainly stating the trace ABI wait and the
defstruct status, carrying the editor known-issues softening
(physical 64/64); banner `WORKBENCH 1.4.0` confirmed on hardware;
assets and readback per the standard ritual. Halt #2 is the publish
go.

**Booked to the 1.6 restart package (register side):** the first
observation-free red — on the faster 1.11 carrier (179-second
structural price), require → `t`, then a red frame within 180
seconds with no external access of any kind. This neither confirms
nor refutes live-vs-loop, but it retires "observation-induced" as a
sufficient explanation and sharpens the reopened question's first
experiment.

## Phase E completion and Halt #2 table — 2026-08-09

Phase E is mechanically complete on the exact Halt-#1-selected Base
identity. The curated public candidate is commit `43b709c7` over public
parent `a8775d43`; two varied fresh detached clones reproduced all 23 roles
as set `d4c7f0b0…f8af2f` with no private evidence input. Product and source
archives each packed twice byte-identically, and their unpacked offline
verifiers pass (23 roles/36 inventory files and 2,133 source files).

One packaging First Red was verifier-only: the Python `data` extraction
filter relaxed a read-only mode before readback. The failed partial output
carried no authority. Trusted local extraction preserves the archive modes;
that two-line repair is included in the final public candidate and the full
double-build/double-pack chain passed afterward. No product byte changed.

Halt #2 is now open with the exact candidate, four asset names and hashes,
reviewed notes, public-readback plan and machine-readable preparation
receipt in `docs/planning/v1.4.0-halt-2-review.md`. Public `main` remains
`a8775d43`, no `v1.4.0` tag exists and no release was created. Only the
owner's literal word `Publish` authorizes publication.

## Publication close — 2026-08-09

The owner supplied the exact word `Publish`. Public `main` advanced atomically
from `a8775d43` to the qualified candidate `43b709c7`, annotated tag `v1.4.0`
was created at that exact commit, and GitHub release `367435024` was published.

All four primary assets were downloaded afresh and returned byte-identically.
The downloaded product archive passes with 23 roles and 36 inventory files,
the source manifest with 2,133 files, and the clean-build receipt with two
varied builds and 23 roles. The public release body is byte-identical to the
reviewed notes. No product artifact changed during publication.

The final machine-readable authority is
`tests/bytecode/dialect-v2/evidence/post-release/v140-public-publication-receipt-20260809.json`.

## Dated post-publication authority rebind — 2026-08-09

The sealed Phase-A release-closure receipt continues to bind the exact plan
prefix owned by Halt-#1 commit `8e4f4be4`.  Later publication-close prose is
an append-only historical extension, not release-freight drift.  The permanent
gate therefore verifies the old binding directly from that commit, requires
the current plan to contain those bytes as an exact prefix, and permits no
other reconstructed-field difference.  The historical receipt is never
rewritten.
