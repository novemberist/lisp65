# 2.4 Post-v1.6 Housekeeping Block — report

Status: **content complete; final post-commit certification pending**

Ran: 2026-08-25, autonomous, host-only, zero device contacts.

## Outcome

The input-fidelity era is closed as a source of live housekeeping state.
Eleven permanent clauses now live in the gate register with an owning gate and
a biting mutation. The 10,000-line v1.6 freight plan is sealed historical, and
the v1.7 cutting surface is a lean inventory rather than another rolling plan.

The selected product remains byte-untouched. This block performed no WPLTO,
product link, media build, device contact, tag or release publication.

## Rule and register closure

- The instrument, shared-resource ownership, final-world/shipped-byte,
  phase-ownership, recorded-claim, forecast, derived-coverage, MAP-domain,
  declared-width, diagnostic-world and bounded-triage rules are harvested.
- Cursor navigation and five independent product fixes are recorded as
  delivered in v1.6.0.
- Comfort, capture, hybrid service, prompt and composed display move as one
  v1.7 freight set, beginning with the sealed `(repl)` fault evidence.
- INIT/prompt swap, matcher, blinking cursor and IDE service time retain their
  own rows. Symbol economy becomes optional after the descope dividend.

## Capacity world

The capacity watch is re-derived from the exact published linked ELF
`82bc474e…`, its item-1 receipt and D5: 105 free symbol slots, 1,413 name
bytes, 273 ordinary text bytes, 11 far-service bytes, E000 195 bytes total
with a 159-byte largest hole, and 47 of 371 mapped product-cold bytes. The
diagnostic arena is absent. Old Comfort/witness numbers no longer describe the
selected product.

## Documentation and evidence

Architecture and user documentation now name cursor navigation, verified
MAP-CPU boot refill through the packed facade, queue ownership and the
sanitized execution-boundary recovery. Known Issues already carried the
correct final wording: v1.5's silent DMA path is fixed in v1.6.0, while the
slow-typing Comfort observation is retained as development evidence and not
misattributed to the platform.

The v1.6 first-red set is logically archived in place: all selected receipts
remain tracked and cited, with a machine-enforced named inventory. Moving
their files would break provenance without reducing repository weight. Every
evidence JSON parses. The publication receipt proves that release commit and
tag land on public `main`; local side branches are historical inventory, not
release authorities. Owner SD-card note: retain the three released media and
none of the v1.6 candidate/diagnostic images.

The full-tail rehearsal found one structural omission and no product failure:
the published v1.6 medium producer was not yet a member of the media-builder
enumeration. Enumeration v12 registers that producer and binds its packed
facade gate, 22-role artifact set, offline readback and four freshly downloaded
asset digests. Its ten mutations reject missing producers and incomplete
shipping closure.

## Gate runtime and hard bar

The new housekeeping gate is read-only and rejects five mutations: divergent
public main, stored capacity, a stored E000 aggregate, omitted first-red
evidence and a device claim.
The complete `make -k check-source` rehearsal took 976.32 seconds
(16 minutes 16.32 seconds) and emitted 2,300 lines. That is only a small drift
from the 2.3 baseline and does not justify tiering: release qualification keeps
one complete bar rather than introducing a fast path with a weaker claim.

The self-certification order is strict: this report and its index entry are
committed first; then two consecutive full runs execute on that exact tree.
The owner handoff reports both exit codes, line counts and runtimes. No tracked
file changes after those runs.

## Recommendation

The next owner action is to cut one bounded v1.7 block from
`docs/planning/v1.7.0-pre-plan.md`. The pre-plan itself commissions nothing.

## Reviewer verification — 2026-08-25

The reviewer's independent full run on the closed block: **exit 0, zero
failures, 2,314 lines, clean worktree** — and notably, the tree stayed
clean without discarding a single receipt rewrite. The idempotence the
block certified with its byte-identical double run holds under a third,
independent pass. The 2.4 block is released; the v1.7 Phase 1a diagnosis
proceeds under its commission.
