# 1.9 full-map recharter — final park

Date: 2026-08-06
Status: **FINAL PARK — no retry**

## Outcome

Phase A and Phase B completed green without a product compile, fresh WPLTO
or device contact.  The one-shot preflight also completed green and left the
card build directory absent.  The sole card invocation then stopped in its
host-gate prefix before WPLTO began.

The primary red is acceptance-order drift.  The inherited host-gate sequence
reconstructed several historical receipts before it ran the new vocabulary
check.  Those reconstructors changed only their date fields (and consequently
the hashes of receipts that bind them).  The vocabulary receipt correctly
rejected those changed authorities as no longer byteidentical.  No compiler,
product link, product byte, WPLTO artifact, Link 91 or device run was reached.

This is a harness/acceptance-closure red, not a capacity, placement, ABI or
product red.  The charter nevertheless says that **a red of any kind from the
sole card invocation parks the programme finally**.  That edge governs.  The
date-only historical receipt rewrites are restored to their bound values;
there is no second card invocation.

## What the recharter banked

- A machine-readable 190-section vocabulary, including both relocation
  sections and all seven convicted v1.8 section precedents.
- 29 assembler declarations, 17 explicit ABI policies and the complete
  13-member ELF-derived C-called assembler-leaf set.  The two missing v1.8
  policies are now permanent rows.
- 92 receipt tokens across every enumerated old and new acceptance consumer,
  including a discriminated success/failure receipt union that forbids
  success-field consumption on a typed failure.
- 22 owner/live-set names, ten independent checker expectations and 378
  rejected vocabulary mutations.
- A free general rehearsal: two micro links, two byteidentical relinks of the
  SHA-bound v1.7 failure object, both terminal inventories at 190/190, exact
  ABI closure on both terminal ELFs, and 18 additional recharter mutations.
- Permanent Phase-A and Phase-B gates in `check-source`.

## Final edge

The ownership programme remains parked.  There is no retry, no new WPLTO,
no Link 91 and no hardware follow-up under this charter.  The positive
geometry result remains unchanged: the owned map fits at zero deficit; this
red says only that the sole card's host-gate ordering mutated an authority
before checking it.

Any future work would require another explicit owner-grounded charter and
must treat acceptance ordering and immutable receipt reconstruction as part
of the pre-card vocabulary.  This document does not imply or authorize such
a charter.

Authorities:

- `docs/planning/1.9-full-map-recharter-work-plan.md`
- `config/c2-v19-acceptance-vocabulary.json`
- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.9-acceptance-vocabulary-receipt.json`
- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.9-full-map-replay-closure-receipt.json`
- `tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.9-full-map-recharter-product-card-first-red.json`


## Reviewer acceptance — 2026-08-06

The edge is honored without relitigation: Phase A/B green, the one
card stopped in the host-gate preflight on acceptance-order drift, no
retry. The park stands.

One pattern is now three data points strong and belongs in the
restart wisdom, not in a fourth recommission: the geometry has passed
every time; the programme has failed **only** in the acceptance
machinery, each time one meta-level higher — vocabulary sections, ABI
policies, now acceptance *order*. A pipeline of checkers has
unbounded meta-levels; enumerating them is a race that has now been
lost three times. Any future recommission should **invert the
design**: the card's acceptance becomes a single self-contained
comparison against a golden, SHA-bound layout artifact — one check,
no pipeline, no order, no vocabulary — with the golden artifact
itself produced and reviewed once. Booked as the restart package's
first page; no commission is implied.