# v2.0 phase-0 `$22` bounded device result

Status: **PASS: BOUNDED `$22` SEAM DID NOT RECUR ON r4**

The product and external one-row library were uploaded and read back
byte-identically before boot.  The mixed-publication seam was exercised once:
the owner typed `(list 1 3)` without submitting, pressed Left exactly once,
then submitted once after no error appeared.  The visible result was `(1 3)`;
no `$22` appeared.

At the first returned prompt, before further input, the CPU was stopped.  The
five latch-state bytes were `0000000000`, therefore `tag == 0`.  The 34-byte
`repl.buf` payload was sealed raw but is deliberately not interpreted as latch
data without the tag-last commit.  `nsym` was 642 and `npool` was 8720.

The final-ELF positive control had already proved that the latch can fire and
survive recovery, so this is the decision table's meaningful non-recurrence
branch.  Its claim is deliberately narrow: one bounded recurrence attempt at
the historical external `%read-line-loop` to resident `%rl-poll` seam.  The
next action is the owner's residual-risk decision with the latch retained;
Comfort and Block 3 do not reopen automatically.
