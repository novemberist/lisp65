# v1.7.0 release product card

Status: **PASS: V1.7.0 RELEASE PRODUCT CARD FINAL GREEN**

The release producer rebuilt the hardware-accepted native `INIT.L65` plus A0
world exactly once.  `WORKBENCH 1.7.0` is the unique emitted banner.  The
accepted and release code planes have equal extent and are byte-identical.
Each materialized banner payload changes exactly one ASCII version digit
(`5` -> `7`); every remaining changed byte belongs to a derived C2D, catalog,
build-ID or CRC field.  All non-banner bytecode entries are identical and the
successor attribution has zero unexplained members.

Resident `.text` and `.bss` extents are unchanged.  Comfort, Block 3, the
canonical prompt swap and diagnostic freight remain absent.  Scope and
Acceptance qualify the same frozen ELF/PRG pair; the card builds no media and
makes no device contact.

The next rung is artifact-only release media plus the owner-held D-session.
`Ship` and `Publish` remain owner words.

## Host closure and release media

The artifact-only producer packed a Same-World pair without another WPLTO,
product link or card.  The product D81 is
`1ced465143d8698cac851f671497e273137ad3e49f18a37ad67def7d4045beee`;
the optional-library D81 is
`9df3628a4ca155ea6b009128950e1aa0bb20664bcab7bf11c6b34bf3504c4558`.
`INIT.L65` is absent from the release product medium by visible-member proof.

Two immediately consecutive `make -k check-source` runs completed with Exit 0
over 2,371 lines each.  Their complete logs are byte-identical
(`57777d45...`), and the tracked diff hash remained `dc6988ff...` before and
after both runs.  No repair occurred between them.  The machine-readable
closure is
[`c2.3-v1.7.0-release-host-closure-receipt.json`](../../tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-v1.7.0-release-host-closure-receipt.json).

The host block is therefore closed.  The remaining rung is the owner-held
release D-session; only its green result makes `Ship` decidable.
