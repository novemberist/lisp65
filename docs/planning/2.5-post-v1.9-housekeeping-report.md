# Block 2.5 post-v1.9 housekeeping report

Status: **complete — host-only, zero product bytes and zero device contacts**.

Block 2.5 closes the tool and document debt accumulated through v1.7-v1.9.
It changes no product source, link, medium, public name or domain behavior.
The commissioned work plan is now historical; this report is its successor.

## Delivered results

1. **Measured certification runtime.** A complete `make -k check-source` took
   1,080.398 seconds across 612 executed targets. The new non-certifying
   `check-source-inner` tier omits only the eight measured direct targets at
   or above ten seconds and reduced the living-loop wall time to 552.140
   seconds. The complete target remains the only closing certification.
2. **Planning authority sealed.** The v1.6-v1.9 roots form one explicit
   historical successor chain. The v1.8 tail was moved byte-for-byte, with
   provenance and SHA-256 retained; every planning document has one index row.
3. **Consumption authority consolidated.** One gate now proves bound equals
   consumed for static-plane extent, MAP geometry/output roots and bytecode
   entry headers. One prelink authority owns the seven literal pins and six
   inherited candidate-dependent closures. Eight sharp mutations fall.
4. **Receipt idempotence restored.** Five repeatable producers preserve their
   sealed `recorded_on` value across midnight. The permanent audit separates
   repeatable writers from one-shot contact evidence, so a new day no longer
   rewrites history.
5. **Evidence retention priced, not guessed.** The ignored promotion evidence
   is 3,238,453,527 bytes. Four archives totalling 3,106,203,117 bytes are
   executable inputs to the offline certification gate. The conservative
   recommendation is to retain all five measured archives; no byte was
   deleted. The only possible later candidate is the 132,250,410-byte v1.2.5
   archive, and only after authoritative remote retrieval plus SHA verification
   and an explicit owner word.
6. **The living public surface is synchronized to v1.9.0.** Status, release
   date, publication identities, sync log and native-editor language are
   current. Optional public modules are distinguished from the preloaded v1.9
   profile instead of being silently erased from the language reference.
7. **The domain audit is materialized.** All 139 public metadata rows execute
   against six representative domains under the released product primitive
   table: 834 cells, comprising 483 errors, 179 documented-permissive outcomes
   and 172 silently wrong outcomes across 41 public names. The result table is
   now a permanent contract; this block authorizes no repair.
8. **The naming audit is materialized.** It inventories 139 public callable
   names, 64 internal error IDs, four library designators, eight media/file
   names and six guide terms. Seven callable rename candidates are proposed.
   The audit also corrects the work-plan premise: `v16core` is historical, not
   a current v1.9 designator, and the newer v2 policy prefers direct renames
   rather than unpriced compatibility aliases. No rename is authorized here.

## Permanent gates

The consolidated consumption authority, repeatable-receipt audit, public
domain contract and public naming inventory are direct `check-source` members.
The gate-and-tool register carries the v1.7-v1.9 rules they enforce: composed
Bank-2 ownership, page-congruent MAP placement, tuple equals `LOADADDR`,
consumption rather than armament, final-link text pricing, domain-qualified
addresses, one positioning model per surface and diagnosis/product-world
separation.

The closing commit is certified by two consecutive complete
`make -k check-source` executions with Exit 0 and a clean, byte-identical
tracked tree after each. Exact wall times and log digests are closing-handoff
metadata rather than tracked evidence, so recording them cannot itself move
the certified commit.

## Owner decision

Accepting this report closes Block 2.5 and opens the already prepared v2
planning surface. The only unresolved Block-2.5 choice is evidence retention;
the recommendation is **retain all five archives now**. Any later deletion is
a separately named owner action, never an automatic cleanup.
