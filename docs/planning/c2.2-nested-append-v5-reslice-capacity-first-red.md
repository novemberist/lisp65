# C2.2 semantic append reslice — C2J capacity first red

Status: **append and rollback reslice capacity-green; C2J first red; no product link**

Date: 2026-07-20
Rollback product: Link 32, SHA-256
`189548ea52b9af748217a0da94b7dc1d5daa5f17d190f5817f2fb4af486a676a`

## What the authorized split proved

The old 3,950-byte capacity phase is now four semantic phases.  The old
2,235-byte rollback is now publish-first invalidation followed by physical
wipe/finalization.  Every resulting target section fits the immutable
1,792-byte cap:

| Phase | Bytes | Headroom |
|---|---:|---:|
| root-kind counting | 371 | 1,421 |
| high-front derivation | 1,328 | 464 |
| transient reservation | 1,606 | 186 |
| persistent reservation | 1,273 | 519 |
| rollback unpublish | 739 | 1,053 |
| rollback finalize/wipe | 1,682 | 110 |

The transition tuple occupies bytes already owned by the exclusive 304-byte
append scratch.  It adds **0 bytes** to resident text, Island and ordinary
BSS.  The serial driver remains the only phase loader; no overlay phase calls
another overlay phase.

## First red

The subsequently drafted single C2J phase combines publication, validated
recovery and clearing.  Its target section measures **2,749 bytes**, or
**957 bytes above** the same hard cap.  The stop occurred before this phase
was wired into the serial driver, before the RUN/STOP fixture, before a
product link and before hardware.

Aggregate runtime-bank capacity is not the problem.  Including the invalid
single C2J slice, the target-object quantum projection is 57,246 of 65,536
bytes (8,290 bytes headroom) and 41 session slices.  The per-slice contract is
the sole red gate.

## Next decision

The measured code has three natural, non-overlapping operations:

1. C2J prepare/write/readback before the first target mutation;
2. C2J load/identity/range/CRC validation plus scratch reconstruction;
3. C2J clear/readback after restoration or successful publication.

Splitting by those actions is the preferred next probe.  Each action remains
serially driven; no phase-to-phase call is permitted.  It is not authorized
by this receipt.  Deckel, closed `$e000`, Island and BSS remain untouched.

The B2 product claim remains pending: the common abort callsite exists behind
the v5 build flag, but no C2J cleanup implementation or RUN/STOP fixture was
reached after this first red.
