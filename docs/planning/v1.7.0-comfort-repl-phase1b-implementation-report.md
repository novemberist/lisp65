# v1.7 Comfort REPL — Phase 1b implementation

Recorded: 2026-08-25
Authority: `65c3b76d`
Replacement disposition: `870e5f53`
Receipt: `c2.3-v1.7-comfort-phase1b-variant-b-adapter-r1-receipt.json`

## Outcome

Variant B and the complete Comfort set are green through the final product
link, Scope and Acceptance. The product delivers capture and the adaptive
hybrid; the library delivers balanced multiline input, history, the `l65>`
prompt and composed-display ownership through the shipped fallback. Diagnostic
freight is absent.

No medium was built and no device was contacted.

## Preflight and the one First Red

The first card passed its product link, Scope and Acceptance, then stopped in
the generated product-profile library adapter. Its outer `extends` wrapper
preserved the nested spelling `p0-repl-comfort-resident.json` but discarded the
declaring suite's directory, so the real compiler looked in the repository
root. Both paths and the consumed 1/1 attempt are sealed in the First-Red
receipt.

The replacement makes the resident suite an explicit adapter input. Its
pre-card real-consumer gate compiles all nine cases and four functions; a
mutation removing that binding reproduces the missing repository-root path
before WPLTO. No product source, behavior, capacity or price changed between
the two cards.

## Final product world

The replacement consumed exactly one WPLTO, one product link, one Scope and
one Acceptance run. Scope and Acceptance left the linked pair unchanged.

| Artifact | Bytes | SHA-256 |
|---|---:|---|
| Final ELF | 647,524 | `79158c0e0b0034d6843b90b4acae32ed6363cc4c835e1a68f4a37317bf00aa3e` |
| Final PRG | 41,566 | `db80cab1906fa6afb7f7a7a56e07ad138d7df23633cb5399853823967415a65c` |

The final compiler profile contains `LISP65_V160_INPUT_CAPTURE` and
`LISP65_V160_INPUT_HYBRID`. The refill witness and every other diagnostic
tenant remain absent. The active mapped product-cold arena occupies 324 of 371
bytes, leaving 47.

## Executable walls

- **Loss:** 94 events produced, captured and drained in order; zero drops;
  event 6 is present.
- **Normalization:** 256 raw inputs across modes 2 and 3, 512 final-linked
  consumer executions; full parity and `$A0 → $20`.
- **Hybrid responsiveness:** 0.771155 frames/character, 1.29676 events/frame,
  29.68 percent margin.
- **Variant-B prompt responsiveness:** 0.773218 frames/character, 1.29330
  events/frame, 29.33 percent stationary margin. A 40-character line including
  the one-time prompt cost retains 29.15 percent.
- **Display:** `l65> (list 1 3)` is one composed row; the evaluated row is only
  `(1 3)`, its tail is blank, and the next prompt/cursor share a row.
- **Semantics:** all nine balanced/history/cursor cases plus the aggregate
  input case pass; the largest Comfort code object is 251 bytes.

## Product-profile library closure

The CALLPRIM table is derived from the replacement ELF: 84 entries with IDs
1, 2, 12, 26, 27 and 40 tombstoned. The emitted Comfort suite consumes that
exact set. CALLPRIM 12 is therefore unavailable, and all nine cases complete
through `screen-bulk-p` plus the five `screen-put-char` cells.

The emitted library has four objects and 815 code bytes. Its blob SHA-256 is
`911653530f52979a92da462ad354d1fa9cc783e42d97ee955d870297a3c16fe5`.

## Capacity and claim boundary

Bias-adjusted free capacity is **32 symbol slots / 581 name bytes** against the
32/384 release floor: zero slot margin, 197 name bytes. Any further named
helper remains a mandatory repricing event, and D5 must measure with
`repl-comfort` loaded.

This report claims host semantics, final-ELF product properties, the emitted
product-profile library artifact, Scope and Acceptance only. It does not claim
media closure, hardware behavior, D5 or release readiness.

The first full repository-tail run found one historical-consumer drift after
these claims were already green.  The completed v1.6 display-card verifier ran
its old three-object construction shape before its sealed-evidence projection;
the authorized `%repl-prompt` successor therefore tripped a Stored-World
equality.  The verifier now consumes the living composed-display gate and
keeps its historical identities bound to their sealing commit.  Mutations
reject both a non-green successor and altered sealed provenance.  No product
or artifact byte changed in this tail conversion.
