# Block 2.5 public-surface refresh

Status: **complete — documentation only**.

The root README, user guide, architecture overview and maintained Known Issues
already described v1.9 correctly. The refresh changed only live documents
whose current-world statements were stale:

- `docs/project-status.md` now binds the published v1.9 commit/tag, product
  identities, 136/136/136/136 hardware boundary, D5 and exact claim boundary;
- the v1.9 release date is 2026-08-30 rather than “pending publication”;
- the public sync log records the completed commit, tag, release ID and asset
  readback instead of the pre-Publish state; and
- the living language reference identifies v1.9, distinguishes documented
  optional modules from the preloaded product profile, and describes the
  native prompt editor.

No historical release note or sealed receipt was rewritten. The public text
now makes the same claims as the v1.9 release receipt: lossless prompt input
and native prompt editing are delivered; type-ahead during evaluation,
Comfort, matcher/blink, `$22` and domain repairs are not.
