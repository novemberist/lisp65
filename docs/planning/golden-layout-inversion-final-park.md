# Golden-layout inversion — terminal product-card disposition

Status: **final park confirmed; the exceptionless replacement card is consumed**

Date: 2026-08-09

## Outcome first

The owner-authorized card was invoked exactly once.  It stopped before WPLTO,
before a linked candidate ELF existed and before the golden comparison ran.
No retry, second golden, Link 91 or device action is authorized.

The stop was not geometric.  The inherited F1 static-plane gate compared a
historical product-build identity with the newly reconstructed carrier:

| Field | Expected | Observed |
| --- | ---: | ---: |
| Images | 6 | 6 |
| Entries | 787 | 787 |
| Resolutions | 3,031 | 3,031 |
| Roots | 350 | 350 |
| Bank-2 bytes | 47,282 | 47,282 |
| Bank-2 SHA-256 | `f09a167b…df846` | `f09a167b…df846` |
| Product build ID | `0x293611ce` | `0x2e90e85f` |

The sole rejected field was `product_build_id`.  That field is not part of the
reviewed golden layout.  The pre-WPLTO gate therefore repeated the historical
mistake in a different layer: it pinned a carrier identity before allowing the
candidate geometry to reach the one authorized comparison.

## Accountability and claim boundary

The golden itself remains valid and its permanent gate stays green.  The card
runner, however, retained `F1W.static_gate()` as a construction check.  That
was outside the approved acceptance cut.  The uncaught `ProbeError` is also a
runner defect; the stopped state was reconstructed afterward without rerunning
the card.

These facts do not reopen the budget.  The authorization said that a red of
the single card parks the programme with no retry.  The exact invocation made
no WPLTO probe, emitted no linked ELF and never compared against the golden,
but it was still the one command authorized as the product card.  Treating the
pre-WPLTO location as permission to repair and try again would add an exception
that the owner did not grant.

Machine authority:
`tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-golden-layout-inversion-product-card-first-red.json`.

## What remains banked

- the 26,436-byte golden, SHA-256 `65a13501…7b4f7`;
- the single exact layout comparator and its twelve mutations;
- a freshly reconstructed static product whose complete geometric counts and
  Bank-2 bytes match the bound Link-90 authority;
- proof that the only pre-WPLTO rejection was a historical build-ID identity;
- zero WPLTO, linked-product, Link-91 and device side effects.

This programme is final under the approved direction-plan commission.  A
future attempt would require a new explicit owner commission whose pre-card
proof establishes that no non-geometric identity or historical closer lies
between producer entry and the linked ELF.  No such commission is implied.

Priorities 2 (defstruct desk recharter) and 3 (trace Core ABI) remain
independent and untouched.


## Owner final disposition — 2026-08-09: one replacement card

The edge routed the final disposition to the owner, and the owner
rules: the red is a **pre-inversion harness defect** — the card
driver retained `F1W.static_gate()` as a non-geometric precondition
and stopped on `product_build_id`, a container identity the reviewed
golden design explicitly declares non-authoritative; the golden
comparison itself never executed and no card artifact of any kind
exists.

**Ordered:**
1. `static_gate()` leaves the card driver; a permanent mutation
   forbids **any** non-geometric precondition in the card path — the
   completion of the inversion's own design intent.
2. **Exactly one replacement card**, with the explicit clause: **no
   further exception, whatever the outcome.** A red of any kind —
   geometric, harness, cosmic — parks Priority 1 finally and
   unappealably; this clause is part of the authorization itself.
3. Green proceeds per the standing table: 1.5 Halt 2 reopens, the
   parity pilot reopens, Link 91 becomes the bundled session's first
   row.

The park document stays in place until the replacement card's
outcome supersedes or confirms it.

## Replacement-card outcome — 2026-08-09

The replacement driver was committed before its preflight and proved the
ordered inversion mechanically: 55 local path calls audited, exactly one
golden comparison, zero non-geometric product preconditions, and all six
mutations rejected (`static_gate`, profile authority, container build ID,
historical closer, missing golden, doubled golden).  The preflight then armed
exactly one exceptionless replacement card.

That card was invoked exactly once.  It stopped during static-plane producer
construction, before WPLTO, because the freshly routed build lacked
`receipts/defstruct-static-plane-authority.json`.  It emitted no linked ELF,
never reached the golden comparison and touched no device.  The error is
captured verbatim in
`tests/bytecode/dialect-v2/evidence/architecture-blocks/c2.3-golden-layout-inversion-replacement-product-card-final-red.json`.

This is a harness/producer-path red, but the owner's replacement clause named
that class explicitly: **no further exception, whatever the outcome**.  The
red therefore confirms this park.  Priority 1 is final and unappealable; there
is no retry, second replacement card, Link 91 or device tail.  The golden and
its comparison remain valid banked evidence, but no product geometry claim is
made because the replacement candidate ELF never existed.


## Reviewer acceptance of the final park — 2026-08-09

The unappealable clause holds and is honored: Priority 1 is finally
parked. For the record, stated plainly: **the golden comparison has
never executed.** Across six cards in two programmes, the geometry
passed every time it was measured, and every death occurred in the
producer/acceptance machinery *around* the card — the last one on a
missing receipt file during producer construction. What remains
permanently: the reviewed golden artifact (the geometry's truth,
SHA-bound), the zero-non-geometric-precondition guard, the single-
comparison gate, and 6/6 relapse mutations.

For a distant future recommission (none implied, none permitted as
continuation): the honest requirement is now visible — the *producer
path* needs the same inversion the acceptance got: a self-contained
card producer whose complete input closure is bound and mutation-
proven before any attempt, so a card can only die on geometry. That
sentence is the whole restart wisdom.

**The direction plan proceeds to Priority 2** (defstruct completion
edge, desk) and Priority 3 (core ABI) unchanged; the bundled device
session loses its Link-91 row and keeps the others.