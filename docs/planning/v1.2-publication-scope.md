# lisp65 1.2 publication scope

Status: **prepared for Class-C halt #1; not a publication authorization**

Date: 2026-07-27

## Recommended cut: slim 1.2 release

Publish the exact promoted C2-lite product and the curated public source
snapshot. Do not add unfinished 1.2.x freight merely because the 1.2
foundation has reserve.

The product payload already exists as the verified R6 ship closure:

- 43 files;
- 2,798,989 bytes before outer archive compression;
- product artifact set
  `37998ce7b6698757fe3839d0af1467e95505fe10e6be6bc7f28a6991cb09941d`;
- package set
  `82ddc3d7fd8bc048b2803081866aa5320a08bd226d18b063c403a33fc9e7e038`;
- manifest SHA-256
  `d7f5c29a0577fd9117c13ea934a467f0be86b2e4629390fcc9b1c4d84bda2324`;
- packaging receipt SHA-256
  `170e1b60ba4fd5f52db73644ece82567b72331eb3d6b2b82fc0661cfc30c9fd8`.

The outer public archive and release receipt are publication work performed
only after owner go. Their inputs must be copied from the R6 closure; no
product rebuild, relink, or substituted medium is permitted.

## Included public claims

- C2-lite Chip-RAM execution and stage-before-publish recovery;
- definition-to-nullary-first-call cure, claimed at `<=16` frames cold and
  `<=10` frames warm;
- KERNAL-free product-owned post-boot closure and generated L-full input;
- byte-identical transactional rollback and usable REPL after RUN/STOP;
- symbol-bearing DIRMISS diagnostics;
- G5 `9/9`, G6 `5/5`, and the exact promoted product/package identities.

## Visible but unclaimed measurements

- one-argument published call: 68 frames;
- GC envelope: 89 frames, one collection, 96 contract block reads;
- 27.653-second observed boot upper bound during stabilization.

## Explicit exclusions

- Freezer during an active definition, `restart-repl`, E3/E4, and other C2.3
  work;
- generalized n-ary direct-call optimization;
- bitops;
- ship builder;
- further parity/control-flow freight;
- any change to the sealed C2-lite memory geometry.

## Public-source preparation

The public candidate is a curated export, not a merge of private history. It
must contain:

- the release notes, known-issues register, sync log, contributor/DCO policy,
  and issue templates;
- only allowlisted source and user/contributor documents;
- a generated `PUBLIC-SOURCE-MANIFEST.json`;
- a DCO-signed commit based on public `main`;
- an independently verified source archive;
- no public push, tag, or GitHub release before the owner decision.

### Public clean-build preflight

The public build entry now follows the C2-lite single-emitter path directly;
the retired 1.1 tier composition is absent. `make workbench-product` builds
from an empty `build/` tree, runs one WPLTO closure, completes publish-last
binding, creates both D81 images, and checks the exact 19-role set.

The permanent clean-build gate ran that entry point in two detached,
`--no-local` fresh clones with different hash seeds, epochs, and time zones.
Both reproduced:

- product artifact set
  `37998ce7b6698757fe3839d0af1467e95505fe10e6be6bc7f28a6991cb09941d`;
- product build ID `0f511db1`;
- profile build ID `c88b44eb`;
- all 19 sealed roles byte-for-byte.

The gate receipt is
`tests/bytecode/dialect-v2/evidence/architecture-blocks/`
`c2.2-public-clean-build-gate-receipt-20260727.json`. Private acceptance
receipts are not compilation, link, media, or qualification inputs.

## Decision at halt #1

One question only: approve the clean-build-qualified slim source snapshot and
the verified R6 bundle, and authorize publication?
