# v1.9 B-light display repair — final red

Status: **B-light descoped; Block A carries v1.9**

The single authorized r7 WPLTO/product link emitted a pair, but the mandatory
difference attribution stopped before Scope or Acceptance.  The stopped
consumer was a historical r2/r3 header-closure function.  r7 had rebound its
candidate-dependent helper to the live successor output root, so that function
compared sealed r2/r3 receipts with the r7 stdlib header and reported
`r2/r3 stdlib-header consumption closure drift`.  This is an era-crossing
checker defect, not an established product defect.

Read-only observations show why the result is retained as useful evidence:
both real compiler consumers used the exact r7 header, the final ELF has the
single direct-cell prompt positioning model, the complete 80x25 framebuffer
gate is green, and seed/final Fixed-Bank0 geometry remains derived.  Those
observations do not make the pair a candidate: the full attribution did not
close and neither Scope nor Acceptance ran.

The all-known-pin preflight therefore missed one member — the
candidate-dependent inherited r2/r3 attribution closure.  The pair is sealed
as unqualified evidence:

- ELF: `ddba7b5eeb62356ed92d9e9f933d394ceefee7d7e2f1f349496355cfc31ec04a`
- PRG: `82f4edfdfb1da15b56792b0d8a70cb6095f1d8e3348d30f486cb0cea1e15f7c1`

The bound one-round fallback now applies without another link or read-only
resume: B-light leaves v1.9, the native-prompt cursor Known Issue remains open,
and Block A is the sole release carrier.  The repaired source and frozen pair
remain evidence for a later owner-gated reopening.
