# C2.2 Link-33 coordinated residency plan (2026-07-20)

Status: **bounded placement probe completed First Red; no product link or
hardware claim.**

## Temperature and placement

| Freight | Temperature | Link-33 seed | Planned home | Reason |
|---|---:|---:|---|---|
| C2J abort orchestration | cold, abort only | 257 B Bank-0 driver | one Session-family `abort-control` phase plus a minimal resident serial executor | validation/rollback phases remain overlays; only the resident executor may load an overlay |
| transient-handle normalization | hot, every dynamic entry lookup | 182 B new Island code | Resident Island, unchanged | no transport is permitted in the entry path |
| shared entry materializer | hot, every refill | 1,051 B Island code | Resident Island, unchanged | the hot-refill single-source contract requires the one direct materializer |
| session-emitter root descriptors | cold, emit-time only | 336 B in the $E000 state section | fixed Bank-5 region immediately below C2J, DMA read/write | descriptors are immutable between `literal_prep` and `literal_append`; the 10-B scalar state remains CPU-direct |
| resident-Island installer | boot only | 1,650 B Boot slice plus 161 B resident anchor | already in the Boot family; anchor remains resident | the slice and its 1,536-B image already moved; its ten-function closure contains shared Session transport, and moving the anchor into its same-VMA payload would self-overwrite and violate “no overlay calls an overlay” |

The abort-control state uses four bytes of the existing lifetime-exclusive
append scratch. It does not create resident state. The controller returns a
bounded `(start slot, end slot)` command; the resident executor is the sole
overlay caller.

The emitter-root region is 336 bytes at Bank-5 offsets 50,416…50,751. The
maximum export-journal extent ends at 42,031 and C2J starts at 50,752, so the
three regions are disjoint by construction.

## Pre-probe arithmetic

The non-LTO target objects establish only direction, not acceptance:

- abort driver 250 -> 126 B; new abort-control slice 132 B;
- emitter state 346 -> 10 B; literal-prep 1,501/1,792 B and literal-append
  648/1,792 B;
- installer already-evacuated finding: zero additional Island credit.

Therefore the first three directions are expected to close the $E000 overlap
and reduce ordinary Bank-0 pressure, but they cannot arithmetically erase the
independent 228-B Resident-Island deficit: both hot residents are required to
remain. A formal $E000 reopening or another owner-selected resident domain is
needed if the Whole-Program measurement confirms that remainder.

## Probe rule

Exactly one product-shaped Whole-Program-LTO seed is permitted. It is not a
product link: no final Island materialization, packing, publish-last binding,
hardware execution, promotion, or inherited green. Every wall is measured
from the seed map. A red wall stops and returns to review.

From this point forward, every C2 prelink capacity receipt must include this
Whole-Program-LTO dry measurement. Object-section sums remain useful for
attribution only; they are not capacity evidence under LTO.

## Whole-Program result — First Red

The one authorized seed reached final Whole-Program LTO exactly once and
stopped in the linker. No PRG or ELF was produced. The measured headrooms are:

| Wall | Link-33 First Red | Coordinated probe | Change |
|---|---:|---:|---:|
| Bank-0 text | -270 B | **-162 B** | +108 B |
| ordinary Bank-0 BSS | -265 B | **-167 B** | +98 B |
| Resident Island | -228 B | **-228 B** | 0 B |
| `$e000` window | +195 B | **+531 B** | +336 B |

The temperature split therefore works but does not close residency. The cold
C2J controller is a 132-B Session overlay with a 139-B resident serial
executor. The 336-B emitter-root array now occupies the disjoint Bank-5 range
50,416…50,751; only 10 B scalar state remain CPU-direct. All 21 append slices
fit the immutable 1,792-B cap.

The installer audit changes no placement: its 1,633-B payload already resides
in the Boot family. The remaining 161-B resident anchor shares transport code
with the Session family; placing it in the same-VMA Boot payload would
self-overwrite and violate the no-overlay-calls-overlay invariant. Its legal
additional evacuation credit is therefore exactly 0 B.

Applying the L65R-v1 catalog and 256-B payload geometry to the final-LTO map
projects 13,921 B for the Boot family (51,615 B headroom) and 60,062 B for the
Session family (5,474 B headroom). These are map-derived pack projections, not
packed-image or product claims.

The First Red is bound in
`c2.2-link33-coordinated-residency-placement-probe-receipt.json`. Its LTO
object, map, diagnostics and resolved profile are read-only. Link 32 remains
byte-identical at
`189548ea52b9af748217a0da94b7dc1d5daa5f17d190f5817f2fb4af486a676a`.

Three independent resident walls remain red. Per the owner order, this stops
before any further placement attempt. The next decision must choose a new
resident-domain arrangement or formally reopen `$e000` with a lower floor.

The Bank-0 deficits must not be added blindly: ordinary BSS follows `.text`,
so evacuating at least 167 B of text would arithmetically close both the -162-B
text wall and the positional -167-B BSS wall. Together with the independent
228-B Island deficit, the lower-bound coordinated move is therefore 395 B,
leaving a theoretical 136 B in `$e000`. This proves only that a formal window
reopening is arithmetically eligible. It does not select tenants or include
alignment, facade/vector or closure costs; no lower floor is authorized.

## Owner decision — formal one-time `$e000` reopening (2026-07-21)

The window is reopened for one Whole-Program placement probe under a hard
**450-byte total-debit cap**, including facade, alignment and closure costs.
Freight is restricted to two packages: at least 167 bytes of whole-function
Bank-0 text relief that also closes the positional BSS deficit, and at least
228 bytes of whole-function Resident-Island relief. The selected package is
`c2_abort_driver` plus `c2_facade_target_c2_dma`, and
`vm_runtime_overlay_transaction_begin`,
`vm_runtime_overlay_transaction_end` plus
`vm_runtime_overlay_exec_batch_island`. Exactly one appended three-byte
facade edge may serve the relocated batch bridge. No unrelated tenant is
eligible.

The probe is one product-shaped Whole-Program-LTO measurement, not a product
link. It reruns the applicable structural gates and stops on the first red.
If it is green, the measured remaining `$e000` bytes become the final
contractual floor. There is no third reopening; every later resident demand
automatically triggers MUST/SHOULD freight triage. Link 32 remains the
untouched rollback line until a separately bound successor link passes its
entire gate set.

## Formal reopening result — First Red

The single authorized Whole-Program attempt reached LTO once and stopped in
the linker. The named packages measured 181 B of text relief and 235 B of
Island relief; all three `$e000` gap sections plus the one facade vector cost
419/450 B and left a provisional 115 B. Text (+41 B), Island (+7 B) and the
window were arithmetically green, but ordinary BSS remained **-379 B**. No
product link, PRG, ELF, final-floor binding or hardware run occurred.

The First Red disproves the assumed text/BSS positional coupling. Fixed-VMAs
for handoff, facade and C2 low state sit between `.text` and downstream
`.rodata`/`.data`/`.bss`; moving text cannot move BSS through those anchors.
The new facade output also exposed a separate lld load-range collision and a
212-B downstream placement advance, but removing that tooling effect would
still leave the baseline BSS wall red. The attempt is bound in
`c2.2-link33-e000-reopening-placement-probe-receipt.json`; Link 32 remains
byteidentical. Per the owner rule this returns to review with no retry.

## Owner decision — one independent ordinary-BSS triage (2026-07-21)

Exactly one new product-shaped Whole-Program-LTO placement probe is authorized
for the independent 379-byte ordinary-BSS deficit. Before that one measurement,
the failed map is inventoried object by object with temperature and lifetime.
The placement ladder is binding and ordered: proved lifetime unions first;
DMA-capable cold state to Bank 5 second; the CPU-hot remainder into the fixed
Bank-0 block from `$c080` within its 273-byte margin third; and only an
unavoidable session-hot remainder may consume the provisional 115-byte E000
floor. The 212-byte ordinary-`.rodata` LMA advance/collision is corrected by
explicit placement in the same probe.

The 450-byte total E000 debit remains unchanged. The Resident Island and the
1,792-byte slice cap are closed. Whole-Program LTO is the capacity authority;
object sums are inventory only. First Red stops without retry. A green probe
binds the final E000 floor but does not authorize Link 33, a product identity,
hardware execution, or a presmoke. Link 32 remains the untouched rollback line.

### BSS-triage pre-LTO First Red

The generated script stopped in lld's parser before LTO, map emission or any
linked artifact. Removing the former standalone facade output left one empty
`SECTIONS {` opener immediately before the reopening assertions. The object
inventory and source/model gates had passed, but no capacity wall was measured
and the one Whole-Program-LTO attempt remains unconsumed. The diagnostic is
bound in `c2.2-link33-bss-triage-placement-probe-receipt.json`; Link 32 is
unchanged. Per First-Red discipline, the structural repair and the actual
one-shot capacity run require a fresh explicit continuation.

Owner continuation is granted: remove only the empty generator opener, retain
the Pre-LTO evidence byte-for-byte, then run the still-unconsumed single WPLTO
capacity probe in a distinct continuation evidence directory.

### BSS-triage WPLTO continuation — First Red

The parser repair passed and the one remaining authorized Whole-Program-LTO
attempt was consumed exactly once. lld emitted the LTO object and a complete
map, then the explicit ordinary-`.rodata` VMA/LMA assertion stopped the link;
no PRG or ELF exists and no post-link structural gate ran.

The map proves that the placement ladder itself closes every measured resident
wall: Bank-0 text has 41 B, ordinary BSS **229 B**, the fixed hot block 33 B,
the Resident Island 7 B and `$e000` a provisional 115 B. The 240-B heap is at
`$c245…$c334`; fixed state ends at `$c335`, exactly 33 B before the runtime
overlay. The ordinary BSS ends at `$bf9b`, 229 B before `$c080`. These are
map observations, not passed capacity gates; the final window floor remains
unbound.

The First Red is solely the requested `.rodata` placement contract. The
generated `SECTIONS { . = 0xb694; } INSERT BEFORE .rodata` assignment records
`$b694` in the map but does not constrain llvm-mos's inherited `.rodata`
output section. Pinned lld starts that section at `$b5f8`, immediately after
the explicitly placed low-resident state, with VMA=LMA and no load-range
collision. That 156-B earlier start explains why actual BSS headroom is 229 B
rather than the 73-B projection.

The continuation is bound in
`c2.2-link33-bss-triage-placement-probe-continuation-receipt.json` (SHA-256
`cdd28156…`). The artifacts are read-only and Link 32 remains byteidentical.
There is no retry, Link 33, final-floor binding or hardware presmoke under this
authorization. The narrow next contract decision is whether to bind ordinary
`.rodata` by the actual predecessor relation
`end(.lisp65_c2_kernal_state) == start/loadaddr(.rodata) == $b5f8`, or to own
the complete inherited `.rodata` output definition merely to force `$b694`.
The former preserves the measured 156-B BSS dividend and is the recommended
cut; either choice requires a separately authorized WPLTO attempt.

### Owner authorization — predecessor-bound successor WPLTO

The recommended cut is authorized for exactly one new product-shaped WPLTO
probe. The free-standing `$b694` location-counter assignment is removed.
Instead, the linker contract requires
`ADDR(.rodata) == ADDR(.lisp65_c2_kernal_state) +
SIZEOF(.lisp65_c2_kernal_state)` and `LOADADDR(.rodata) == ADDR(.rodata)`;
the currently measured endpoint is `$b5f8`. The prior First-Red artifacts and
Link 32 remain immutable. The successor uses a distinct output directory and
receipt, reruns all probe gates without inherited green, and stops on its first
red. A green result exposes the measured 115-B floor as a binding candidate;
the final contract binding, Link 33 and hardware still require later steps.

### Predecessor-bound successor WPLTO — First Red

The one authorized successor WPLTO was consumed exactly once. The symbolic
`.rodata` predecessor relation passed at `$b5f8`, and all five measured walls
remained positive: 41 B text, 229 B ordinary BSS, 33 B fixed hot block, 7 B
Resident Island and 115 B `$e000`. The E000 debit is 419/450 B. Handoff,
pre-ownership (including the fixed heap), profile-data references and runtime
slice caps passed before the first red. No product-closure link or hardware
run occurred, and the final window floor remains unbound.

The first reached structural red is the fixed-facade gate. At `$f3ec`,
`c2_entry_records` in `.lisp65_c2_kernal_window.c2_resident` emits
`JSR $1800` directly to Island-resident `c2_product_handle_normalize`. This is
the precise cross-domain edge the facade contract forbids; KERNAL freedom and
all later gates were not reached.

The map exposes a zero-capacity candidate for the next review: the current 14
three-byte vectors occupy `$b5a2…$b5cb`; I/O reveal occupies `$b5cc…$b5d6`,
MAP switch `$b5d7…$b5e0`, and the fixed state begins at `$b5e4`. A fifteenth
vector at `$b5cc`, with I/O reveal and MAP switch shifted by three bytes, would
consume the existing three-byte `$b5e1…$b5e3` alignment pocket while leaving
state, `.rodata`, BSS, Island and `$e000` geometry unchanged. This is a map
projection and a facade-contract change, not an authorized fix or capacity
claim.

The First Red is bound in
`c2.2-link33-bss-triage-predecessor-placement-probe-receipt.json` (SHA-256
`6822becb…`); all seed and gate artifacts are read-only. Link 32 remains the
unchanged rollback line. There is no retry, floor binding, Link 33 or presmoke
under this authorization.

### Owner authorization — fifteenth facade-vector successor WPLTO

The measured facade repair is authorized for exactly one new product-shaped
WPLTO placement probe. A fifteenth three-byte fixed vector at `$b5cc` is the
only new window-to-Island path for `c2_product_handle_normalize`; the direct
`JSR $1800` from `$e000` is forbidden. I/O reveal moves to `$b5cf` and MAP
switch to `$b5da`, consuming exactly the existing `$b5e1…$b5e3` alignment
pocket. Fixed state remains at `$b5e4`, ordinary `.rodata` at `$b5f8`, and all
five measured wall values must remain unchanged. The vector address and the
15-entry facade are contractual identity.

The probe writes distinct evidence, inherits no green gate and stops at its
first red. A green probe exposes the 115-B `$e000` remainder for the separate
final-floor binding step. It does not authorize that binding, Link 33 or any
hardware execution.

### Fifteenth facade-vector successor WPLTO — First Red

The one authorized WPLTO attempt was consumed exactly once. The complete map
confirmed the projected zero-capacity move: the five walls remain 41/229/33/7/
115 B for text, ordinary BSS, fixed hot block, Resident Island and `$e000`;
the formal window debit is 422/450 B. The 15-vector facade passed at 45 B with
`c2_facade_handle_normalize` at `$b5cc`; handoff, pre-ownership, profile-data
references and KERNAL freedom also passed. The final floor nevertheless
remains unbound.

The first red is in the later preinstallation-Island relocation-provenance
gate. The retained relocation at `$b5cd` correctly targets the Island
normalizer, but its source is the contractually fixed three-byte assembler
vector at `$b5cc`. That vector is deliberately a zero-sized `NOTYPE` symbol,
whereas the current gate accepts only positive-sized C function intervals;
it therefore reports `source-function-unresolved`. This is a proof-model
gap, not an unmediated window exit or a capacity failure.

The narrow recommended continuation is to teach that gate one additional
contract-derived source interval: exactly
`(.lisp65_c2_host_facade, $b5cc, 3 B, c2_facade_handle_normalize)`. All other
Island references remain subject to positive-sized function provenance, and
mutations must reject an unlisted facade address and a vector of any other
size. Because the ELF and all preceding gate outputs are SHA-bound and
read-only, this can be a separately authorized pure gate replay; no compiler
or linker retry is needed. The First-Red receipt is
`c2.2-link33-bss-triage-facade15-placement-probe-receipt.json` (SHA-256
`59209a27…`). Link 32 remains byteidentical; Link 33 and hardware are not run.

### Owner authorization — fixed-facade provenance replay

The preinstallation-Island gate gains exactly one contract-derived source
interval: `(.lisp65_c2_host_facade, $b5cc, 3 B,
c2_facade_handle_normalize)`. This is not an unsized-symbol heuristic. A
wrong interval address, wrong interval size and an unlisted facade relocation
address must each be rejected by the permanent model matrix. Every other
Island relocation still requires positive-sized function provenance.

The corrected gate is replayed once against the immutable WPLTO ELF; no
compiler or linker runs. A green replay authorizes binding the measured
115-B `$e000` remainder as the final contractual floor and then a fresh Link
33 with the complete gate program. Any red replay or later gate stops the
chain immediately.

### Fixed-facade provenance replay — First Red

The pure replay ran once with zero compiler/linker/product/hardware runs and
stopped before relocation binding. The exact interval and all three source
model mutations passed, but its section field was `None`: the existing
`symbol_sections` helper intentionally omits zero-sized `NOTYPE` symbols and
therefore cannot identify the assembler vector's owner. The ELF itself still
proves a zero-sized symbol at `$b5cc` inside the 45-B
`.lisp65_c2_host_facade` output at `$b5a2`; the input tree and Link 32 remain
unchanged.

The next narrow cut, if authorized, is to derive the owning section from the
contractual facade output range or a section-index-preserving ELF view instead
of the C-function-only helper. The exact `$b5cc + 3 B` interval and the same
wrong-address/wrong-size/unlisted-address mutations remain unchanged. Receipt
SHA-256 is `0c55bc59…`. No retry, final-floor binding, Link 33 or hardware run
occurred.

### Owner authorization — st_shndx provenance successor replay

One further pure replay is authorized. ELF `st_shndx`, decoded by
`llvm-readobj --symbols`, is the primary section-ownership truth for the
zero-sized vector. The contractual facade range is an independent crosscheck:
the symbol and all three bytes must lie inside the indexed 45-B facade output,
and an index naming any other section is red. No general `NOTYPE` allowance is
introduced. The wrong-address, wrong-size, wrong-section-index and unlisted
facade-address mutations are all permanent.

The predecessor replay First Red is immutable input. This successor performs
no compile or link. If green, it authorizes the already specified 115-B final
floor binding and fresh Link 33; any red stops the chain.

### st_shndx provenance replay — green; final floor bound

The successor replay passed with zero compiler/linker/product/hardware runs.
ELF `st_shndx=14` names `.lisp65_c2_host_facade`; the zero-sized symbol is at
`$b5cc`, and the complete three-byte vector lies inside the 45-B output at
`$b5a2`. The range crosscheck and all four mutations passed. The immutable
WPLTO tree and Link 32 are unchanged. Replay receipt SHA-256 is `67422c4b…`.

Per the owner-authorized green disposition, the measured **115-B `$e000`
remainder is now the final contractual floor**. The generated linker script
and the KERNAL-freedom gate both require actual window headroom `>= 115 B`;
the report distinguishes that floor from any headroom above it. A third
opening is forbidden, and any later resident demand triggers automatic
MUST/SHOULD freight triage. Fresh Link 33 is authorized next with no inherited
green gate.

### Fresh Link 33 after final-floor binding — First Red

The authorized Link 33 stopped in its prerequisite Resident-Island seed link;
no product-closure link or hardware run occurred. The protected receipt is
`c2.2-product-link33-bss-triage-structural-receipt.json` (SHA-256
`43913289…`), and Link 32 remains byteidentical. The 115-B final floor remains
validly bound; it was not reached by this failed seed.

The cause is one complete profile-binding error in the new product-link
driver. It reconstructed the profile from the older nested-append base and
therefore selected 20 append slices, omitted `LISP65_C2_RESIDENCY_TRIAGE`, and
left `SESSION_EMITTER_STATE_BYTES=346`. The green WPLTO profile has 21 slices
including `abort_control`, includes the residency-triage define, and binds the
session emitter state to 10 B. The resulting oversized `.text` and state then
overlap Handoff, Gap 0 and profile rodata; the final-floor assertion also
fires as a downstream consequence. These are not independent capacity reds.

The narrow recommended continuation is a profile-single-source repair: Link
33 must consume the same generated/configured profile object as the green
WPLTO (`SLICES`, `FEATURES`, 10-B session state), not rebuild it from an older
base. A new seed/product attempt needs review; no retry is implicit.

Owner authorization accepts that continuation with a class-closing hash gate.
The canonical object is `config/c2-link33-product-profile.json`; WPLTO and
product-link drivers must consume it through the same strict configurator and
must not restate any profile field. A pure zero-link replay first proves that
the immutable green WPLTO resolved profile is exactly the object's nine
defines, 21 append slices, 10-B state and 15-vector facade. The replay receipt
and the next fresh Link-33 receipt must publish the identical object SHA-256.
Only after the replay is green may the new one-shot Link 33 start, with no
inherited structural or capacity result.

The binding replay passed with zero compiler/linker/product/hardware runs.
The canonical object is SHA-256 `4ceb35d4…`; its receipt is `2ac45ba1…` and
binds the historical WPLTO resolved profile byte-for-byte to the same nine,
21, 10-B and 15-vector configuration. The fresh profile-bound Link 33 may now
run; every structural and capacity gate remains fresh.

The fresh attempt proved the profile-hash gate and produced exactly one
Resident-Island seed link, then stopped before a product link. The remaining
final-section inventory is still the 140-name Link-28 pin and rejected the
correct 167-name Link-33 shape (`4` old names absent, `31` profile names new).
Receipt SHA-256 is `7e128fcb…`; Link 32 remains byteidentical. The seed map's
five resident walls are all positive, but these are observations only because
the inventory gate was first red. Any profile-derived successor inventory and
any new Link-33 attempt require separate review; no retry is authorized.

Owner authorization now supplies that review. The inventory expectation is
derived from the same configured profile object: the 140-name Link-28 envelope
loses its prior append ABI and receives the canonical 21-slice ABI plus the
exact E000-reopening and BSS-triage sections. The required review attribution
is four removed names, 31 additions and 167 expected names. Missing and excess
names remain equally fatal, and a same-count resurrection of an old Link-28
name is a pinned negative. A pure replay against the immutable First-Red seed
precedes—and alone may unlock—one fresh Link-33 attempt.

Capacity reporting must distinguish the 531-B pre-package window value from
the final floor: 416 purpose-bound bytes occupy the window, while 6 facade
bytes sit outside it; therefore `531 - 416 = 115 B` and the total formal debit
is `416 + 6 = 422 B`. The fresh Link receipt must expose this full equation.

The replay passed exactly: 167 expected and actual names, four removals, 31
additions, three negative mutations rejected and an unchanged immutable seed
tree. Receipt SHA-256 is `d8e6d6f3…`; no compiler or linker ran. The one fresh
Link-33 attempt authorized by the owner may now start in a new output tree.

### L65R-v2 final resident blocker — branch-temperature First Red

The owner-authorized branch-temperature pass classified the complete 56-byte
L65R-v2 addition in `vm_runtime_overlay_exec_family` as boot-exclusive: the
Slot-8 boot/state guard and authenticated-frame selection are unreachable from
the Session catalog.  The record verifier now performs the guard, seals the
frame and publishes it through the existing lifetime-exclusive install seam;
the resident dispatcher is again format-neutral.  The source contract rejected
five attribution mutations and six relocation mutations.

The single product-shaped WPLTO probe nevertheless stopped before Link 33.
`vm_runtime_overlay_exec_family` shrank by the full 56 bytes, while WPLTO grew
`vm_fixbinop` by 2 bytes and `vm_run_inner` by 8 bytes.  Net resident-text
reclaim is therefore 46 bytes: `.text` fell from 38,030 to 37,984 bytes against
the fixed 37,982-byte corridor and remains **2 bytes red**.  The dependent
ordinary-rodata anchor error is consequential, not a separate wall.  Both Boot
slices remain comfortably under 1,792 bytes (record verifier 1,411; installer
1,322), and Link 32 is unchanged.

The automatically triggered freight triage found no eligible resident bytes:
all three SHOULD items (bitops, public gc/room/error carrier, restart-repl) and
all three COULD items (new H/I/J substrate, color-scroll rider, tick/time) are
not yet loaded in this candidate, so each has measured reclaim 0 bytes.  The
existing IDE/IDEX/M65D conversion, internal collector, error renderer and input
path are either MUST foundation or released 1.1 behavior and are not silently
reclassified.  Further subtraction therefore requires an owner choice touching
MUST/released surface; byte shaving, a new placement attempt, and a third
window opening remain unauthorized.  The protected diagnosis is
`c2.2-l65r-v2-boot-branch-relocation-first-red-diagnosis.json` (SHA-256
`2217ab1e…`).

It passed the canonical profile and derived 167-name inventory, completed one
seed link and Island materialization, then the final product-link attempt
stopped before emitting a product. Slot 37 grew from the 1,633-B seed carrier
to 1,883 B after the true 1,781-B Island image replaced its 1,536-B placeholder;
the unchanged cap is 1,792 B, so the exact overflow is 91 B. Final composition
is 102 B installer code plus 1,781 B embedded image. Receipt SHA-256 is
`35e1ac30…`; Link 32 remains the rollback identity and hardware is not run.

The leading successor direction for review is a two-record boot contract:
one executable installer slice and one identity-bound data-only Island carrier,
each independently below the cap. The installer DMA-copies from boot storage,
checks the destination CRC and publishes readiness last; no overlay invokes
another overlay. Raising the cap is forbidden. Shrinking the 1,781-B hot
Island by at least 91 B is the alternative residency problem. Neither path is
authorized by this First Red.

Owner authorization selects one contract-first two-record probe, with the
actual 1,781-B image extracted from the bound seed ELF. L65R-v1 cannot express
a non-executable record because every entry requires an in-payload entry point
and ABI 1. The probe therefore defines a strict L65R-v2 evolution: unchanged
32-B header/entry widths, one `DATA_ONLY` flag, `entry_offset=$ffff`, ABI 0 and
the record VMA interpreted as the fixed `$1800` destination for that kind.
New C2 products emit v2 only; stale v1 Attic generations are invalidated before
restage, so no dual decoder or feature sniffing exists.

Record 8 remains the executable installer. Record 9 is boot-only data, cannot
be dispatched, and supplies its physical source as boot-base plus catalog file
offset. The required sequence is generation/record/payload authentication,
direct DMA, destination CRC, then READY publish-last. Any failure clears READY
and wipes the destination. The probe must report exact boot-pack geometry and
mutate generation, kind/flags, entry sentinel, bounds, source CRC, target CRC,
execution attempt and premature publication. It emits no product bytes and
does not authorize another Link 33.

The contract probe passed. It consumed the exact 1,781-B Island image from the
SHA-bound seed ELF (`8651e7fb...`), not a placeholder. Record 8 contains the
102-B executable installer and retains 1,690 B below the unchanged cap; record
9 contains the 1,781-B data-only carrier and retains 11 B. The ten-record v2
boot model is 14,325 B, including 154 B of additional alignment/storage cost,
and leaves 51,211 B in the 64-KiB boot store. Its carrier begins at file offset
`$3100`, so the direct DMA source is `$08203100`; source and destination share
CRC `$4c63` and the exact payload SHA.

All 16 negative cases rejected fail-closed, including a v2 data record under a
v1 header, callable or ABI-1 carrier metadata, stale generation, source/target
CRC faults, nested overlay loading and premature READY publication. The
failure model clears READY and wipes `$1800`; the success model publishes
READY only after destination authentication. Receipt SHA-256 is
`5a52cb00...`; compiler, linker, product-link and hardware counts are all zero.

This proves the L65R-v2 record contract, exact boot geometry and publish-last
host model only. L65R-v1 cannot honestly encode the non-executable carrier.
Consequently the next gate is separate review of a v2-only product
implementation and product-shaped capacity/placement probe (packer, target
decoder and generation transition) before any further Link 33. No product
delta or prior structural green is claimed by this receipt.

### Handoff reanchor feasibility — map-only result

The owner commissioned one map-only calculation after the boot-branch WPLTO
stopped two bytes past the fixed `$b481` handoff. No compiler, linker, product
byte or hardware action was permitted. The calculation binds the failed map
(`9e365be6...`) for the actual `$b483` text end and the last valid
predecessor-bound map (`88e1eb02...`) for the lawful low-resident chain.

The requested standing 32-byte LTO-noise reserve is feasible. The handoff must
move by **34 bytes**, from `$b481` to `$b4a3`: two bytes clear the current
overlap and 32 bytes become reserve. Handoff, the fifteen-vector facade, I/O
reveal, MAP switch, fixed low-resident state, ordinary rodata, the verifier
binding table, data and BSS then move as one predecessor-bound chain. Ordinary
BSS ends at `$bfbd`, leaving **195 bytes** before the unchanged fixed block at
`$c080`. The exact maximum absorbable shift is 229 bytes; it would put the
handoff at `$b566`, BSS exactly at `$c080`, and leave 227 bytes of text room.

Two tempting pockets are explicitly excluded. The two bytes between state and
rodata in the failed seed are an already-rejected predecessor-placement drift,
not alignment capacity. The 33 bytes at `$c335..$c355` lie after the fixed
`$c080/$c218` blocks and remain positional, unchanged and non-fungible for the
reanchor.

The verifier-table address quoted historically as `$b86d` is not current:
Link 28 corrected it to `$b914`, while the last valid Link-33 WPLTO geometry
places it at `$b932`. The reanchor projects it at `$b954`; the two Handoff CRC
operands project from `$b4aa/$b4ae` to `$b4cc/$b4d0`. These are projections,
not accepted pins: an authorized implementation would repin all 15 vectors,
the three publish-last addresses, five authority files, eleven gate sources
and six successor-receipt families. Historical receipts remain immutable.

Banner trimming is not a competing resident solution. Trimming the visible
Lisp banner content saves **0 Bank-0 resident bytes**, because that content is
bytecode. Removing the complete native banner launch has only a historical
3-byte upper-bound measurement (while retaining the separately beneficial
REPL byte-index correction); carried to the current two-byte overlap it would
leave one byte, not the requested 32-byte standing reserve. Rolling back the
whole banner block is invalid attribution because that block also carried an
81-byte Bank-0 credit.

The machine-readable result is
`c2.2-handoff-reanchor-feasibility-receipt.json`; it leaves Link 33 blocked and
Link 32 untouched pending the owner choice.

The owner selected Option 1 alone on 2026-07-21. The authorized migration is
exactly `$b481 -> $b4a3` with all fifteen vectors and three publish-last
addresses repinned as one successor package. The exit is one fresh WPLTO run
with all walls and structural gates; Link 33 remains blocked until that receipt
returns green and is reviewed. Banner trim, byte shaving, a third E000 opening,
and movement of `$c080/$c218/$c356` remain forbidden.

### Handoff reanchor WPLTO — preinstallation-Island gate First Red

The one authorized WPLTO attempt was consumed and stopped before Link 33. The
immutable map confirms the reanchor itself: `.text` ends at `$b479`, the new
handoff begins at `$b4a3`, and the resulting 42-byte text reserve exceeds the
requested standing 32-byte floor. The other observed walls are also positive:
195 B ordinary BSS, 273 B fixed hot block, 7 B Resident Island and the final
115-B `$e000` floor. Boot and Session slices retain at least 11 and 31 B. These
are observations from a stopped probe, not accepted capacity results.

First Red came later in the preinstallation-Island relocation gate. Its six
reported references are three LO/HI address pairs, all in the named
`vm_runtime_overlay_install_island` root and all targeting exactly `$1800`.
Disassembly binds them to three zero-valued, 2,048-byte `__memset` calls: the
mandatory predecessor erase before installation and the two compiled failure
paths that wipe the destination. No Island-owned value is read and the earlier
control-flow stage accepted the exact two existing guarded Island edges.

This exposes a gate-model contradiction. The written Island invariant says
that the installer may write and verify the destination while manufacturing
it, but the current retained-relocation rule rejects every data reference
without distinguishing such writes. The narrow recommended continuation is a
contract/gate correction followed by a **pure replay** against the SHA-bound,
read-only WPLTO ELF: permit only complete LO/HI pairs from the named installer
that feed a zero-valued, 2,048-byte `__memset` of the whole Island; every read,
other operation, wrong target/length/value, unmatched reference and unguarded
control edge remains red. The correction must carry mutations for both sides.
It authorizes no compiler, linker, second WPLTO, product link or hardware run.

The protected diagnosis is
`c2.2-handoff-reanchor-wplto-preinstall-gate-diagnosis.json`. Link 32 remains
byteidentical and Link 33 remains blocked pending explicit review of this
bounded correction/replay proposal.

The owner authorized exactly that narrow manufacturing-write correction and
one pure replay.  The corrected preinstallation-Island gate passed against the
unchanged read-only ELF: all six retained references form the three complete
LO/HI pairs used by zero-valued, 2,048-byte `__memset` calls from the named
installer root.  The mutation matrix rejects a wrong target, length, fill
value or call, an incomplete pair, an extra reference and a missing wipe.

The same replay then stopped at the later Boot-lifetime gate.  Its 42 reported
"installer" edges are numeric-VMA collisions in the fixed-overlay model, not
installer references: 40 arise from workbench-overlay control flow and one
from `vm_callprim` to `buf_from_stage` in `.lisp65_rt_buffer_alloc`; the final
workbench edge targets `eval_init`.  Retained relocation provenance finds 40
actual references whose target section is `.lisp65_rt_island_00`, all 40
originating inside that same installer section, and **zero external installer
edges**.  The gate incorrectly models ownership as the bare interval
`$c356..$c84e`, even though multiple overlay sections deliberately occupy that
VMA.

The bounded recommendation is therefore the already-established overlay
identity correction: derive installer ownership from retained relocation
target section plus symbol/addend, never from bare VMA.  Equal numeric targets
owned by another overlay section are not installer edges; an external
relocation to the installer section, an ambiguous target, or unresolved
provenance remains red.  Mutations must prove both directions.  This diagnosis
authorizes no correction or second replay; compiler, linker, WPLTO, Link 33 and
hardware counts remain zero.  The protected diagnosis is
`c2.2-handoff-reanchor-wplto-boot-lifetime-gate-diagnosis.json`; Link 32 remains
the rollback line and Link 33 remains blocked.

The owner authorized the exact section-qualified correction and one successor
pure replay.  The correction resolves retained relocation targets through the
ELF section table or the complete set of sections claimed by a symbol; it
fails closed on ambiguous installer provenance.  An equal numeric VMA owned by
a neighbor section is explicitly accepted, while an external installer
symbol, an external installer-section-plus-addend and ambiguous provenance are
each rejected.  The original First Red and its empty replay output remain
immutable; the authorized successor uses a new one-shot receipt and output
tree.  No compiler, linker, WPLTO, Link-33 or hardware action is included.

The section-qualified model and all four mutations passed, but the one
authorized successor replay stopped inside its symbol-provenance reader.  The
60 rows labelled unresolved are fully identified: 28 target
`__lisp65_workbench_overlay_start`, which ELF assigns to
`.lisp65_workbench_overlay`, and 32 target the explicit Absolute linker marker
`__lisp65_workbench_runtime_overlay_vma`.  Both equal `$c356` by contract.  The
reader rejected their valid five-field `llvm-objdump --syms` rows because it
required at least six whitespace fields; this discarded the provenance before
the corrected ownership model could consume it.  The true installer-target
set remains 40 internal relocations and zero external ones.

The next bounded recommendation is parser-only: consume structured `Section`
fields from `llvm-readobj --symbols`, accept the section-bound workbench marker
and the exact contract-named Absolute runtime-overlay VMA marker, and retain a
hard error for every unknown Absolute symbol at the same numeric address.
Mutations must cover both valid five-field/Absolute markers and invalid
unknown-Absolute/external-installer targets.  This is a new review point: the
authorized replay was consumed, no additional replay ran, and Link 33 remains
blocked.  The protected diagnosis is
`c2.2-handoff-reanchor-wplto-lifetime-symbol-parser-diagnosis.json`.

Owner authorization commissions the common ELF-truth layer and permits one
immediate parser replay.  `tools/host-lisp/elf_truth.py` consumes structured
`llvm-readobj` JSON and is the only new symbol/section/relocation source.  It
preserves overlay multiplicity and `st_shndx`, requires exactly one sized or
contractual interval owner, and admits Absolute markers only through an exact
name/value registry.  Boot-Lifetime is the first client; the remaining named
gates migrate in stages under their existing mutation suites before the C2.2
acceptance chain.  The immediate one-shot replay remains bound to the same
read-only ELF and does not authorize Link 33.

The shared structured reader and Boot-Lifetime first client passed in the one
authorized symbol replay.  The replay stopped only at its following wall-set
comparison: it expected 273 B for the fixed block while the active BSS-triage
contract correctly includes the 240-B fixed hot heap.  The actual arithmetic
is `$c356 - ($c218 + 45 + 240) = $21 = 33 B`; the canonical Handoff-reanchor
WPLTO driver already pins 33 B.  All five observed walls are positive (42,
195, 33, 7 and 115 B respectively).  This is a replay-harness pin error, not a
capacity change or product finding.

The authorized replay was consumed.  A further replay requires review of the
single correction `273 -> 33`; no compiler, linker, WPLTO, Link 33 or hardware
action ran.  The protected diagnosis is
`c2.2-handoff-reanchor-wplto-symbol-replay-wall-pin-diagnosis.json`.  The ELF
truth migration remains commissioned independently; Boot-Lifetime is migrated,
and the other named gates remain staged work before the C2.2 acceptance chain.

The owner authorized the exact replay-harness correction from the obsolete
273-B pre-hot-BSS pin to the post-triage 33-B pin, followed by one additional
pure replay.  No capacity floor or product byte changes; the value is the
already-authorized fixed-heap geometry.  A green replay may proceed to the
fresh Link-33 attempt; any further First Red stops the chain.

The wall-pin replay passed and is SHA-bound as the mandatory predecessor of
one fresh Link-33 attempt.  Its wall set is 42/195/33/7/115 B, Boot-Lifetime
reports 40 internal and zero external installer relocations, and five
successor-pin reports were emitted.  Owner authorization now supersedes the
earlier `Link 33` prohibition for exactly one product-closure attempt; hardware
remains unauthorized until that link and every fresh structural/capacity gate
return green.

The fresh Link-33 driver invocation stopped in prerequisite checking before an
output tree, compiler or linker existed.  Its inherited prerequisite compared
the current canonical profile (`6226b60c...`, ten defines, facade `$b5ee`)
against the historical profile-binding receipt (`4ceb35d4...`, nine defines,
facade `$b5cc`).  That receipt remains valid history but cannot be current
authority after L65R-v2 and the Handoff reanchor.  The green wall-pin replay
already supplies the correct successor profile pin (`377afbeb...`) and section
inventory pin (`f9ab8bff...`), both against the current canonical profile.

The bounded successor correction is to keep historical receipts immutable and
replace only their current-parity role with those two WPLTO successor pins.
The driver consumed no product-closure link; a new Link-33 attempt nevertheless
requires explicit review under First-Red discipline.  Link 32 is unchanged and
hardware remains blocked.
