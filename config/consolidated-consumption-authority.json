{
  "authority_population_derivation": "perturb every scalar leaf of the explicitly bound candidate manifest through the real compiler-definition renderer; collect every active force-include report, phase-owned output root, LOADADDR geometry, and bound feature/profile population from the materialized invocation",
  "consumer_population_derivation": "every linked *.prg.elf product node under the active build-graph roots owes one adjacent path-and-value consumption receipt",
  "format": "lisp65-consolidated-consumption-authority-v5",
  "output_root_population_derivation": "compiler, linker and producer resolve from the real target; every active qualifier or media-builder graph node registers its actual artifact path; every resolved parent must equal the phase-owned producer root before link",
  "rule": "every candidate input proves bound equals consumed; both consumer and authority populations are derived before link; every build and checking stage resolving a path or value belongs to that population; absence of an explicit authority fails closed"
}
